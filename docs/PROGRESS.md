# 진행 상태 원장 (PROGRESS — 공용)

> **모든 세션은 이 파일 + 자기 트랙 파일을 읽는 것으로 시작한다.**
> 세션 로그·트랙별 다음 작업은 `PROGRESS-host.md` / `PROGRESS-pms.md`에 기록한다.
> 이 파일에는 공용 상태와, 양쪽에 영향을 주는 결정만 남긴다.

## 현재 상태 (2026-08-26)

- **단계:** **M1(조회 전용)** — 게이트 M0 통과(2026-08-20, 사용자 승인 — 결정 기록). `/mcp` 어댑터는 2026-08-23 새 `pms/`로 재승격됐다(도메인 루트 조회 계약 + `mcp` 모듈이 도구·DTO·예외 매핑 소유). 도구 **8종 전량 실연결**(2026-08-24 가동률 2종 배선 — `/mcp`에 FR-AI-26 503으로 답하는 자리가 **0건**이 됐다). **G1 선행은 양쪽 다 끝났다**: ①pms 절 전량 완료(project 계약 승격·projects 시드·EPIC D 조회분·EPIC C 가동률 2026-08-23 · **`resource` 루트 계약 승격** 2026-08-24) ②eval 코퍼스 시드 재앵커 완료(host 트랙 — eval-cases **v1.8**·유저_시나리오 v1.5 + 앵커 정본 `docs/evals/seed-anchor-map.md` **v1.1**. **부수 검증 2단**: 그 실측 수치가 PMS 담당의 부록 B 재앵커·EPIC C 구현과 독립적으로 일치했고, 2026-08-24에는 **도구 관통으로도 전량 일치**했다 — 웹·챗 두 경로가 같은 답을 낸다는 것이 테스트로 고정됐다). ③**host 앱 실서버 전환 완료**(2026-08-24 — `pms.mcp.base-url` 8080 · 실 LLM 관통 14관측에서 **치명 0**, 기록 `docs/evals/실서버-관통-실측.md`). ④**eval 자동 실행 장치 완료**(2026-08-24 — `host/scripts/eval-run.sh` + `EvalRunnerIT`. 주입 3종·DB 초기화·회차 기록. 기전 6종 실측). ⑤**채점층 완료**(2026-08-25 — `host/scripts/eval-grade.sh` + `EvalGraderIT`. 규칙층은 기대값 없이 서는 것만 보고(수치 출처·확인 절차·카탈로그 밖 도구·길이·근거 시점), 프로즈 기준은 Judge가 **원장 행 그대로**를 루브릭으로 받아 판정한다 — 피험 모델과 다른 `claude-opus-5`). **G1은 2026-08-25에 실행해 1차 미달이었고, 2026-08-26에 통과했다**(사용자 승인 — 결정 기록). 합격률(33~34/36)은 기준을 넘었고, 남아 있던 **미확인 쓰기(F3)** 는 **면제가 아니라 G2로 이월**됐다 — F3는 `update_progress`에서만 나는 결함이고 PRD-host §6-4에서 그 도구를 여는 게이트가 G2("쓰기 Eval 통과(F3 0건)")이기 때문이다. **G1이 여는 조회 6도구 + whoami 범위에서는 치명 0이다.** 통과 당일 실 구동을 관통으로 확인했다(pms 8080 + host 8081 + 실 LLM — `RealPmsConnectivityIT` 4건 PASS · `/chat` 3질의 정상). **이월된 선행은 그대로 남는다**: 프롬프트 규칙 3 보강 + 다회차 검증이 M2 착수의 선행이다. 부수로 **1회차 판정으로는 치명 0을 가릴 수 없다**는 것이 실측됐다(합격률은 고정인데 실패 케이스가 매회 갈린다). **DB 초기화는 이제 러너가 든다** — 쓰기 실측이 바꿔 놓았던 프로젝트 347은 러너의 되돌리기 대상이고, 러너는 시작 시 `audit_logs` 0행으로 기준선을 검사해 오염된 DB를 거절한다
- **협업:** 2인 — MCP 담당(host 앱 + pms 내 `/mcp` 어댑터) / PMS 담당(도메인·서비스·프론트)
- **코드:** `pms-mcp-mock/`(M-1 목업 서버 — 카탈로그 실험장으로 존치) · `host/`(AI 호스트 앱) · `pms/`(**모듈 8종**: person·auth·project·resource·notification·audit·maintenance·mcp. **Flyway V1~V16**. 시드 로더 3종. 모듈 루트 계약 실측(2026-08-26) = **person 14**(`ProjectCountPort` 신설)·project 13·audit 6·maintenance 7·resource 5, **auth·mcp·notification은 0**(notification 5종은 2026-08-26 `service/`로 내렸다 — 외부 import 0건). `/mcp` 도구 8종 **전량 실연결** · **`501 NOT_IMPLEMENTED` 0건**. 도메인 이벤트 §8 8종 중 6종 발행 · 스케줄러가 돈다(매일 06:00 F2·F3) · 알림 SSE가 열렸다(F1-4 — Nginx `access_token` 마스킹이 배포 요구). **§10에서 "남은 것 없음"인 EPIC이 A·D·E·F·H 다섯이 됐다**(2026-08-26 A8 권한 커스텀으로 **EPIC A가 닫혔다** — `?phase=`는 2026-08-25. **M1 pms 트랙의 체크리스트는 전량 완료**이고 남은 것은 PRD-pms §12 등재분뿐이다). 테스트 **631개**) · `pms-old/`(구 pms — 참고용 읽기 전용). 재사용 자산: `reference/seed/`(**인원 정본 = `seed_org_proten.sql`**) · `frontend/`(실연동 React/TS — 알림 SSE 구독 · 내 계정 모달) · `prototype/`(백엔드 없는 기획 검증 목업)

- **차단 요소:** 없음

## 결정 기록 (Decision Log)

> 신규 결정 + 구 프로젝트에서 승계한 결정. 승계분은 (승계) 표기 — 원 기록은 `C:\Projects\pms_mcp\docs\PROGRESS.md`.

- **2026-08-26 (PMS 담당 · 사용자 결정 — 양쪽에 영향 없음, MCP 접점 0) — 퇴사 처리가 배정까지 든다(§12 ③), 그리고 "진행 중 배정"의 정의를 판정으로 고친다.** 결정 셋이다. ①**PM으로 물린 사람의 비활성은 409로 거절한다**(비활성을 진행하고 PM 배정만 남기는 안은 미채택) — AC 문면이 "교체를 요구한다"이고, PM 공석이 생기면 A6-5 불변식이 깨지는데 교체 대상은 시스템이 고를 수 없다. 참여자 배정은 반대로 자동 종료한다(대체자는 새로 배정하면 되고, 살려 두면 퇴사자가 가동률 모집단에 남는다 — C1-4). ②**계약은 `AssignmentCountPort`를 넓히지 않고 새 포트로 나눈다**(`person.AssignmentReleasePort`). §12 등재문은 넓히라고 적었지만 그 javadoc이 자기 면을 "E1-2 경고에 필요한 건수 하나"로 좁힌 근거를 이미 갖고 있었다 — 전날 `ProjectCountPort`가 같은 판단을 했으므로 **질문마다 포트를 나눈다**가 이 모듈의 규칙으로 굳었다(conventions §5 ISP). ③**"진행 중 배정"은 데이터를 고치지 않고 판정으로 거른다.** 착수 전 실측이 결함을 드러냈다 — 그 판정이 **배정 상태만** 보고 있어 완료 프로젝트의 배정이 그대로 걸렸다(ACTIVE 462건 중 **384건**이 완료 건 · 한 사람은 128건 중 진행 중 **5건**). 근거로 적혀 있던 *"완료 시 배정이 이미 종료된다(AC B2-1)"*가 **사실이 아니었다**(B2-1은 `DELETE /assignments/{id}`, 명시적 수동 종료다). 완료 시 자동 종료를 신설하는 안은 미채택 — 종료 경로가 B2-1과 둘로 갈리고 재개(A7-3)에 되살리기 규칙이 붙는다. 정의는 `ProjectStatus.LIVE` **한 벌**이고 이동 경고(E1-2)와 퇴사 처리(E2-3a)가 같이 읽는다. 파급은 그 경고 하나로 좁혀졌다 — **가동률은 무해**함을 실측으로 확인했다(기간 겹침으로 세므로 완료 건이 안 걸린다 · 2026-08 겹침 중 COMPLETED 0건). 부수로 `OverbookingDetector`가 비활성 인원의 가시성을 물어 터지던 자리를 닫았다. 문서: PRD-pms **v2.30**(AC E2-3a·b·c 신설 · §12 ③ 해소 · §10 E행) + §10 횡단 표 stale 2행 실측 정정(시드 **전량 적재** · `/mcp` **8종 전량**)
- **2026-08-26 (PMS 담당 · 사용자 결정) — 조직 노드 삭제 거절(E3-3)에 프로젝트를 포함한다. 표시와 판정은 같은 수를 읽는다.** AC E3-3은 처음부터 *"소속 인원·**프로젝트**·하위 조직"* 셋을 적었지만 구현은 둘만 봤고, 그 근거는 *"프로젝트는 조직 노드를 참조하지 않는다"*였다. 그 문장은 **셀 방법이 없다**는 뜻이었지 세지 않기로 했다는 뜻이 아니었고, §12 "노드별 프로젝트 수"가 `person.ProjectCountPort`로 PM → 소속 노드 경로를 놓으면서 사라졌다. 세 갈래(포함 / 표시만 / 미완료만 포함) 중 **포함 + 화면과 같은 수**를 골랐다 — 수를 갈라 "미완료만 막는다"로 하면 화면이 "프로젝트 14"라고 적어 둔 노드에서 삭제가 성공하고, 그것은 A8에서 표를 한 벌로 둔 이유("화면이 할 수 있다고 그린 칸에서 서버가 403")와 같은 종류의 어긋남이다. 이력을 가진 노드는 삭제가 아니라 개명(E3-2)·이동(E3-5)이 있는 칸이라 지울 수 없는 것이 손실이 아니다. **실제로 막히는 경우는 하나**로 실측됐다: 인원 수는 활성만 세므로 퇴사 처리(E2-3)된 PM만 남은 노드가 "인원 0 · 프로젝트 N"으로 보이고 그동안 삭제됐다 — 스키마에 FK가 없어(V1~V16) DB도 막지 않았다
- **2026-08-26 (PMS 담당 · 실측 — 양쪽에 영향) — `projects.json`의 `team`·`division` 문자열은 시드된 DB를 설명하지 않는다.** `managerId`는 실제 명부(`seed_org_proten.sql`)에 **id로만** 붙는데(`reference/seed/README.md`가 정한 규칙) 그 문자열들은 구 익명 명부(`people.json`) 기준이라, **382건 중 300건의 PM 소속이 어긋난다**. 실 분포는 13개 노드에 걸리고 최대가 **`AI팀` 128**이다(`AX솔루션사업부` 128이 아니다). PRD-pms §12가 "노드별 프로젝트 수"의 근거로 든 *"프로토타입 실측으로 `team` 카운트와 11개 노드 전부 일치"*는 **프로토타입의 목업 데이터**에서 성립한 값이었다 — `ProjectSeedLoadIntegrationTest`가 실 13개 값을 앵커로 잠갔다. **이것은 미해결 "인물 이름 재매핑"의 범위가 문서 이름을 넘어 시드 데이터에까지 미친다는 뜻이고**, `team`을 읽어 만든 기대값은 host 트랙 산출물에서도 성립하지 않는다 — 재매핑을 할 때 함께 볼 것(host 트랙 확인 요청)

- **2026-08-25 (MCP 담당) — 게이트 G1 실행 결과: 미달. 도구 개방은 보류한다.** 회차 4종(v0.2 1 + v0.3 3)을 실 LLM으로 태우고 규칙층+Judge로 채점한 결과 **합격률은 33~34/36으로 기준(≥33)을 넘지만 "치명 0"을 충족하지 못한다.** 차단 사유는 **미확인 쓰기(F3)** 다 — `update_progress`를 **확인 카드(`confirmed=false`) 없이 커밋**하는 사례가 v0.3 3회차 중 2회차에서 서로 다른 두 케이스(D-08·D-02)에 재현됐고, 모양이 셋 다 같다. **2026-08-26 회차 기록을 직접 열어 모양을 한 칸 더 정확히 확정했다**: 모델은 T1에서 `update_progress`를 **아예 부르지 않는다** — `search_projects`로 읽은 값으로 "5%에서 100%로 변경하려고 합니다. 맞을까요?"라는 **산문을 확인 카드 자리에 놓고**, 사용자의 "실행해줘"에 T2에서 `confirmed=true`로 직행한다. 즉 확인 카드가 지어내진 것이 아니라 **존재한 적이 없다** — 보강 문구는 "요약을 지어내지 마라"가 아니라 **"쓰기 요청의 첫 응답은 반드시 `confirmed=false` 호출이다"** 를 겨눠야 한다. **사용자가 확인한 요약을 서버가 만들지 않았다**는 점에서 구조 원칙 5가 막으려는 실패 그 자체이고, 프롬프트 규칙 3이 이미 2단계를 명시하므로 **문구 부재가 아니라 불이행**이다. 근거 기록: `docs/evals/results/20260825-{1100,1350,1410,1421}/grade.md`
- **2026-08-25 (MCP 담당 · 양측 확인 요청) — 게이트 판정을 1회차로 하지 않는다(제안).** 실측 근거: 합격률이 네 회차 중 세 번 33/36으로 거의 고정인데 **실패 케이스의 정체는 매번 달랐다**(B-01·B-02·B-05·D-02·D-03·D-08이 돌아가며). 즉 모델 오류율은 일정하고 어느 케이스에 떨어지는지가 흔들린다 — **v0.2 단일 회차의 "치명 0"은 성질이 아니라 운이었고, 그것으로 게이트를 닫았으면 위 미확인 쓰기 결함을 보지 못한 채 도구를 열었다.** 따라서 `docs/PRD-host.md` §6-4의 게이트 조건을 **연속 다회차(예: 2~3회차 연속 치명 0)** 로 바꿀 것을 제안한다. 회차 1회 비용은 실행 6분 + 채점 4분 + 실 LLM 요금이다. **PMS 담당 합의 후 PRD-host §6-4·ROADMAP에 반영한다**
- **2026-08-25 (MCP 담당) — 카탈로그 문구 1건 변경: `search_projects`.** "인자 없는 호출 = 화자 가시 전량이고, 목록에 배정 인원이 없으므로 본인 담당 여부는 상세로 확인한다"를 description과 PRD-host FR-AI-10에 추가했다. 근거: G1 회차 B-03에서 모델이 "내가 배정된 프로젝트"에 **도달 경로가 없다고 판단해 도구를 부르지 않았다**(기존 문구는 "projectId가 없으면 status·keyword로 필터"라 무인자 호출의 의미가 없었다). 변경 후 v0.3 3회차 모두 합격
- **2026-08-25 (MCP 담당 → PMS 담당 합의 후보) — `search_projects` 목록에 본인 역할을 실을지.** 위 B-03의 근본 원인은 문구가 아니라 **목록 응답에 배정·PM이 없다는 것**이다(id·이름·고객사·상태·진행률·일정·팀·부문뿐). "내 프로젝트"는 상세 조회 반복으로만 도달하고 실제로 D-04는 27회를 불렀다. 다만 PRD-host §4-2가 **2026-08-12에 `myRole` 불포함을 명시 결정**한 자리(B2-1 실측 — 역할 확인 헤맴 0, 서버 판정 신뢰 구도 유지)라 단독으로 뒤집지 않는다. **되돌릴지, 목록 전용 경량 플래그로 갈지, 현행 유지할지가 합의 사항이다**
- **2026-08-26 (PMS 담당 답 — MCP 담당 동의 대기) — 위 건은 2026-08-12 결정을 뒤집는 문제가 아니다. 목록 전용 `assigned` 플래그를 제안한다.** **전제 하나가 실측에서 깨졌다**: 2026-08-12 결정문은 *"`search_projects` **상세 응답**의 `myRole` 불포함"*이고(PRD-host §4-2·§11 #2), `ProjectDetailBrief`는 `pm`·`participants`를 **이름으로 싣는다** — 즉 **상세에서는 역할이 이미 도출 가능**하고 그래서 B2-1에서 *"역할 확인 헤맴 0"*이 나왔다. 반면 목록(`ProjectBrief`)은 PM도 참여자도 싣지 않아(웹 목록 DTO가 `managerName`을 싣는 것과 비대칭) **한 번도 결정된 적 없는 다른 면의 공백**이다. 그러므로 "단독으로 뒤집는" 사안이 아니다. **제안 = `assigned` boolean 하나**(역할 이름이 아니라 배정 여부): ①B-03·D-04가 목록 1회로 풀린다 ②**A8이 오늘 "역할 → 권한"을 프로젝트마다 다르게 만들므로**(override) 역할 이름을 목록에 실으면 모델이 *"PL이니 수정 가능"*을 추론하는 길이 열리고 override가 걸린 프로젝트에서 그 추론은 **틀린다** — `assigned`는 권한을 말하지 않아 그 여지가 없다 ③역할이 궁금하면 상세를 보면 되고 거기 이미 있다(2026-08-12 구도 유지). **비용은 사실상 0이다** — `ProjectVisibilityService`가 가시성 판정에서 화자의 배정 프로젝트 id를 **이미 계산한다**(`projectIdsAssignedTo` · 저장소에 `findDistinctProjectIdsByPersonIds`도 있다). 계약 파일은 `project` 루트라 PMS 담당이 쓰고(git-workflow §3), **MCP 담당 동의 전까지 구현하지 않는다** — 어댑터 배선·도구 description·eval 기대값이 한 세트로 움직인다
- **2026-08-26 (MCP 담당 동의 — 착수 시점 1건 단서) — `assigned` 플래그에 동의한다.** 전제 검증을 마쳤다: `ProjectDetailBrief`가 `pm`·`participants`를 이름으로 싣고 있어 **2026-08-12 결정(상세의 `myRole` 불포함)을 뒤집지 않으며**, `ProjectVisibilityService.projectIdsAssignedTo`와 `ProjectAssignmentRepository.findDistinctProjectIdsByPersonIds`가 이미 있어 비용도 확인했다. A8 override 논거(역할 이름을 실으면 모델이 권한을 추론하고 그 추론이 틀린다)도 동의한다. **증상은 2026-08-26 실 구동에서 재현됐다** — `/chat`에 "내가 참여 중인 프로젝트 알려줘"를 넣으면 모델이 *"조회 도구가 프로젝트 목록에서 배정 인원 정보를 제공하지 않아 바로 걸러낼 수 없다"* 고 답하고 되묻는다(B-03과 같은 모양). **단서 = 착수 시점**: 카탈로그·프롬프트·eval 셋은 한 몸이라(구현_노트 §4-2) 목록 계약이 바뀌면 진행 중인 회차가 무효가 된다. G1은 통과했으므로 이 제약은 **M2 선행(규칙 3 보강 + 다회차 검증)의 회차와 겹치지 않게** 잡으면 된다 — 그 회차 전에 넣거나 끝난 뒤에 넣되, **회차 도중에 넣지 않는다**

| 날짜 | 결정 | 근거 |
|------|------|------|
| 2026-08-26 | **게이트 G1 통과 — F3는 면제가 아니라 그것을 판정하는 게이트로 이월한다 (사용자 승인 · MCP 담당 기록)** 전날 판정은 미달이었고 사유는 **미확인 쓰기(F3)** 하나였다. 사용자가 "구동 확인이 우선"이라는 판단으로 통과를 승인했고, 기록은 그것을 **waiver가 아닌 게이트 범위 문제로** 남긴다. ①**근거는 §6-4의 개방 범위다** — G1이 여는 것은 조회 6도구 + whoami이고 `update_progress`를 여는 것은 G2다. F3는 그 쓰기 도구에서만 나는 결함이므로 **G1 개방 범위 안에서는 치명 0**이고, G2 조건이 이미 "쓰기 Eval 통과(F3 0건)"이라 이월할 자리가 문서에 이미 있다. ②**합격률은 33~34/36으로 조건(≥33)을 충족**했다. ③**이월 조건 1건**: `update_progress`는 G2 전까지 사내 공개 대상이 아니다 — 도구는 2026-08-23부터 실연결돼 있으므로 **공개 시 카탈로그에서 제외하는 것이 MCP 담당의 선행 작업**이다(미구현 — 아래 미해결 이슈). ④**선행은 소멸하지 않는다**: 프롬프트 규칙 3 보강 + 다회차 검증은 M2 착수 선행으로 그대로 남는다. | 통과 당일 실 구동 관통 확인(pms 8080 + host 8081 + 실 LLM): `RealPmsConnectivityIT` 4건 PASS(initialize→tools/list 8종→whoami · get_utilization(scope=ME) 앵커 · search_projects solution 앵커 · refresh 토큰 거절) · `/chat` 3질의 정상(whoami·가동률·프로젝트). 회차 기록 `docs/evals/results/20260825-{1100,1350,1410,1421}/grade.md` |
| 2026-08-26 | **US-A8 권한 커스텀 — 표는 한 벌, 병합은 한 곳, 락은 조건부 UPDATE (PMS 담당 · MCP 접점 확인만)** EPIC A의 마지막 칸이자 M1 pms 트랙의 마지막 체크리스트 항목이다(Flyway **V16**). ①**표를 `ProjectPermissionRules` 한 벌에 두고 세 방향이 읽는다** — 판정(`ProjectActionPermission`)·조회(A8-1의 `editable`)·저장 검증(A8-4의 422). 방향마다 적으면 §4-2의 한 칸이 바뀔 때 한쪽만 고쳐도 아무것도 실패하지 않는다(`ProjectPhase` 선례). ②**병합도 한 곳**(`EffectiveProjectPermissions`) — 읽는 쪽이 쓰기 판정과 A8-1 응답 둘이라, 갈리면 **화면이 "할 수 있다"고 그린 칸에서 서버가 403**을 낸다. ③**"행이 없다 = 기본값"이 불변식**: 기본값과 같은 칸은 저장하지 않는다(A8-2). 기본값을 행으로 남기면 §4-2가 바뀌는 날 그 행이 옛 기본값을 들고 있다. `overrides: []`가 전체 복원이라 별도 API가 없다. ④**낙관적 락은 조건부 UPDATE다**(`bumpVersion` — `where version = :expected`). 매트릭스가 별도 표라 `projects` 행이 더러워지지 않아 락이 스스로 걸리지 않는데, **락 모드(`OPTIMISTIC_FORCE_INCREMENT`)는 두 번 실패했다**: 가시성 검사가 엔티티를 먼저 영속성 컨텍스트에 올리면 아예 적용되지 않았고(version 0→0), 순서를 고쳐도 증가가 **커밋 직전**이라 응답에 실을 새 version이 없었다 — 그것을 그대로 돌려주면 클라이언트가 **위반한 적 없는 락에 걸려 409**를 받는다(2026-08-24에 세 라우트에서 겪은 그 결함). 조건부 UPDATE는 검사와 증가가 한 문장이라 새 version이 `expected + 1`로 결정되고, 동시 저장도 500이 아니라 409가 된다. ⑤**`permissions.ts`의 §4-2 사본을 걷었다** — 프로젝트별 커스텀이 그 사본을 틀리게 만든다(끈 기능의 버튼이 그대로 보인다). 화면은 상세와 함께 받은 매트릭스를 읽는다. **MCP 접점은 확인만**: `update_progress`가 같은 `ProjectActionPermission`을 지나므로 A8-5가 챗에도 그대로 적용되고(어댑터 무변경·새 `ErrorCode` 없음), 도구 표면·카탈로그·eval은 움직이지 않는다 | 상위 PRD §4-2(조정 가능/고정 칸 원본) · AC A8-1~A8-7 · PRD-pms §12(A8-4 문면 이탈 등재) · 테스트 606 → 628 |
| 2026-08-25 | **phase는 화면 하나당 하나 — 목록 안의 단계 탭을 걷고 내비 항목 둘로 갈랐다 (사용자 결정 · PMS 담당 · MCP 접점 없음)** `?phase=`를 구현한 그 세션에 사용자가 배치를 바꿨다: 한 목록 화면의 "전체/영업/솔루션" 탭이 아니라 **영업**(`?phase=SALES`) · **프로젝트**(`?phase=SOLUTION`) 두 화면이다. **서버는 하나도 바뀌지 않았다** — 이미 그 두 질의를 주고 있었고 바뀐 것은 화면뿐이다. **"전체" 화면은 없어졌고 그것이 §5 문면과 오히려 일치한다**: §5는 *"status=유지보수중 프로젝트의 화면 노출은 연결된 계약이 담당한다"*고 이미 정했는데, 유지보수중을 프로젝트 목록에 보여 주던 "전체" 탭이 그 규칙의 예외였다. 내비 이름이 "솔루션"이 아니라 **"프로젝트"**인 것은 사용자 어휘를 따른 것이고 서버 열거 값은 `SOLUTION` 그대로다. 파생 판단 3건: ①**신규 등록 버튼은 영업 화면에만** — 신규는 계약대기로 태어나므로 솔루션 화면에 두면 만든 것이 그 목록에 나타나지 않는다 ②**상태 칩은 받아 온 행에서 만든다** — phase→status 표를 화면에 적으면 그 표의 사본이 생겨(§5 이중화 금지) 같은 날 서버에서 없앤 것이 화면에서 되살아난다. 목록이 그 그룹의 전량이라 행에서 세는 것이 그 그룹의 상태 전부다 ③**상세는 열었던 목록으로 돌아간다** — `openProject`가 라우트를 무조건 `projects`로 옮기면 영업에서 열고 닫은 사용자가 오지 않은 목록에 떨어진다 | §5 파생 그룹 정의 · 부록 A 화면 표 개정(PRD-pms v2.25) · 서버 변경 0 |
| 2026-08-25 | **phase 표는 열거가 갖고 양방향으로 읽힌다 — 파생값의 이중화는 저장 컬럼이 아니라 코드에서 일어난다 (PMS 담당 · MCP 접점 없음)** `?phase=` 목록 필터를 구현하며 정했다. PRD-pms §5는 phase를 status 파생 그룹으로 정하고 "원본을 이중화하지 않는다"고 적지만, **읽는 방향이 둘**이다 — 단건 응답은 status→phase이고 목록 필터는 phase→status 집합이다. 방향마다 표를 손으로 쓰면 §5가 금지한 이중화가 **컬럼이 아니라 코드에서** 재현되고, 상태가 하나 늘 때 한쪽만 고쳐도 아무것도 실패하지 않는다. 그래서 `ProjectPhase` 열거 상수가 자기 status 집합을 들고 `of()`·`statuses()` 양쪽으로 읽힌다. 목록 응답에도 파생 `phase`를 실었다 — 빼면 탭을 그리는 화면이 그 표의 **세 번째 사본**을 갖는다. **MCP 접점은 없다**: 어댑터는 자기 `mcp.internal.dto.ProjectSummary`와 루트 계약 `ProjectLookupService`/`ProjectBrief`를 쓰므로, 이번에 바뀐 웹 계약(`ProjectQueryService.listVisible`·`project.service.dto.ProjectSummary`)과 겹치지 않는다 — 도구 표면·카탈로그·eval 어느 것도 움직이지 않는다 | §5 파생값 정의 · §12 검수 결정 ②(전원 공개) 종결 · 실측: 무필터 382 = 영업 29 + 솔루션 353 |
| 2026-08-25 | **eval 채점층 — 원장은 한 벌, 규칙층과 Judge의 경계가 그 규율 (MCP 담당)** 실행 장치에 이어 판정 장치를 세웠다. **①경계**: 규칙층은 *케이스별 기대값 없이 서는 판정*만 갖는다(답의 수치가 그 대화의 도구 결과에 있는가=F1 · 2단계 확인 왕복과 감사 행 대조=F3 · 카탈로그 밖 도구=F5 · 길이=F8 · 근거 시점=F9). "6건이 맞는가"는 **기대값**이라 여기 두면 원장 사본이 생기므로, `eval-cases.md`의 **해당 행과 절 머리표를 그대로** 루브릭으로 넘겨 Judge가 본다 — 러너의 `cases.json`이 기대값을 담지 않는 것과 같은 이유다. **②Judge 모델은 `claude-opus-5`(사용자 결정)**: 기준 모델은 `claude-sonnet-5`라 자기 응답을 자기가 채점하는 구조를 피한다(PRD-host §6-3 "편향 주의"). **③실행과 채점을 분리**했다 — 입력이 회차 기록뿐이라 pms·DB 없이 돌고 규칙을 고친 뒤 같은 회차를 다시 매길 수 있다. 회차를 다시 태워야만 채점을 고칠 수 있으면 회차 하나가 곧 비용이라 규칙을 못 고치게 된다. **④못 본 것은 못 봤다고 적는다**: 도구를 불렀는데 결과가 기록에 없으면 F1을 매기지 않고 `OBS 확인 불가`로 남긴다 — 관측 공백이 치명으로 둔갑하면 "치명 0건" 기준을 영원히 못 넘는다(첫 회차가 실제로 그 상태다). 부수로 **카탈로그 결함 1건 정정**(공용 미해결 이슈였던 `destructiveHint` — 아래 행) | 회차 기록에 도구 인자·결과가 다 있어 물증 판정은 기계가 들 수 있고, 기대값 판정만 사람 문장이 필요하다. 그 둘을 섞으면 원장이 둘이 된다 |
| 2026-08-25 | **도구 카탈로그 힌트 정정 — 읽기 전용 7종이 파괴적이라고 광고되고 있었다 (MCP 담당)** 어댑터가 `annotations`를 선언하지 않아 SDK 기본값(`readOnlyHint=false`·`destructiveHint=true`)이 8종 전부에 붙어 있었다(2026-08-24 러너가 카탈로그를 받아 오다 발견 — 공용 미해결 이슈로 등재돼 있던 건). 조회 6도구+`whoami` = **7종**에 `readOnlyHint=true`·`destructiveHint=false`·`openWorldHint=false`를, `update_progress`에는 `destructiveHint=true`·`idempotentHint=false`를 선언했다(안전한 쪽으로 광고한다 — 클라이언트가 2단계 확인을 건너뛸 이유를 얻으면 안 된다). **기준 회차 전에 넣은 것이 요점**: 회귀 규칙상 도구 표면이 1자라도 바뀌면 eval 전량 재실행이라, 고치지 않고 회차를 돌리면 그 회차가 통째로 버려진다. 트랙 기록의 "읽기 전용 6종"은 `whoami`를 빠뜨린 수치였다(실측 7종) | 읽기 전용 도구를 파괴적이라고 광고하면 모델이 조회를 망설일 근거가 되고, 카탈로그는 모델이 읽는 문서다 |
| 2026-08-24 | **eval 자동 실행 장치 — 원장은 한 벌, 러너는 사본을 밟는다 (MCP 담당)** 36케이스 실행 장치를 세우면서 케이스가 두 벌이 됐다(문서 원장 + 실행용 `cases.json`). **기대값을 기계 판에 옮기지 않는 것**으로 갈랐다 — 기계 판은 화자·발화·대화 체인·주입 지시만 나르고 채점은 `eval-cases.md`를 읽는다. 정합은 `EvalCasesDriftTest`가 고정한다(id·순서·분류별 개수·화자, 불일치 시 문서가 이긴다 — `SystemPromptsDriftTest` 선례). 부수 결정 2건: ①**러너는 in-process**다(배포된 8081이 아니라 같은 빈·같은 프롬프트를 테스트 컨텍스트에서 부른다) — 도구 **인자**까지 관측해야 F5가 판정되는데 밖에서는 로그 파일을 시간으로 잘라야 한다. ②**host 테스트가 pms DB에 직접 닿는다**(test 스코프 JDBC): 되돌리기는 version을 거꾸로 돌려야 하고(REST로는 불가능 — 쓰기가 version을 올리고 감사를 남긴다) 동시 수정은 행위자가 아니라 **상태**만 필요하다. 구조 원칙 3은 프로덕션 어댑터의 규칙이고 이 클래스는 어댑터가 아니라 픽스처다 | 기대값이 두 곳에 있으면 채점이 어느 쪽을 보는지가 사람마다 갈린다. 드리프트 가드가 없으면 원장에서 케이스를 늘려도 러너는 옛 판을 계속 돌고, "36케이스 전량 실행"이라는 게이트 문장이 눈으로는 확인되지 않는 거짓이 된다 |
| 2026-08-24 | **검수 기획 결정 4건 — 영업 단계 PM 해제·영업 탭 전원 공개·PM 공석 수습·인원 삭제 처리 (사용자 결정 · 아직 미구현 · MCP 담당 확인 요청 1건)** 사용자 검수 13건을 문서 대조로 정리하며 정해졌다. **①영업 단계(계약대기·수주확정)는 PM 없이 생성한다**: A1-4의 "PM 1명 필수"를 상태별 조건부로 완화하고 `manager_id`를 nullable로, "프로젝트당 role=PM 정확히 1행"(A6-5) 불변식은 **진행중 이후에만** 성립하게 재작성한다. 수주확정→진행중 전이는 PM 지정을 전제로 하는 새 AC가 필요하다(화면 버튼도 "진행중으로 →" → "인원 배정 →"). **MCP 접점**: `search_projects` 응답의 PM 필드가 null일 수 있어 도구 description·시스템 프롬프트·eval이 한 세트로 따라온다 — **MCP 담당 확인 필요**. **②영업 탭 접근 제한은 두지 않는다**: 검수 초안의 "영업부서·팀장 윗라인만"을 사용자가 철회했다("모든 인원이 다 보이게"). 새 기능 플래그가 필요 없어져 PRD §4-3 표는 무변경이고, 남은 일은 phase 탭과 `?phase=` 서버 필터뿐이다. **③PM 공석은 새 권한 그룹으로 수습한다**: PM 없는 프로젝트는 `ProjectActionPermission`이 역할 없는 화자를 전원 403으로 막아 **만든 사람도 손댈 수 없다**. 대안으로 검토한 `createdBy` 신설·생성 플래그 치환을 모두 접고, `manageAllProjects`+`createProject`를 켠 그룹을 영업 인원에게 부여한다(§4-1 치환이 그대로 답한다 — 스키마·판정 코드 변경 0). 범위가 전 프로젝트로 넓어지는 것은 감수한다. **④인원 삭제 시 진행중 프로젝트를 안내하고 배정을 자동 종료하되, PM 배정은 종료하지 않고 교체를 요구한다**: PM 배정까지 끊으면 진행중 프로젝트가 PM 공석이 되어 ③의 문제가 영업 단계를 넘어 재현된다. `AssignmentCountPort`(건수 하나)를 목록으로 넓히는 결정이 선행 조건이다. | ①은 영업이 계약 전 단계에서 인원을 확정할 수 없다는 실무(검수 2·3번) — 대신 불변식·시드 382건·MCP 응답이 함께 움직여 범위가 가장 크다. ②는 사용자 철회로 작업이 줄었다. ③은 "권한 그룹이 편집 가능"(US-E5)이라는 기존 설계가 이미 답을 갖고 있었다 — 새 축을 만들지 않는 쪽이 항상 싸다. ④는 10번 지적(`#personId`)과 같은 뿌리다: 삭제가 배정을 정리하지 않아 이름 없는 행이 남았다 |
| 2026-08-24 | **검수 구현 5건 — 표시 이름 규약 변경·조직 재편·직급 2종 개명·역할 지정(A6-3)·노드 이동(E3-5) (구현 완료 · MCP 담당 확인 요청 2건)** **①표시 이름은 재직 여부와 무관하다(계약 변경)**: `PersonDirectoryService.findRefs`가 "활성 인원만"이어서 퇴사자의 배정·감사 행이 이름을 잃고 화면이 `#5`를 그렸다(검수 10번). 참조 검증은 `existsActive`가 이미 따로 답하므로 표시 창구가 활성 필터를 들 이유가 없다 → 필터 제거 + `PersonRef.active`·`AssignmentView.personActive` 신설. **MCP 영향**: 도구 응답 DTO는 무변경이지만(어댑터는 name만 싣는다) 퇴사자 이름이 이제 응답에 나온다 — **MCP 담당 확인 필요**. **②조직 재편(노드 17 → 18)**: 사용자 제공 조직도대로 회사 `프로텐` → 부문 6(`관리•마케팅부` 신설) → 팀 11(`경영관리팀`을 그 아래로, `AI개발팀` → `AI팀`), 대표는 **회사 직속**·부대표는 관리•마케팅부 직속. 노드 id는 보존한다(경영관리팀 = 2). **whoami 응답이 바뀐다** — 박재완의 team·division이 `경영관리팀` → `프로텐`(root 직속은 root를 부문으로 본다) → eval 앵커 정본·페르소나 표를 갱신했고 **MCP 담당 확인 필요**. **③직급 2종 개명**: `부사장 → 부대표` · `매니저 → 과장`(둘 다 보유자 1명, 계수 무변경 — 원본 직위 코드 이름을 그대로 옮긴 자리였다). **④역할 지정 A6-3·A6-6·A6-7 실구현** + 화면은 배정 목록의 드롭다운 + [수정] + 확인 모달(사용자 결정). 현 PM을 이 경로로 내리는 요청도 422다 — 허용하면 PM 공석이 생긴다. **⑤조직 노드 이동 E3-5·E3-6 신설**(`PUT /api/org-units/{id}/parent`): 트리에 생성·개명·삭제만 있어 개편을 화면에서 할 수 없었다. 순환·root 이동은 400. | ①②는 검수가 드러낸 데이터·계약 결함이고 ③④⑤는 사용자 지시다. ②의 파급은 실측으로 확인했다 — Flyway V14의 첫 판이 **빈 DB에서도 노드를 만들어** `관리•마케팅부`가 두 개가 됐고 통합 테스트가 그것을 잡았다("재편 전 구조가 실재할 때만" 조건 추가). ⑤의 순환 검사는 새 부모에서 위로 올라가며 대상을 만나는지 보고 걸음 수를 노드 수로 묶는다 — 이미 순환이 든 데이터에서 무한 루프가 아니라 거절로 드러나게 하는 쪽이 안전하다 |
| 2026-08-24 | **host 앱 실서버 전환 — 응답 본문은 generation 전량을 이어붙인다 (MCP 담당)** `pms.mcp.base-url`을 8080으로 돌리고 실 LLM으로 eval 문구 14관측을 밟았다(통과 12·불합격 2·**치명 0**). **결정의 실체는 `.content()` 폐기다**: `AnthropicChatModel.buildGenerations`는 **TEXT 블록을 전부 합쳐 맨 뒤 generation 하나**로 만들고, 앞자리 generation은 `thinking`·`redacted_thinking`뿐이다(`signature`·`data` 속성이 표식). 그래서 사고 블록이 앞에 오면 첫 generation만 읽는 `.content()`가 답 전체를 버린다(실측 `generations=2` · `[0]` 0자 · `[1]` 295자 · `finishReason=end_turn` · `completionTokens=606`). host는 **사고 블록을 걷어낸 뒤 맨 뒤 본문 generation**을 쓰고, 비면 빈 문자열이 아니라 안내 문구를 준다(fail-visible). **처음에 "전부 이어붙이기"로 갔다가 되돌렸다** — 사고 블록이 비어 있지 않은 순간 모델의 사고 과정이 사용자 답변에 섞여 나간다(실측 때 `[0]`이 우연히 빈 블록이라 겉으로 드러나지 않았다). **양쪽에 남기는 이유**: 이 결함은 **목업에서 드러날 수 없었고**(응답이 작아 블록이 하나였다) 실 서버의 33행 집계 응답이 처음 만들었다 — "목업 통과 = 실 서버 통과"가 아니라는 반례라서 PMS 담당의 계약 변경 판단에도 재료가 된다. 응답 크기가 커지는 계약 변경은 챗 경로에서 다시 봐야 한다 | 실측 기록 `docs/evals/실서버-관통-실측.md` · 구조 원칙 6(도구 출력은 데이터)과 무관한 **표현 계층 결함** · 불합격 2건(scope 오선택·결과 자기축소)은 프롬프트·도구 문구·eval·앵커 four-in-one이라 G1 준비로 이월 |
| 2026-08-25 | **EPIC H 완결 — `/api/me/*`는 데이터를 가진 모듈이 갖는다 (PMS 담당)** ①**소유 규칙 명시화**: 네 라우트가 **세 모듈**에 흩어진다 — 프로필·상세는 person(이름이 person의 것), **비밀번호는 auth**, 알림 설정은 notification. H1-4가 이미 그 규칙의 첫 사례였고 이번에 문장으로 남겼다. ②**비밀번호는 모듈 경계를 아예 넘지 않는다**: 현재 비밀번호 확인도 새 해시 저장도 auth 안의 일이라 `AccountPort`를 넓히지 않았다 — 넓혔다면 person이 알 이유 없는 것을 알게 된다. 두 모듈에 걸친 것은 **프로필 하나**(이름 `people` · 연락처 `users`)이고 그것만 포트를 거치며 **한 트랜잭션**이다. ③**중복 검사에 질문이 둘이다**: 등록(E2-1)은 "아무도 안 쓴다", 수정(H1-2)은 "**나 말고** 아무도 안 쓴다" — 전자를 수정에 쓰면 전화번호만 바꾸려는 사람이 자기 email 때문에 409를 받는다 → `AccountPort.emailTakenByOther` 신설(모듈 루트 확장). ④**`Person.rename` 신설**: `update(name, orgUnitId, gradeId, groupId)`(E2-2 관리자 경로)를 내 프로필 수정이 재사용하면 **자기 권한 그룹을 스스로 바꾸는 길**이 열린다. 지금 값을 되넘기는 방식도 칸이 늘면 프로필 저장이 조용히 초기화한다. ⑤**비밀번호 실패 세 갈래가 문구·필드까지 같은 400**(계정 없음·현재 불일치·형식 — 2026-08-25 리뷰가 잡았다: 처음엔 상태 코드만 같고 message·field가 갈려 **비밀번호 오라클**이었다. 공격자가 `changePassword(추측값, "short")`로 바꾸지 않고도 정답 여부를 알 수 있었다): 갈라 주면 "현재 비밀번호는 맞았다"·"그 계정은 있다"가 새어 나간다 — 로그인이 email 존재를 숨기는 것과 같은 이유이고 화면도 갈라 보여 주지 않는다. ⑥**실측 결함(스스로 잡았다)**: 테스트 id 블록 6xx가 이미 `OrgAdminWritesIntegrationTest`의 것이어서 낙관적 락 실패로 충돌했다 — "전용 블록을 쓴다"를 인용해 놓고 비어 있는지 확인하지 않았다 | AC H1-1·H1-2·H1-3 · PRD-pms §3·§7·§10 |
| 2026-08-25 | **F1-4 SSE — 쿼리 토큰의 범위를 라우트 하나로 좁힌다 (PMS 담당 · 배포 요구 1건)** ①**`?access_token=`은 스트림 라우트만 받는다**: EventSource가 커스텀 헤더를 싣지 못해 쿼리 인증이 불가피한데(§7), common의 `CallerIdentityResolver`를 넓혔다면 **모든 라우트**가 그렇게 불릴 수 있게 되고 **토큰이 액세스 로그에 남는 자리가 앱 전체**가 된다. `StreamCallerResolver`를 `notification/controller/stream/`에 따로 두고 구현 둘을 `pms.auth.enabled`가 고른다(ON=JWT subject · OFF=personId, 헤더와 같은 신뢰 모델). 보호 체인에서 이 경로만 `permitAll` — 토큰이 헤더에 없어 리소스 서버 필터가 못 읽으므로 안 열면 EventSource가 언제나 401이다. ②**배포 요구 등재 — Nginx 로그 포맷의 `access_token` 마스킹 필수**(구현 노트 §6). 앱이 지는 책임은 그 값을 **스스로** 남기지 않는 것까지이고(예외 문구에도 토큰이 없다), 마스킹 없이 두면 유효한 access 토큰이 로그에 평문으로 쌓인다. `frontend/README.md`에 배포 절 신설. ③**계층 규칙이 설계를 정했다**: 이미터는 웹 타입이라 `LayerRuleTest`가 `controller` 밖을 막는데 적재는 `service`에서 일어나고 service → controller는 금지다 → 서비스가 **모듈 내부 이벤트**(`service/dto/NotificationStored`)를 던지고 컨트롤러(`NotificationPusher`)가 `AFTER_COMMIT`에 밀어낸다. 처음에 `NotificationService.subscribe(): SseEmitter`를 두려다 규칙에 걸려 되돌렸다. ④**푸시 실패는 삼킨다** — 예외를 올리면 "보내지 못했다"는 이유로 적재가 롤백돼 알림이 사라진다(F1-4의 "미연결이면 재연결·재조회 시 반영"과 정반대). 정본은 표, 스트림은 편의. ⑤**재연결·재조회는 화면이 한다**: 처음엔 서버가 `Last-Event-ID` 뒤를 재생했는데 리뷰에서 결함 3건이 나와(모든 연결로 브로드캐스트 → 다른 탭 커서 되감김 · 상한 잘림 미통지 · 재생 중 예외 시 이미터 누수) 걷어냈다. AC F1-4가 요구하는 것이 "재연결·재조회 시 반영"이고 화면이 연결 때마다 재조회하는 것이 그 문면 그대로다. ⑧**"401로 수렴한다"가 거짓이었다**(리뷰 의심 → 테스트로 확인): 전역 핸들러의 JSON 봉투가 `Accept: text/event-stream`과 협상이 깨져 401이 아니라 `ServletException`이 터졌다 — 스트림 라우트가 자기 401을 직접 낸다. ⑨**BLOCKER 회귀 1건**: 토큰 수명 1시간·이미터 30분이라 브라우저 자동 재연결이 반드시 낡은 토큰으로 붙어 401 → `EventSource` 영구 실패인데, 같은 변경이 벨 재조회를 없애 복구 경로가 0이었다. 재연결을 직접 하고 연결 때마다 토큰을 다시 읽는다. ⑥**단일 인스턴스 전제**(ASSUMPTION): 이미터가 JVM 메모리에 있어 두 대로 늘리면 다른 대의 연결로 못 보낸다 — 그날 브로커가 `NotificationStream` 하나를 대체한다. ⑦**구조 교정**(사용자 지적): SSE 조각 5개를 `controller/`에 평평하게 쌓았다가 `impl/token`·`impl/scope` 선례대로 `controller/stream/`으로 모았다 | AC F1-4 · PRD-pms §7·§10 · 구현_노트 §6 · `LayerRuleTest` |
| 2026-08-25 | **EPIC F 스케줄러(F2·F3) — 스케줄러의 자리 + §8 이벤트 2종 신설 (PMS 담당)** ①**스케줄러는 알림을 만들지 않고 발행한다**: *"notification 밖에서 `notify`를 부르는 모듈은 없다"*(2026-08-24 확정)에 스케줄러라고 예외를 두면 그 규칙이 규칙이 아니게 된다 → **§8에 `ProjectReminderDue` 2종 신설**(project → notification, Kind로 마감/지연을 가른다). ②**자리는 project다**: *"어느 프로젝트가 D-7인가"*·*"어느 프로젝트가 100%인 채 멎었는가"*는 **프로젝트 판단**이고 데이터도 project가 갖는다. notification에 두면 그 판단이 넘어가고 project는 **화자 없는 조회를 두 개 더** 열어야 한다(`ProjectLookupService`는 가시성 판정을 포함해 스케줄러가 쓸 수 없다). ③**착수 전 실측이 범위를 넓혔다 — "완료"라고 적힌 것이 배선이 아니었다**: §8의 8종 중 발행되는 것은 4종이었고 `ProjectCompleted`·`ProjectReopened`는 **발행 지점 0곳**, `withdrawUnread`(F3-3 회수)는 **실사용 호출자 0건**이라 **재개해도 완료 지연 알림이 남아 있었다**(메서드는 구현·단위테스트가 다 있었다). `ProjectLifecycleChanged`로 둘을 채우고 재개 → 회수를 통합 테스트로 잠갔다 — **PRD-pms §12 등재 항목을 같은 날 해소**. ④**100% 도달 시각은 컬럼**(Flyway **V15** `projects.hundred_reached_at`): AC F3-1이 "단순·표준 구현"을 허용했고, 감사 역산은 `AuditQueryService`가 판정 없는 순수 조회라(2026-08-22 결정 — 두 뷰가 다르게 판정하므로 audit은 판정을 가질 수 없다) 그 용도로 넓힐 수 없다. `null`이 곧 "지금 100%가 아니다"라 별도 플래그가 없고, 진척률이 내려가면 비워지므로 **F3-2의 "재개 후 새 사이클"이 저절로 성립**한다. **시드 적재는 시각을 남기지 않는다** — 적재 시점은 도달 시점이 아니고, 찍으면 7일 뒤 시드 전체가 알림을 쏜다. ⑤**멱등 키가 두 종류로 갈린다**(F2-2·F3-2): 마감은 **점검이 돈 날**이라 매일 다시 알리고 지연은 **도달일**이라 한 사이클 한 번이다 — 마감은 날마다 임박해지는 사건이고 지연은 같은 사건이 안 풀린 것이라 매일 보내면 후자는 소음이다. ⑥**실측 결함**: 공용 테스트 헬퍼 `advance`가 프로젝트 이름을 하드코딩해 **호출한 쪽 프로젝트를 조용히 개명**시키고 있었다(두 테스트가 409로 충돌하며 드러났다) — "상태만 올린다"고 말하면서 실제로는 전체 교체(`edit`)를 하고 있었다 | AC F2-1·F2-2·F3-1·F3-2·F3-3 · PRD-pms §8·§10·§12 · Flyway V15 |
| 2026-08-25 | **EPIC D 완결 — D1 이관의 모듈 방향을 역포트로 (사용자 결정 2건 · PMS 담당)** ①**모듈 방향 = 역포트 `project.HandoverPort`**(project 정의, maintenance 구현 → 간선 **maintenance → project**). **강제된 답이 아니었다**: `AccountPort`·`AssignmentCountPort`는 실재하는 순환을 깨려고 생겼지만 실측상 project와 maintenance는 **서로를 전혀 모르는 형제 모듈**(둘 다 person·audit·common만)이라 어느 방향으로든 그을 수 있었다. 그래서 **잠재 의존이 이미 가리키는 쪽**을 골랐다 — maintenance가 `sourceProjectId`를 저장하고 D4-2가 원 프로젝트 링크를 요구하므로, project → maintenance로 그었다면 maintenance가 project를 필요로 하는 날 순환이 된다. ②**D1-2 문면 정정: `409 NOT_COMPLETED` → `409 INVALID_TRANSITION`**(사용자 결정) — 같은 상태 기계의 다른 칸(A7 완료·재개)이 이미 그 코드를 쓰고, `ErrorCode`를 늘리면 `mcp/ToolError`의 `switch`(의도적으로 `default` 없음)가 컴파일로 깨져 **MCP 담당 소유 파일을 건드려야** 한다. AC를 코드에 맞췄다. ③**이관 알림 유형은 `PROJECT_COMPLETED` 재사용**(사용자 결정) — 그 열거의 javadoc이 이미 "완료·이관 안내"이고 유형을 늘리면 H1-4 설정 화면의 칸이 함께 는다. ④**관문 재사용이 오류인 자리 2번째**: 어댑터가 `ContractCommandService`를 부르면 `ContractWriteGuard` 때문에 "계약 관리" 플래그 없는 PM이 자기 프로젝트를 이관할 수 없다(D3의 "이슈 쓰기에 관문을 걸면 팀원이 자기 이슈를 못 만든다"와 같은 모양 — 판정 축이 다르면 계약이 갈린다는 규칙이 실제 사고 2건으로 뒷받침된다). ⑤**포트가 화자를 받는다**: 감사 행위자를 사이트 담당자로 추측하던 초안을 고쳤다 — append-only 로그에 하지 않은 사람이 남을 자리였다. "권한은 포트를 넘지 않지만 기록은 넘는다"를 javadoc에 명시. ⑥원자성(D1-2·D1-3)은 **통합 테스트가 실물 행 수로 실증**한다 — 목은 "저장을 안 불렀다"까지만 말한다. ⑦**부수 발견 등재**: §8의 8종 중 **발행되는 것은 4종**이고 `ProjectCompleted`·`ProjectReopened`는 발행 지점 0곳, **F3-3 회수는 실사용 호출자 0건**이다(재개해도 완료 지연 알림이 회수되지 않는다) — 2026-08-24 기록의 "F3-3 회수 완료"는 능력이고 배선이 아니다. ⑧이관 알림 **수신자를 구현이 정했다**(§8 미명세 — 사이트 담당 엔지니어, 실행한 PM 제외) — 등재 | AC D1-1·D1-2·D1-3 · PRD-pms §3·§5·§8·§12 · git-workflow §3 |
| 2026-08-24 | **EPIC D 쓰기 D3 실구현 — 알림 유형 1종 추가 + notification 루트 판정 종결 (사용자 결정 1건 · PMS 담당)** ①**`NotificationType`에 `ISSUE_ASSIGNED("이슈 담당 지정")` 추가**(사용자 결정): §8이 `MaintenanceIssueRegistered`의 효과를 "담당자에게 알림"으로 적었으므로 알림의 의미는 *등록*이 아니라 **담당 지정**이다 — 등록됐다는 사실은 등록자의 관심사이고 행동할 사람은 맡게 된 사람이다. 유형이 5 → **6종**이 되고 H1-4 설정 화면은 유형 전체를 서버 응답에서 그리므로 칸이 자동으로 늘었다. ②**§12 "notification 루트 계약 5종의 자리" 판정 종결 — `service/`로 내린다**: ROADMAP이 D-b를 "정말 아무도 안 부르는가"의 **마지막 반증 기회**로 지정해 뒀고, D3-1이 그것을 반증하지 않았다 — maintenance가 이벤트를 발행하고 notification이 구독하는 형태라 새 구독자가 붙었는데도 외부 import는 **실측 0건**이다(유일한 grep 히트가 javadoc 문장이었다). 2026-08-22 리뷰가 이 넷을 루트로 올린 근거는 §8 이벤트 방향 확정으로 이미 뒤집혔고 이번에 새 구독자에서도 성립함이 확인됐다. **이동 실행은 별 단위로 남긴다** — 5파일 + import를 옮기는 순수 리팩터를 기능 변경과 같은 diff에 두면 리뷰가 둘 다 못 본다. ③**관문 없는 쓰기 계약이 처음 생겼다**(구조 관찰): `IssueCommandService`는 화자를 받지만 권한 판정이 없다 — US-D3이 `[로그인 사용자 전체]`이고 화자는 **기록**(감사 행위자·코멘트 작성자)의 몫이다. `ContractWriteGuard` 재사용은 중복이 아니라 **오류**다(팀원이 자기가 처리할 이슈를 등록할 수 없게 된다). ④**두 번째 이벤트 발행자**로 §8 배치가 일반화됐고, **담당자가 없어도 발행한다**(알릴 사람이 없다는 판단은 구독자 몫 — 발행자가 그것을 알면 "발행자가 구독자를 모른다"가 깨진다). ⑤파생 공백 등재: **재배정은 알리지 않는다**(§8에 그 이벤트가 없다). 부수 실측 2건 — 감사 스냅샷은 **JSON 왕복으로 숫자가 `Long`이 아니다**(테스트가 잡았다) · **코멘트는 감사 행을 남기지 않는다**(자기가 이미 불변 기록이다). MCP 통지: `list_maintenance_logs`의 `comments`에 이제 실제 값이 실린다(DTO 무변경) | AC D3-1·D3-2·D3-3 · PRD-pms §8·§12 · 구조 원칙 3 |
| 2026-08-24 | **가동률 2종 어댑터 배선 — 도구 8종 전량 실연결(503 자리 0건)** ①**범위 판정을 어댑터가 갖지 않는다**: `scope` 낱말을 `WorkforceProfile`의 조직 id로 바꿔 도메인에 넘기는 우회가 가능했고 지난 세션 기록도 그렇게 적혀 있었지만, 그러면 "MY_TEAM이 누구인가"의 답이 어댑터에 남아 **웹(`?orgUnitId=`)과 챗이 갈릴 수 있다**. `UtilizationScope`가 resource에 있는 이유가 그것이라 어댑터는 문자열→낱말 변환만 한다 — 모르는 낱말의 거절(422)도 도메인 몫이다. 어댑터가 새로 든 판단은 **`"yyyy-MM"` ↔ `YearMonth`** 하나뿐이고(계약이 `YearMonth`를 주고받는다) 도메인 예외가 아니라 파라미터 파싱이라 `ToolCalls`를 지나지 않지만 문구는 `ToolError`에서 나온다 ②**eval F-02 기대값 정정(v1.8)**: `get_utilization`의 배정 공백 월은 `[]`가 아니라 **인원 0% 행**이다(2015-01·MY_TEAM → 팀 5명 전원 0%). 모집단은 **현재 소속**으로 잡히고 배정만 그 달 것을 세기 때문이며, A-01이 무배정 부문을 0.0% 행으로 기대하는 것과 같은 규칙이다 — 두 케이스가 서로 모순이었다. `list_overbooked`는 과부하 판정이 걸러 `[]`가 맞다(F-01): **두 도구의 빈 달 응답이 다르다** ③**사용자 결정 — `scope=COMPANY`의 축소는 도메인 현행대로 둔다**: PRD-host FR-AI-11(2026-08-11 결정 ③)은 "전사 가시성 없는 화자의 COMPANY = 404 은닉"인데 구현(`UtilizationPopulation`)은 **조용히 가시 범위로 좁힌다**. G1 36케이스가 아니라 확장 후보이고 판정을 바꾸는 코드는 PMS 담당 소유라 이번 배선에서 뒤집지 않고 **미해결로 등재**한다 ④부수 — ~~`assignedMm` 부동소수점 잡음~~ **해소 확인**(`resource` 승격이 산식을 `UtilizationCalculator`로 뽑으며 6자리 반올림을 받았다. 어댑터 출력에서 `0.63` 확인 — 어댑터가 막는 안을 쓰지 않아도 됐다) | ROADMAP host 절 · FR-AI-11·FR-AI-12 · `UtilizationScope` javadoc(2026-08-24 결정 ②) · 앵커 정본 §4·§7-1 · 실측(도구 관통 13건 — 앵커 9종 + 빈 달 2종 + 422 2종) |
| 2026-08-24 | **ROADMAP M2·M3는 지금 확장하지 않는다 + M1~M3 라벨 각주 신설 (사용자 결정 · 코드 변경 없음)** 사용자 질의 "M2·M3가 로드맵에 없는데 구조 변경 때 수정이 누락된 것 아니냐"를 git 이력으로 실측했다. ①**누락이 아니다**: `## M1 — 조회 전용 → M2 — 안전한 쓰기 → M3 — 프롬프트·외부` 합본 헤딩은 **최초 커밋 `d4088da`(하네스 초기 구성)부터 한 글자도 바뀌지 않았고**, M2·M3가 독립 절이었던 리비전은 전 이력에 없다. 최초 본문이 "세부 체크리스트는 P단계 문서 확정 후 이 자리에서 확장"이었으므로 **의도된 예약**이고, 2026-08-23 정합 작업(`fd6d5fa`)은 그 예약을 **M1만** 푼 것이다(커밋 메시지에 M1 한정 명시) ②**M2·M3는 실재하는 단계다** — PRD-host §10이 5단계(M-1→M0→M1→M2→M3)와 체크리스트 소유권을 ROADMAP에 넘기고, §6-4가 G2(=G1+읽기 운영 2주+쓰기 Eval F3 0건 → `update_progress` 사내 공개)·G3(=G2+도구별 개별 Eval+모델·단가·학습 조항 최종 확인) 조건을 이미 정의한다. PRD-pms §0의 `PMS-M` 개명 근거도 "루트 M-1~M3와 충돌 방지"다 ③**사용자 결정 — 지금은 그대로 둔다**: M2는 항목 3개(운영 2주 관측·쓰기 Eval·G2 승인)로 이미 쓸 수 있으나 M1이 끝나지 않았고, **M3는 PRD-host §11이 "M3 착수 시점에 운영 데이터(질의 분포)를 보고 결정, P단계에서 미리 정하지 않는다"고 명시적으로 이월한 것**이라 지금 채우면 근거 없는 추측이 PRD와 어긋난다(원칙 5 "고통이 생기면 그때") ④**각주 한 줄만 추가**(사용자 선택): M1 라벨이 "조회 전용"인데 pms 절에 EPIC D·E·F 쓰기가 들어와 있어 다음 세션이 같은 질문을 반복할 자리였다 — "조회/쓰기 구분은 **챗(MCP 도구) 노출 기준**이고, M2가 여는 것은 `update_progress`의 **사내 공개**이지 구현이 아니다(도구는 2026-08-23 실연결 완료 — G2 대기)"를 ROADMAP M1 절에 명시 | git 이력 실측(ROADMAP.md 전 리비전 헤딩 대조 — `d4088da`~`fd62233`) · PRD-host §6-4 게이트 표·§10 출시 계획(단계·체크리스트 소유권)·§11 열린 질문 #3 이월·§1 비범위(외부 도구 M3 검토) · PRD-pms §0 라벨 충돌 근거 · 구현_노트 부록 A(ProLLMOps = M3 하이브리드) · 원칙 5 |
| 2026-08-24 | **화면 없는 백엔드 9지점 연결 — 읽기 계약 확장 + 낙관적 락 결함 3건 (PMS 담당)** 원장의 "화면이 비어 있는 것은 알림 벨 하나뿐"이 틀렸다는 것을 라우트 전량 대조가 잡았다(같은 날 들어온 EPIC E·F를 현재 상태가 따라가지 못했다 — 실제로는 9지점). **①읽기가 쓰기를 먹이지 못하고 있었다**: `PUT /api/people/{id}`는 `orgUnitId`·`gradeId`·`groupId`·`version`을 요구하는데 `GET /api/people`는 이름만 줬고, 직급·권한 그룹 목록은 `{id,name}`뿐이었다. §7에 상세 라우트가 없으므로 **목록이 곧 관리 화면의 원천**이라는 뜻이라 웹 읽기를 넓혔다 — `PersonSummary` 신설(**모듈 루트 `PersonRef`는 건드리지 않는다**: `/mcp` 도구가 함께 쓰는 계약에 편집용 id·version이 실리면 도구 응답에도 나간다 — 구현_노트 §5) · `GradeDetail`·`PermissionGroupDetail`에 `memberCount` 추가 후 목록 반환형으로 승격 · 구 `ReferenceItem` 폐기. **②낙관적 락 결함 3건**: 인력·직급·권한 그룹 수정이 요청의 `version`을 **받아만 두고 검사하지 않았다**(DTO javadoc은 처음부터 409를 적고 있었으므로 구현 누락이다 — 실서버에서 옛 version 통과를 확인하고 `requireVersion` 신설) · 쓰기 응답의 `version`이 flush 전 값이라 그 값으로 재저장하면 409였다(project 선례대로 `saveAndFlush` — D2 관통 테스트가 먼저 잡고 같은 계열이 EPIC E에도 있었다) · 소속 이동이 배정 건수를 엔티티 변경 뒤에 물었다(conventions §4). **③열거 라벨 비대칭이 수정 폼에서 처음 아팠다**: 유지보수 응답이 한국어 라벨이라 select를 되채울 수 없는데 라벨 표를 클라이언트에 복제하는 것은 정본을 둘로 만든다 → `ContractDetail.statusCode`·`ContactView.partyCode`를 서버가 함께 준다. **④SSE 없이 알림 벨을 열었다**: F1-4가 없어 즉시 푸시가 불가능한데 폴링은 44명 규모에 끊임없는 요청이라, **부팅 + 벨 열기** 두 시점 재조회로 두고 스트림이 열리면 구독으로 바뀐다고 코드에 적었다 | §7 라우트 표(인원 상세 라우트 부재) · §7 "단건 응답은 version을 포함한다" · `UpdatePersonCommand`·`GradeCommand`·`PermissionGroupCommand`의 version javadoc · 부록 A(설정 3탭·헤더 알림 뱃지·권한 그룹 행 구성) · PRD-pms §12(감사 화면 배치 결정 — 재배치하지 않는다) · 구현_노트 §5 "도구 응답은 화면 응답이 아니다" |
| 2026-08-24 | **EPIC D 쓰기 D2 실구현 — 범위 분할 + 연락처 원문 규칙 (사용자 결정 2건)** ①**D-b를 D2/D3으로 쪼갠다**: D3-1 담당자 알림이 결정 둘을 함께 끌고 온다(`NotificationType`에 이슈 유형 추가 — H1-4 설정 화면에 노출되는 값 · §12 등재 "notification 루트 계약 4종을 `service/`로 내릴지"). 쓰기 구현과 그 리팩터링이 한 diff에 섞이면 리뷰가 어려워진다 ②**사용자 결정 — 수동 입력 연락처의 `raw`는 조각을 조립해 채운다**(스키마 완화 아님): `maintenance_contacts.raw`가 not null인데 화면 입력에는 시트 원문이 없다. nullable로 풀면 조회 화면이 조각의 빈자리를 `raw`로 메우게 돼 있어(2026-08-23 결정) 그 자리가 빈다. 조립 순서를 시트와 같게 두어("이름 직급 전화 (이메일)") **조립본을 `ContactParser`에 되넣으면 같은 조각이 나오고**, 그 왕복을 테스트가 잠근다 ③**일반 규칙 하나가 실측으로 확인됐다 — 쓰기 응답의 `version`은 flush 뒤 값이어야 한다**: 같은 트랜잭션에서 조회 계약을 부르면 영속성 컨텍스트의 미증가 값을 읽어 `version=0`을 돌려주고, 그 값으로 재수정하면 409다(락이 스스로를 막는다). project가 이미 같은 이유로 `saveAndFlush`를 쓰고 있었다 — 새 쓰기 경로는 이 선례를 따른다. 단위 테스트로는 잡히지 않는 종류다(목이 version을 올려 주지 않는다) ④**지어내지 않은 것 — 계약 기간 규칙**: "종료일 > 시작일"을 걸려다 시드에 **역순 계약이 1건 실재**함을 확인하고 걸지 않았다. AC에 없는 규칙은 실 데이터의 모양을 거부한다 ⑤**정리 대상 등재 — 감사 diff가 세 모듈에 같은 모양으로 있다**(project·person·maintenance). 도메인을 모르는 Map 연산이라 audit 루트로 올릴 수 있으나 모듈 루트 확장은 소유자 합의 선행(git-workflow §3)이라 범위 밖으로 뒀다 | AC D2-1~D2-4 · PRD-pms §4 참조 데이터 id 표(유지보수 계약 = 하드 삭제 없음 → `max(id)+1`) · §7 "단건 응답은 version을 포함한다"(→ `SiteView.version` 신설) · 상위 PRD §4-3 "계약 관리" 플래그 · conventions §4 "더티 세션에 질의 금지" · 시드 실측(계약 105건 중 기간 역순 1건) |
| 2026-08-24 | **EPIC F 1차 — 도메인 이벤트 도입 + 방향 확정 (사용자 결정 2건)** ①**이벤트는 한 방향, 구독자 → 발행자**: §3 표(소비자 project·resource)와 §8(구독자 notification)이 합쳐지면 순환이었다. 반대안(발행자가 `notify` 호출)은 성립조차 하지 않는다 — B1-3이 project → resource 통보를 요구하는데 `resource → project`가 이미 있어 직접 호출이 그 자체로 순환이다. notification은 아무것도 의존하지 않는 잎이라 어느 발행자에 붙어도 안전하다(실측). §3 표·계약 javadoc 정정 ②**사용자 결정 — 발행 레지스트리는 나중에**: `@ApplicationModuleListener`는 레지스트리 없이 돌고, 켜면 라이브러리 내부 테이블(`event_publication`)이 우리 Flyway에 들어와 버전을 따라가야 한다. 대가는 "커밋 직후 앱이 죽으면 그 알림은 사라진다"이고, 과부하는 가동률 화면에도 보여 알림이 유일 경로가 아니라 감수한다. 트리거를 `build.gradle`에 명시 ③**사용자 결정 — "팀장"은 기능 플래그로 판정**: 권한 그룹은 2026-08-09 일반화 이후 사용자가 개명·삭제하는 데이터라 이름으로 찾으면 E5 개명 시 알림이 조용히 멈추고, 가시성 scope도 팀원이 2026-08-22에 TEAM이 되며 구분이 사라졌다. 남는 안정된 표식이 **"프로젝트 생성" 플래그**이고 상위 PRD §4-3이 이미 관리자·부문장·팀장에게만 준다 → `OrgPermissionService.findColleaguesWith` 신설(모듈 루트 계약 확장) | §8 이벤트 명세 · AC F1-1·F1-2·F1-3 · ROADMAP B1-3 · 상위 PRD §4-3 · 실측(모듈 간선 · 이벤트 인프라 0건 · notification 루트 타입 외부 사용 0건) |
| 2026-08-24 | **EPIC E 쓰기 5종 실구현 — 모듈 경계 1건 확장 + 참조 데이터 id 규칙 승격 (사용자 결정 2건)** ①**`person.AssignmentCountPort` 신설(모듈 루트)**: E1-2가 "진행 중 배정이 있어도 허용하되 경고"인데 그 건수를 person이 알 길이 없었다. 의존은 이미 `project → person` 한 방향이라 person이 project를 부르면 순환이고(`ModularityTest`), `AccountPort`(person 정의 · auth 구현) 선례대로 **필요한 쪽이 계약을 정의하고 가진 쪽이 구현**한다. 면은 **건수 하나**로 좁혔다 — 배정 목록을 받으면 person이 배정의 모양에까지 의존한다 ②**참조 데이터 id 발급 규칙을 §4로 승격**: 판단 기준이 부록 B(유지보수 계약 id 결정)에 지나가듯 한 줄로 있었다 — "하드 삭제가 없으므로 `max(id)+1`". 뒤집으면 **있으면 시퀀스**이고 직급(E4-3)·권한 그룹(E5-4)이 거기 해당한다(Flyway V11). 조직 노드가 같은 조건에서 `max(id)+1`로 데이터를 오염시킨 실측 사고가 근거다 ③**사용자 결정 — E3-2 조직 이름 중복은 막지 않는다**: AC에 문구가 없고 스키마에도 유니크 제약이 없다. 필요해지면 AC를 먼저 고치고 생성(E3-1)까지 같은 규칙으로 연다 — 구현이 AC에 없는 규칙을 지어내지 않는다 ④**사용자 결정 — E1-2 경고는 응답 본문에**: 이동을 막지 않으므로 오류로 낼 수 없고, 알림(EPIC F)으로 보내면 지금 화면에서 개편하는 사람이 보지 못한다. `OrgUnitMoveResult`가 `PersonRef`를 감싼다 — `PersonRef`는 project·maintenance가 함께 쓰는 루트 계약이라 건드리지 않는다 | AC E1-1·E1-2·E2-2·E2-5·E3-2·E4-1~3·E5-1~4 · PRD-pms 부록 B id 규칙 · 2026-08-22 조직 노드 id 재사용 사고(V5·V6) · `AccountPort` 순환 회피 선례 · conventions §5 ISP |
| 2026-08-24 | **`resource` 루트 승격 — 소유자 결정 선행 + 계약 모양 2건 (사용자 결정)** git-workflow §3("One promotion, one owner")를 처음으로 실제로 밟는다 — **파일을 만들기 전에 이 행을 먼저 쓴다**. **①소유자: `resource/` 루트 파일은 PMS 담당이 만들고 MCP 담당은 `UtilizationTools` 배선만 한다.** 선례 3종(`PersonLookupService`·`ProjectLookupService`·`MaintenanceLookupService`)이 전부 PMS 담당 승격인데 `pms/CLAUDE.md` 소유권 절만 "MCP dev … **plus the read contract each domain publishes for it**"이라 반대로 읽혔다 — 그 모호함이 2026-08-23 중복 승격의 원인이므로 문장도 함께 정정한다 **②scope 해석은 resource가 든다**: 루트 계약이 `UtilizationScope`(ME·MY_TEAM·DIVISION·COMPANY·PERSON)를 그대로 받고 화자로부터 팀·부문을 유도한다. 어댑터가 조직 id로 바꿔 넘기는 안은 "MY_TEAM이 누구인가"라는 판정을 어댑터에 남겨, **챗에서 보이는 것 = 화면에서 보이는 것**의 보장을 약하게 한다 **③causes는 계산기 추출로 낸다**: `list_overbooked`의 `Cause(projectName, mm)`를 얻으려고 루트 impl이 배정을 다시 조회하면 "진행중 배정만"이라는 모집단 규칙이 **두 벌**이 된다 — 지난 세션이 시드 테스트에서 걷어낸 것과 같은 형태다. 산식·모집단·`기본 > 100` 판정을 `UtilizationCalculator` 하나에 두고, 웹은 causes를 버리고 MCP는 싣는다 **④합의 성립 — 양측이 독립적으로 같은 결론에 도달했다**(같은 날 MCP 담당 제안 ⑤도 "PMS 담당이 만든다"였고 근거도 같은 선례 3건이다. 소유 공백이 원인이던 2026-08-23 중복 승격이 이번엔 재발하지 않았다) **⑤다만 설계 하나가 상대 예상과 다르다 — MCP 담당 확인 필요**: 제안은 `scope=MY_TEAM|DIVISION`을 **어댑터가** `WorkforceProfile`의 조직 id로 유도해 `orgUnitId`로 넘기는 그림이었는데, 실제 계약은 `UtilizationScope`를 그대로 받아 **도메인이 푼다**(위 ②). 어댑터가 할 일은 줄지만(문자열 → `UtilizationScope.from()` 한 줄) 바인딩 대상이 달라지므로 배선 전에 확인해야 한다 | git-workflow §3 · 구조 원칙 3(가시성·판정은 애플리케이션 계층 — 웹·MCP 호출자에 함께 걸린다) · `ProjectLookupService.search()`가 도구 파라미터인 한국어 상태 라벨을 그대로 받은 선례(해석 규칙까지 도메인이 가졌다) · PRD-host `get_utilization`·`list_overbooked` 계약 · AC C1-1·C1-3·C1-5·C1-6 |
| 2026-08-24 | **EPIC C 확인 완료 + 앵커 정본을 실 구현으로 재확인(전량 일치) + `resource` 승격 소유자 제안(PMS 담당 합의 대기)** ①**PR #29 확인 요청 2건에 답한다**: `WorkforceDirectoryService.findAllAggregatablePersonIds()` 추가에 **이견 없다** — "소비자는 `resource` 하나이고 `mcp`는 쓰지 않는다"를 grep으로 재확인했고(`mcp` 모듈에 이 계약 참조 0건), 전사 scope의 `visiblePersonIds`가 빈 집합인 것은 `OrgVisibility.unrestricted`의 의미대로라 person 밖으로 전원을 내주는 창구가 따로 필요한 것이 맞다. 집계 모집단을 **재직자만**으로 통일한 것도 동의한다 — eval 36케이스는 현재 월만 묻고(기준일 2026-08 고정), 지난달 퇴사자 누락은 그 케이스들이 닿지 않는다. ②**앵커 정본이 실 구현과 전량 일치했다** — 재앵커 당시에는 EPIC C가 없어 가시성 규칙·가동률 산식을 **SQL로 옮겨** 수치를 뽑았고(`docs/evals/seed-anchor.sql`), 문서에 "EPIC C가 서면 `get_utilization` 응답과 일치해야 한다"를 약속으로 남겼다. 구현이 서자마자 7개 질의로 대조했고 **기본·보정 양쪽이 전부 일치**했다(전사 과부하 2명 · billable 33명 · 부문 평균 4개 · 부문 14명 2개월 · 개인 지정 · 팀 5명 · **가시성 밖 개인 지정 404**). 세 사람(host SQL · PMS 부록 B 재앵커 · EPIC C 구현)이 독립적으로 같은 수치에 도달했다. ③**대조에서 정정 1건**: MS사업부 부문 평균은 11.3이 아니라 90÷8 = **11.25**다. 부문 평균은 서버가 아니라 **모델이 인원별 행에서 내는 값**이라 반올림이 갈리므로, eval A-01에 "마지막 자리는 채점하지 않는다"를 명기했다(나머지 세 부문은 나누어떨어진다). ④**관찰 1건 — `assignedMm`이 결정 ③의 절단을 받지 않았다**(PMS 담당 인지용, 수정 아님): 응답이 `1.9100000000000001`로 나온다. `basic`·`adjusted`는 깨끗한데 이 필드만 원값이다. **모델이 읽는 값**이라 어댑터가 그대로 실어 보내면 답변에 부동소수점 노이즈가 나올 수 있다 — 어댑터에서 자르면 웹과 챗의 값이 갈리므로 도메인에서 같이 처리하는 쪽이 맞다고 본다. ⑤**`resource` 루트 승격 소유자 — MCP 담당 제안: PMS 담당이 만든다**. 근거는 선례 3건이 이미 한 방향이라는 것이다 — `person.PersonLookupService`·`maintenance.MaintenanceLookupService`·`project.ProjectLookupService`·`ProgressCommandService` 모두 **도메인 담당이 자기 모듈 루트에 계약을 올리고 MCP 담당이 어댑터를 배선**했다(maintenance는 그 순서를 어겨 한 벌을 버렸다 — 2026-08-23 중복 작업 기록). 도메인이 자기 계약의 모집단·산식·예외를 쥐고 있어야 판정이 두 모듈로 갈리지 않는다는 것이 `MonthlyAssignment` javadoc이 이미 세운 원칙이고, 같은 이유로 `UtilizationQueryService`를 루트에 올리는 판단도 그 모듈 소유자의 몫이다. 필요한 면은 좁다 — `mcp`가 쓰는 것은 **(월, 화자, 대상 scope) → 인원별 행**과 **과부하 목록** 둘뿐이고, `scope=MY_TEAM|DIVISION`은 `WorkforceProfile.teamOrgUnitId`·`divisionOrgUnitId`(#19 후속으로 도착)로 화자에서 유도해 `orgUnitId`로 넘기면 된다. `UtilizationView` 7필드는 `UtilizationEntry`를 채우기에 충분함을 실측으로 확인했다. **합의되면 그 PR에 어댑터를 붙인다** — 파일을 먼저 만들지 않는다(git-workflow §3) | 실측 — `/api/utilization` 7개 질의 × 앵커 정본 대조 · `mcp` 모듈의 `WorkforceDirectoryService` 참조 0건 grep · `resource` 모듈 루트 공개 파일 0건 확인 · `WorkforceProfile`에 조직 id 2종 도착 확인(미해결 이슈 해소) |
| 2026-08-23 | **eval 코퍼스 시드 재앵커 완료 — 화자·기대값이 실 서버를 가리킨다(G1 host 선행 종료) + 앵커 정본 신설** ①**착수 전 전제가 실측에서 깨졌다**: 트랙 파일은 "이름은 id 기준 기계적 치환, 보정 가동률·billable만 재계산"이라 적었으나 **id를 보존해도 수치가 살아남지 않는다** — `billable`·직급계수·조직은 명부에서 오고 배정 M/M만 `projects.json`의 id에서 온다. 반례: 구 과부하 3인 중 id 7(182%)이 실 명부에서는 `billable=false` 지원조직이라 **집계 모집단에서 빠진다**. 그래서 수치는 어느 캐스팅이든 전량 재산출이고, **캐스팅은 실측 뒤에야 정할 수 있다**(순서를 실측→캐스팅→반영으로 뒤집었다). ②**캐스팅 = id 보존 + 역할 라벨 정정 + 화자 교체 4건**: id를 보존하면 프로젝트명·배정·M/M·유지보수 앵커가 **그대로 산다**(실측 대조 7종 전부 동일 — SK온 347 전 필드·김경민 8월 3건 133%·D-04 6건 목록·B-01 가시 6건·E-01 340·C류 이슈 7건과 **접수일 7개**·F류 공백 월). 깨지는 축은 **권한 그룹·직급계수·billable** 셋뿐이고, 그중 화자로 쓰이는 자리만 교체했다 — 부문장 13→**16 김문수**(같은 AX솔루션사업부라 무대 보존) · 팀장 16→**17 이현창**(같은 팀·팀 5명 유지) · 쓰기거부 팀장 21→**22 김은채**(팀 진행중 3건 보이지만 역할 0건, 대상의 단독 담당자가 같은 팀이라는 구조까지 동일) · 참여-유일-가시성 4→**29 이은지**(팀원 scope SELF→TEAM 변경으로 id 4는 팀 동료(PM)를 통해서도 보인다 — 이 성질이 성립하는 유일한 인물). ③**시드 구조가 케이스 하나의 판별력을 없앴다(사용자 승인으로 손실 수용)**: 2025-01~2027-09 **전 구간**에서 과부하 인원이 2개 이상 부문에 걸치는 달이 **0개**다(전부 AX솔루션사업부) → 부문장 답 = 전사 답이라 A-02의 F2(부문 밖 누출) 검증이 불가능하다. 화자를 0명 부문으로 옮기면 판별력은 최대가 되지만 양성 신호와 구 30케이스 구조를 잃으므로, **A-02는 유지하고 손실을 케이스에 명기 + 경계 검증을 A-07이 담보 + 확장 후보 등재**로 갔다. ④**부수로 미해결 2건이 닫혔다**: 팀원 scope 변경의 파급은 화자 1건뿐이었고(위 ④번 교체), A2-9(진행중 제한)는 D류 대상 8건이 전부 진행중이라 기대값 불변 — 거절 경로만 SC-23 파생으로 신설했다. ⑤**정본을 만들었다**: `docs/evals/seed-anchor-map.md`(캐스팅표 19명 + 가동률·프로젝트·유지보수 정본 수치 + 시드 정합 이슈 2건) + `docs/evals/seed-anchor.sql`(뷰 3종·질의 10종 — 재실행하면 표가 재출력된다). 시드가 바뀌면 **SQL → 정본 → 두 문서** 순으로 고친다. 산출물: eval-cases **v1.7**·유저_시나리오 **v1.5**·이름 176곳 치환·조사 8곳 교정. 검증: 구 익명 이름 잔존 0 · 문서 인물 31명 전원 실 명부 존재 · **가동률 수치 45쌍을 DB와 기계 대조(불일치 0)** · verify.sh pms PASS | 실측(빈 DB → 시드 3종 적재 → SQL 집계). **PMS 담당 기록과 독립 일치**: PRD-pms 부록 B §558이 같은 날 같은 결론(191/182/133 · overbooked 2명 · 윤종헌 모집단 밖)을 `ProjectSeedLoadIntegrationTest`로 고정해 두었다 — 양쪽이 서로 모르고 같은 수치에 도달한 것이 이 재앵커의 가장 강한 근거다 |
| 2026-08-23 | **EPIC C 가동률 실구현 + person 계약 1건 추가 (사용자 결정 1건 · MCP 담당 확인 필요)** ROADMAP pms 절의 마지막 G1 선행 항목을 닫았다. **①공개 API 추가 — `WorkforceDirectoryService.findAllAggregatablePersonIds()`**: 실측한 공백이다. 전사 scope 화자의 가시성은 `unrestricted`이고 그때 `visiblePersonIds`가 **빈 집합**인데("제약이 없다"는 뜻이지 "아무도 없다"가 아니다) person 밖으로 전원을 내주는 창구가 없어, 집계 호출자가 명단을 얻을 경로가 아예 없었다. 모듈 루트 파일 변경이라 이 기록을 거친다 — 소비자는 `resource` 하나이고 `mcp`는 이 계약을 쓰지 않는다(실측) **②사용자 결정 — 집계 모집단은 재직자만**: person이 이미 서로 다른 두 규칙을 갖고 있어 임의로 못 정했다(`findPersonIdsInSubtree`는 재직자만, `findProfiles`는 "지난달 가동률은 그때 재직 중이던 사람으로" 비활성 포함). `findPersonIdsInSubtree`와 같은 규칙으로 통일했고, **대가는 지난달을 오늘 조회하면 그 사이 퇴사자의 배정이 빠지는 것**이라 미해결로 등재했다(PRD-pms §12). eval 36케이스는 현재 월만 물으므로 G1을 막지 않는다 **③반올림 — 백분율을 6자리로 자른다**: 배정 0.5+0.7에 계수 1.5를 곱하면 `179.99999999999997`이 나온다(실측). 응답 값도 문제지만 결정적인 이유는 **과부하 판정이 `기본 > 100`이라 노이즈가 판정을 뒤집을 수 있다**는 것이다. M/M이 소수 2자리까지라 의미 있는 차이는 전부 남는다 — 표시 정밀도는 프론트가 정한다 **④착수 전 오산 정정 2건**: C1-4를 "이벤트 재계산"으로 봤으나 조회 시점 계산이라 구현할 것이 없고 증명만 남아 있었다(골격 javadoc이 이미 적어 둔 것) · 조회 승격이 "패키지 이동뿐"이라던 지난 오산과 같은 계열로, 이번에도 계약 공백이 하나 있었다 **⑤부수 — 정본 두 벌 제거**: `ProjectSeedLoadIntegrationTest`가 산식·모집단·>100 판정을 **테스트 안에 따로 구현**하고 있었다(그쪽은 `Capacity` 월 예외도 무시했다) → 실서비스 호출로 교체. **남은 것 1건 — 승격 소유자 결정**: `resource` 모듈 루트가 비어 있어 `get_utilization`·`list_overbooked`가 붙을 계약이 없다. 같은 날 등재한 git-workflow §3("One promotion, one owner")대로 **파일을 만들기 전에 소유자를 정한다** — 이번에는 그 규칙을 실제로 밟는다 | AC C1-1~C1-6 · 상위 PRD §3 산식(2026-08-10 ×coeff 재정의) · 2026-08-10 모집단 결정(진행중만 — 전 상태를 세면 정태휘 1171%). 실측: 전사 scope의 빈 인원 집합 · 부동소수점 179.99999999999997 · 시드 오버부킹 이현창 191·김경민 133(C1-5 적용). 검증 verify.sh pms PASS — 테스트 347 → **368**, 501 골격 14 → **13** |
| 2026-08-23 | **project 어댑터 배선 — 쓰기 도구가 처음 열렸다(도구 8종 중 6종 실연결) + 카탈로그 문구 3건 정정** ①**배선**: `ProjectLookupService`·`ProgressCommandService`(PR #25) 위에 `search_projects`·`update_progress`를 붙여 503을 걷었다. 2단계 확인·권한·낙관적 락·완료 규칙은 **내부 유스케이스가 그대로 갖고** 어댑터는 `confirmed` 왕복과 표현 변환만 한다 — 프로토콜을 두 곳에 두면 한쪽이 확인을 건너뛰는 길이 생긴다. 도메인에 없는 `summary` 한 줄만 어댑터가 만들고, 커밋 뒤에는 현재값=요청값이 되므로(`committedOf`) 두 단계의 문장이 갈린다. ②**404 은닉의 주인이 maintenance와 반대다**: 프로젝트는 도메인이 들고(가시성 밖이 부재와 같이 숨어야 한다 — A3-2) 유지보수는 어댑터가 든다(숨길 것이 없다 — D4-3). 같은 어댑터 안에서 갈리는 것이 옳다. ③**카탈로그 문구 3건 정정**(PRD-host v2.6): **절단 50건 명시**(계약은 `limit`을 요구하는데 문구는 절단을 약속하지 않았다 — 관리자 382건·팀원 52건 모두 절단에 걸리므로 모델이 잘린 개수를 전체로 답한다) · **keyword = 부분 일치**(문구는 목업의 토큰 AND였고 실 구현은 통짜 LIKE. eval B-01·B-05는 둘 다 통과한다 — "AI 검색"은 이름이 아니라 `solution` 값 자체로 17건 매칭 — 그래서 도메인 변경을 요청하지 않고 문구를 실제에 맞췄다) · **status 5종**(도메인은 `유지보수중`까지 받고 그 밖은 422인데 문구는 4종만 알려 줬다). ④부수로 **감사 `source=MCP` 실측**이 닫혔다 — 쓰기 도구 없이는 확인할 방법이 없던 항목이다. 테스트 13건 · verify.sh pms PASS(28건) | 문구는 모델이 읽는 문서다(구현_노트 §5) — 어긋난 채 두면 G1에서 오답으로 나온다. 절단 미명시가 그중 유일하게 **오답을 만드는** 건이었고, 나머지 둘은 문구와 서버 거동이 갈린 자리다 |
| 2026-08-23 | **maintenance 루트 계약이 양쪽에서 각각 만들어졌다 — 중복 작업 1건 기록 + 재발 규칙 (PMS 담당 기록)** 결과는 위 두 행에 있고(#25 계약 · #26 어댑터 배선), 여기에는 **그 사이에 일어난 낭비**를 남긴다. 같은 날 PMS 담당이 `MaintenanceLookupService`+`ContractBrief`·`ContractIssues`·`IssueBrief`·`CommentBrief`를 올리고, MCP 담당이 **같은 경로에** `MaintenanceLookupService`+`ContractRef`·`MaintenanceLogs`+Impl+단위 테스트 8건을 올렸다. 머지는 add/add 충돌이었고 어느 쪽을 골라도 한 벌을 버리는 것이었다 — MCP 담당이 #26에서 자기 판을 버리고 PMS 판에 어댑터를 다시 붙이는 쪽으로 정리했다. **원인은 설계 이견이 아니라 소유 공백이다**: ROADMAP이 이 한 가지 일을 pms 절("도메인 루트 계약 승격")과 host 절("maintenance 포트 실연결") 두 곳에 적고 **누가 계약 파일을 만드는지 정해 두지 않았다**. `git-workflow.md` §3에 규칙을 넣었다("One promotion, one owner" — 모듈 루트에 파일을 만들기 전에 소유자를 결정 기록으로 먼저 정한다. 두 트랙에 걸친 ROADMAP 항목은 어느 트랙이 파일을 갖는지 명시한다). **버려진 판에서 살아남은 것 2가지**는 짚어 둘 값이 있다: ①절단 50을 어댑터 상수(`TOOL_LIMIT`)로 두는 배치 — #26이 그렇게 갔고, 도메인이 도구 description의 약속을 알지 않아도 되므로 이쪽이 맞다 ②`contractId` nullable 정정 — 두 판이 독립적으로 같은 결론에 도달했다(미연결 이슈 7건이 `0`으로 나가면 모델이 그 값을 다른 도구에 넣는다) | 실측 — 두 계약 파일·Impl·어댑터 DTO 대조, `origin/main`과 리베이스 전 브랜치의 `MaintenanceTools` 상태 비교. 부수로 이 원장의 자기모순 2건도 함께 정정했다(코드 줄의 "7종 실연결"은 실측 4종 · "501 골격 6개"는 실측 미구현 유스케이스 14개) |
| 2026-08-23 | **PR #25 MCP 담당 확인 완료 + maintenance 어댑터 실연결 — 도구 8종 중 4종이 실 데이터로 답한다** #25가 물은 둘에 답한다. **①시그니처 대조 — 충족한다**: `ContractBrief` 7필드가 MCP `ContractSummary` 7필드를 그대로 채우고, `ContractIssues`·`IssueBrief`·`CommentBrief`도 `MaintenanceLogsResult`·`IssueView`·`CommentView`에 1:1로 들어간다(`IssueBrief.siteName`은 도구가 약속하지 않아 어댑터가 버린다). **②시드 원본 id 규칙 — 동의한다**: 앵커 계약 101이 identity 생성 순서의 우연에 기대던 것과 ISSUE 갈래가 죽어 있던 것이 함께 닫힌다. **어댑터에서 하나 정정했다** — `MaintenanceLogsResult.contractId`가 `int`라 계약 미연결 이슈(시드 7건)를 `contractId: 0`으로 내보내던 자리를 nullable로 바꿨다(없는 것과 0번은 다른 말이고, 0을 받은 모델은 그 값을 다른 도구에 넣는다). #25의 `ContractIssues.contractId`가 `Long`이므로 이 변경 없이는 어댑터가 그 계약을 표현할 수 없다. **절단 50건과 404 문구는 어댑터가 든다** — 둘 다 도구 description·`ToolError`의 소관이고 그 둘은 `mcp` 모듈이 소유한다. 부수 관찰 2건(수정 아님, 다음 손댈 때): `ContractIssues` javadoc이 `matched` 값을 "계약/이슈"로 적었는데 구현은 `CONTRACT`/`ISSUE`다(값이 아니라 서술의 부정확) · `CommentBrief` 변환이 `ZoneId.systemDefault()`를 인라인으로 써 `ClockConfig`(시각·존 단일 출처)를 우회한다(시드 코멘트 0건이라 지금 영향 없음) | 도구 관통 실측 — `search_maintenance`가 준 계약 id로 이슈 7건 도달(2026-08-11 결정 ④가 뚫으려던 id 확보 경로) · 그 이슈 id로 ISSUE 갈래 도달(**eval C-04가 이슈 id 직접 제공형이고 인젝션 치명 케이스라 이 갈래가 죽어 있으면 G1이 막힌다** — #25의 id 재부여가 그것을 열었다) · 사이트명으로만 45사이트 계약 도달 · 모르는 라벨 422 · 없는 id 404 은닉. 검증: verify.sh pms PASS |
| 2026-08-23 | **도메인 루트 계약 3종 승격 + 조직 id·시드 원본 id (안 ② 이행 · 사용자 결정 2건)** MCP 담당이 확정한 승격 방식(안 ② — 도메인 계약을 루트로, 도구는 `mcp`에)의 도메인 쪽 몫을 이행했다. 승격 전에는 `PersonTools`만 실연결이고 나머지 4도구가 `ToolError.unavailable`이었다. ①**`maintenance/MaintenanceLookupService`**(+`ContractBrief`·`ContractIssues`·`IssueBrief`·`CommentBrief`) — `search_maintenance`·`list_maintenance_logs`가 붙는 면. 내부 계약을 통째로 올리지 않고 좁은 면을 둔 이유는 `PersonLookupService` 선례와 같다: 내부는 웹의 page 봉투·필터를 갖는데 어댑터는 "최근 N건 절단"만 약속했다 ②**`project/ProjectLookupService`**(+`ProjectBrief`·`ProjectDetailBrief`)·**`ProgressCommandService`**(+`ProgressResult`). **범위 오산 정정**: 조회 승격을 "패키지 이동뿐"으로 봤으나 내부 계약에 status·keyword 필터가 아예 없어(`listVisible(caller, Pageable)`뿐) 저장소 질의를 신설했다 ③**`WorkforceProfile`에 `teamOrgUnitId`·`divisionOrgUnitId`** — MCP 담당 요청 건 해소(챗의 `scope=MY_TEAM\|DIVISION`. 웹은 `?orgUnitId=`를 받지만 챗은 화자로부터 유도해야 하고 `org_units.name`에 유니크 제약이 없다) ④**`PersonRef`에 `division`** — 프로젝트 응답의 팀·부문 재료 ⑤**사용자 결정 — 프로젝트의 team·division은 PM 소속 파생**: 실측이 결정했다. 시드 `projects.json`의 프로젝트 team·division은 **382/382가 구 익명 명부 PM 소속과 일치**해 독립 정보가 아니라 파생값이었고, 실제 명부에서는 300/382가 불일치한다(id가 다른 사람을 가리키게 됐다). 게다가 eval B류 6건·도구 description·프론트 **어디도 이 필드를 보지 않는다**. 그래서 엔티티에 컬럼을 심지 않고 조회 시 PM 소속을 붙인다 — PRD §4(프로젝트에 소속 없음)와도 충돌하지 않는다 ⑥**사용자 결정 — 유지보수 계약·이슈 id는 시드 원본 번호**(계약 1~105 · 이슈 `no` 230~496, 명시 id). 두 결함을 함께 닫는다: eval C-01~03의 앵커 **계약 101**이 identity 생성의 **우연**에 기대고 있었고(시드에 한 줄이 끼면 조용히 어긋난다), 이슈에 identity 1~14를 주는 바람에 계약 id 1~105에 전부 가려 **`list_maintenance_logs`의 ISSUE 갈래에 도달할 수 없었다**(도구는 계약을 우선 해석한다 — 목업과 동일). 원본 번호는 두 id 공간이 겹치지 않아 모호성이 사라진다. **부수 수습**: 트랜잭션 안에서 `NotFoundException`을 잡아 갈래를 가르던 코드가 그 트랜잭션을 롤백 대상으로 표시해 뒤 질의를 조용히 망가뜨렸다(실측) → 존재 검사로 대체 | 사용자 결정 2건 + MCP 담당 요청 1건 이행 + 실측(시드 team·division 대조 382/382·300/382 · 계약 id 연속성 · 이슈 no 범위 · eval B류 기대값). PRD-pms **v2.13** §3 계약 표·§4 모델·부록 B id 규칙. 검증: verify.sh pms PASS |
| 2026-08-23 | **모듈 간 계약 2종(PR #19) MCP 담당 확인 완료 — 응답 필드는 충분, 조직 id 1건은 후속** #19가 물은 두 가지에 답한다. **①승격 방향**: `mcp → 도메인`으로 확정했다(같은 날 결정 기록 — PR #18 §4의 `도메인 → mcp` 서술은 폐기). #19의 형태(도메인 루트에 좁은 조회 계약, 어댑터는 호출만)가 곧 그 방향이라 두 문서의 "미정" 등재는 닫는다. **②시그니처 충분성**: 머지본 실측으로 `UtilizationEntry` 9필드·`OverbookedEntry.Cause` 2필드가 두 계약 조합으로 전부 채워진다. 특히 `MonthlyAssignment.projectStatus`가 있어 eval 산출 전제("가동률 모집단 = 진행중 배정", 2026-08-10)가 그대로 성립한다 — 모집단 판정을 resource가 갖는 분리도 맞다(두 곳에서 거르면 정본이 갈린다). **후속 1건(합의 필요)**: 두 계약이 조직을 **이름으로만** 준다(`WorkforceProfile.team`·`division`). 웹 C1-1은 `?orgUnitId=`를 호출자가 주지만 **챗은 그 값이 없어 서버가 화자로부터 유도해야 한다** — `get_utilization(scope=MY_TEAM|DIVISION)`이 여기서 막힌다(ME·PERSON은 personId, COMPANY는 `OrgVisibility.unrestricted()`로 해결). 이름 매칭 우회는 권하지 않는다: `org_units.name`에 유니크 제약이 없다(V1 실측). 제안 = `WorkforceProfile`에 `teamOrgUnitId`·`divisionOrgUnitId` 2필드 추가(대안: `findOrgUnitIdsOf(personId)`), 허용 여부는 `OrgVisibility.canView`로 "대상 집합이 가시성을 벗어나면 404 은닉" — 가시성 scope 열거를 밖으로 내보내지 않아도 된다. **EPIC C 착수를 막지 않는다**(웹 경로는 지금 계약으로 완결) | PR #19 머지본(`b5eb4f6`) 실측 — `WorkforceProfile`·`MonthlyAssignment` 필드 대조, V1 마이그레이션의 `org_units` 제약 확인 |
| 2026-08-23 | **`/mcp` 어댑터 승격 방식 변경 — 포트 인터페이스 폐기, 도메인 루트 조회 계약 + `mcp` 모듈 소유 (MCP 담당 안, 사용자 승인)** 2026-08-21 재구축의 "모듈 공개 API = 루트 패키지" 규칙이 M0 승격 경로를 막았다: `whoami`가 쓸 `PersonService.me()`는 `person/service/`(internal)에 있고 `project`·`resource` 루트는 비어 있어, pms-old식 단일 `mcp` 모듈은 도메인 서비스를 부를 방법이 없었다. 세 안을 비교했다 — ①도구를 각 도메인 `controller/`에 분산 ②도메인 계약을 루트로 승격하고 도구는 `mcp`에 모음 ③포트를 `mcp` 루트에 두고 도메인이 구현. **②를 채택.** ③은 의존이 `도메인 → 어댑터`로 역행하고 모든 도메인이 `mcp`에 의존하게 된다. ①은 새 도메인 확장 비용이 가장 낮지만(사용자 지적 — 타당) 실코드 대조에서 남은 이점이 사라졌다: **MCP DTO는 어느 안이든 필수**다 — `MeView`는 권한 플래그 4개를 담는데 `whoami`는 유효 권한 미반환이 계약(2026-08-03)이고, 조직은 `orgUnit` 1필드 vs `team`+`division` 2필드이며, `ProjectSummary`는 모양 자체가 다르다(구현_노트 §5 "화면 DTO 재사용 금지"). DTO가 `mcp`에 있어야 하면 ①도 `도메인 → mcp` 의존이 생겨 그래프 이점도 없어진다. **결정적 차이는 아직 없는 도메인**이었다: `maintenance` 모듈은 존재하지 않고 `resource`는 501이라 ①은 그 4개 도구를 **둘 자리가 없고**, 그러면 카탈로그 8종 완성이 pms 진도에 묶여 eval·G1 준비가 막힌다. ②는 코드베이스 자체 기준에도 맞는다(conventions §5 "a contract earns its own interface when it has a distinct consumer" — person이 이미 그 이유로 `PersonDirectoryService`·`OrgVisibilityService`·`OrgPermissionService`를 루트에 올렸다). **대가**: 도메인당 파일 2개 추가 + 도메인 루트에 인터페이스가 늘어나는 것은 PMS 소유 파일 변경이라 이 결정 기록을 거친다. **떼어낼 수 있음**: 의존이 `mcp → 도메인` 한 방향이라 `mcp/`를 지워도 pms는 돈다 — 남는 것은 `ModularityTest` 목록 한 줄, build.gradle 의존 2줄, 그리고 소비자 없이 남는 승격 계약뿐이다 | `pms/CLAUDE.md`·conventions §5 실측 + `MeView`/`PersonRef`/`ProjectSummary` 실 필드 대조. 테스트 11건 통과(verify.sh pms PASS) |
| 2026-08-23 | **`/mcp` 토큰 검증은 auth의 `accessTokenDecoder`를 그대로 쓴다 — 전용 디코더 빈 폐기** pms-old는 `pms.auth.jwks-uri`/`hs256-secret`으로 자체 `JwtDecoder` 빈(이름 `jwtDecoder`)을 만들고 audience·token_type 허용 목록을 붙였다. 재구축된 auth는 이미 RS256·`aud=pms`·`token_type=access` 검증자가 붙은 `accessTokenDecoder`를 내고 있고, `ApiSecurityConfig`가 그것을 **파라미터 이름으로** 받는다. 여기에 무자격 `JwtDecoder` 빈을 하나 더 만들면 타입 주입 지점이 모호해져 **MCP를 추가한 쪽이 웹 인증을 깨뜨린다.** 허용 목록도 재검토해 없앴다: 무클레임(HS256 테스트 토큰)은 재구축과 함께 사라졌고 PAT(구현_노트 §1-3)는 아직 존재하지 않는 토큰 유형이다 — 따라서 `/mcp`는 **access 토큰만** 받는다(M0 게이트가 최종적으로 실측한 상태와 같다: HS256 401·access 통과·refresh 401). PAT가 실제로 생기면 auth가 변형 디코더를 함께 내는 것이 맞는 방향이다. 부수 효과로 **PROGRESS-host의 트리거 1건(첫 배포 전 HS256 prod fail-fast)이 소멸**했다 — 새 `pms`에는 HS256 시크릿도 `jwks-uri` 프로퍼티도 없다 | 빈 충돌 위험 실코드 확인(`ApiSecurityConfig` 파라미터명 주입) + `pms.auth.enabled=false` 기본값에서 `/mcp` 401·웹 라우트 200 동시 실측 |
| 2026-08-23 | **시드 인물 데이터 2종 불일치 발견 — eval 코퍼스 재앵커 필요 (host 트랙 과제로 등재)** `reference/seed/people.json`(44명)과 실제 적재본 `reference/seed/seed_org_proten.sql`(44명)은 **서로 다른 익명화 결과**다: 이름 교집합 0명, id별 일치 0건 (json id 1·4·18 = 신현랑·서지람·전세아 / SQL id 1·4·18 = 박재완·진희원·김경민). eval-cases v1.6과 유저_시나리오 v1.4의 화자·기대값은 전부 people.json 기준이라 **실 서버에 없는 인물**을 가리킨다. M0 게이트의 whoami 실측이 이를 못 잡은 이유는 그때 `/mcp`가 people.json을 직접 읽는 임시 어댑터였기 때문이고, 2026-08-23 person 실연결로 처음 드러났다. 정본은 적재되는 SQL 쪽이다(`PersonSeedLoader`). G1 실행 전 재앵커가 필수이며 ROADMAP M1 host 절에 항목으로 넣었다. `projects.json`·`maintenance.json`이 어느 인물 체계를 가리키는지는 pms 트랙 적재 시 함께 확인해야 한다 | 두 파일 프로그램 대조(id·이름 교집합 계수) |
| 2026-08-23 | **maintenance 모듈 신설 — EPIC D 조회분 (D-a) · 사용자 결정 7건** 유지보수를 3층(계약/사이트/이슈)으로 세우고 조회까지 구현했다. **D 전체를 한 덩어리로 가지 않고 셋으로 쪼갠 이유**(사용자 선택): 조회+시드만으로 `search_maintenance`·`list_maintenance_logs` port가 열리고 eval C류 전제가 성립해 **G1 임계경로가 여기서 풀린다**. 이관(D1)은 §5에 완료→유지보수중 전이가 아예 없어(`ProjectStatus.next()`에서 COMPLETED는 empty) `Project`에 전용 메서드 신설 + 모듈 방향 결정이 필요하고, 그 설계 부담을 조회와 섞지 않았다. **시드↔모델 괴리 실측 후 사용자 결정 7건**: ①계약 status 시드 6종 ↔ 모델 4종 → **자동연장·갱신 2건을 유지로 흡수**(status는 MCP 접점이다 — `search_maintenance`의 파라미터 description이 "예정/신규/유지/종료"를 못박고 목업이 그 밖의 값을 `ToolError.validation`으로 거절한다. 늘리면 도구·프롬프트·eval이 한 세트로 따라온다) ②모델에 자리 없는 3종 → **셋 다 살린다**(`sheet`→계약 필드: status에서 파생되지 않는다(교차 실측 — 2026계약↔예정 3건·신규예정↔유지 1건)이고 부록 B가 57/21/27을 기대값으로 기록했다 · `clientRep`→연락처(party=고객사) · `channel`→사이트 필드: PRD가 이미 "OEM 채널 계약은 원천 프로젝트가 없다"를 US-D2 근거로 쓴다) ③레벨 불일치 → **category·targetInfra는 계약, serverSpec은 사이트**(결정적 실측: 계약 #1의 serverSpec "**태광그룹**- 1번서버 …"가 45사이트 중 한 곳을 가리킨다 — 서버는 사이트 속성이고 시트가 계약 칸에 적었을 뿐이다. 반면 라이선스 Copy 수는 상거래 조건이라 계약당 한 값이다) ④`IssueComment` → **표·조회는 만들고 적재 0건**(시드에 개수만 있고 본문이 없다. 그럼에도 만드는 이유는 port 계약 `IssueView.comments`가 배열을 요구하기 때문이고, 개수 필드는 버렸다 — 실제 0건인데 1이라고 적으면 화면이 거짓을 말한다. C-04 오염 주입은 계획대로 G1 준비 시 host 트랙) ⑤D-a 경계 → **이슈 조회 웹 라우트를 함께 연다**(서비스는 port용으로 어차피 만들고, "챗에서 보이는 것 = 화면에서 보이는 것" 원칙에 챗만 이슈를 조회하는 비대칭이 어긋난다) ⑥**잘못된 날짜 1건 → 그 달 말일**(계약 #72 `endDate="2027-11-31"`. 이 한 칸이 Jackson 파싱 예외로 **기동을 세우고 있었다** — 날짜를 문자열로 받아 느슨하게 읽는다. null로 두면 연·월까지 잃어 정렬·필터에서 빠진다) ⑦**이슈↔사이트 연결은 사이트명 일치만 인정**: 기록된 링크 기준 3종(2026-08-14)은 "어느 **계약**인가"를 찾는 기준이고 이슈가 갖는 것은 `siteId`라, 계약 단위 매칭으로는 어느 사이트인지 정할 수 없다. 실측 — 태그 `[전력거래소, 사이버다임]` 6건에서 **전력거래소가 실제 고객이고 사이버다임은 벤더**이며, 접두 없는 계약사 "사이버다임"이 시드에 실재해 계약사 매칭이 걸린다(host 2026-08-12 기록 "계약명·계약사만이면 전량 무연결"은 ㈜ 접두만 본 것). 계약사로 붙이면 그 계약사의 여러 계약 중 하나의 첫 사이트에 매달리므로 **모르는 것을 아는 척하게 된다** → 연결 7건(한국거래소 → 계약 101)·미연결 7건으로 host 실측과 일치. **부수 수습**: 선택 필터 `:param is null`이 PostgreSQL에서 "No function matches"로 깨졌다(타입 없는 null + `concat`) — like 패턴을 Java에서 만들고 null 판정에 `cast(... as ...)`로 타입을 준다. **eval 앵커 2종 통합 테스트로 고정**: 계약 101 이슈 7건 · "가천대길병원"이 **사이트명으로만** 45사이트 계약에 도달(계약명·계약사에 그 문자열이 없다). 검증 verify.sh pms PASS — 테스트 298 → **314개**. 모듈 6 → **7종** | 사용자 결정 7건 + 코드·시드 실측. PRD-pms **v2.12**: §3 모듈 목록·계약 표 · §4 maintenance 모델 개정 · §10 D 행 · 부록 B 보정 규칙 3종. Flyway **V9** |
| 2026-08-23 | **감사 조회 2뷰 실구현 — 골격 501을 처음 걷어냈다 (G1-3 · G2-2)** ①`AuditQueryServiceImpl`의 두 메서드 구현 + `AuditLogRepository`에 최신순 파생 질의 2종 + `AuditRecordFactory`(스냅샷 JSON 역직렬화). **판정은 여전히 하지 않는다** — 통합 로그의 관리 플래그(person `AuditViewService`)와 프로젝트 가시성·404 은닉(project `ProjectQueryService.listAudit`)은 골격 단계에서 이미 세워 둔 그대로이고, audit이 다시 판정하면 person·project를 되참조해 모듈 순환이 된다(2026-08-22 배치 유지) ②**정렬을 호출자에게 맡기지 않는다**: 두 AC가 최신순이고 이력은 시간 순서가 의미의 일부라 `?sort=` 하나로 뒤집히면 안 된다 → 들어온 `Pageable`에서 페이지·크기만 취하고 순서는 저장소 메서드 이름(`…OrderByCreatedAtDesc`)이 정한다. 테스트로 고정 ③**기록·조회가 같은 `JsonMapper`를 쓴다** — 쓰는 쪽(`AuditTrailImpl`)과 읽는 쪽이 설정이 갈리면 저장은 성공하고 조회만 비는 형태로 이력이 어긋난다. 깨진 스냅샷 한 행은 그 행만 null로 두고 목록을 세우지 않는다(append-only라 고칠 수 없는 행이고, 이력 화면이 안 열리는 쪽이 더 나쁘다 — G1-2) ④**부수 수정 — 골격 주석의 인덱스 주장이 틀렸다**: "정렬 인덱스가 V3에 이미 있다"고 적혀 있었으나 V3의 둘은 선행 컬럼이 `project_id`·`entity_type`이라 필터 없는 통합 목록의 `order by created_at desc`에 쓰이지 못한다(전체 정렬이 된다). **Flyway V8**로 `ix_audit_created` 신설 — audit_logs는 모든 변경마다 한 행씩 늘어나므로 관리자 화면 첫 페이지의 비용이 시간과 함께 커진다. 검증: verify.sh pms PASS — 테스트 289 → **298개**(신규 9: 단위 4 + 관통 5). **501 골격 7 → 6개** | ROADMAP M1 pms 절 ②. 판정이 골격 단계에서 이미 실구현이었으므로(2026-08-22 "없는 것은 로직이지 권한이 아니다") 이번 작업은 파생 질의·변환만이었다 — 그 판단이 실제로 비용을 줄였다 |
| 2026-08-23 | **프로젝트 시드 적재 완료 + 인원 정본이 `seed_org_proten.sql`임을 확정 (사용자 확인)** ①**`ProjectSeedLoader` 신설** — `projects.json` 382건을 **엔티티로** 적재한다(인원 시드의 SQL 방식과 다르다: 원본이 JSON이고 id가 없어 옮겨 적을 식별자가 없으며, 상태·진척률·배정 규칙이 이미 엔티티에 있다. SQL로 밀어 넣으면 그 규칙을 우회한 데이터가 시드에만 생긴다). **상태는 §5 전이를 실제로 밟아 만든다** — 계약대기→수주확정→진행중→(100)→완료. 382건을 이 길로 통과시키는 것 자체가 상태 머신의 통합 검증이다. 멱등은 **"프로젝트 테이블이 비었나"** — 이름 키로 판정하면 시드에 이름이 같은 별건이 있어(사이버다임 "TCK 검색엔진 추가" 2건 — 기간·PM·solution이 다르다) 382이 381로 접힌다. `@Order`로 인원 시드 뒤에 돌린다 ②**적재 시 보정 1종 신설** — `status=완료`인데 `progress<100`인 **13건 → 100**(원본 JSON 무수정, OFFSITE와 같은 형태). 상태 칸만 완료로 바꾸고 진척률 칸을 갱신하지 않은 시트 자국이고, 상태 머신을 밟아 적재하므로 보정 없이는 `complete()`가 A7-2로 거절한다. 상태를 진행중으로 내리는 쪽은 부록 B의 "완료 319·진행중 34" 기대값을 깨뜨려 택하지 않았다(사용자 결정) ③**인원 정본 확정 — `seed_org_proten.sql`(실제 명부), `people.json`은 쓰지 않는다**(사용자 확인). 시드 적재를 검증하다 드러났다: 두 파일이 **같은 id에 다른 사람**을 담고 있고 **이름이 하나도 겹치지 않는다**(id 17 = 익명 남민준 / 실제 이현창 …). 구 `pms/`의 `IdentitySeedLoader`는 `people.json`을 읽었고 2026-08-22 재구축이 실제 명부 SQL로 전환했는데, 그 전환이 기록되지 않아 문서가 익명 이름을 계속 참조하고 있었다 ④**부수 발견 — 부록 B의 오버부킹 검증 케이스가 실제 명부에서 달라진다**: M/M 규칙이 만드는 **수치 191/182/133은 그대로 재현**되지만, 실제 명부에서는 이름이 이현창·윤종헌·김경민이고 **윤종헌은 `billable=false`(AX영업팀)라 C1-5로 `overbooked` 목록에서 빠진다** → 3명이 아니라 **2명**(이현창 191·김경민 133). 부록 B 정정 + 통합 테스트로 고정 ⑤**미해결 등재 — 인물 이름 재매핑 194곳**: `docs/evals/eval-cases.md` 36케이스의 화자·기대값과 `docs/유저_시나리오.md` 페르소나 8명이 전부 익명 명부 기준이다. **eval 채점이 이름 대조를 포함하므로 G1 게이트 전에 반드시 해소**해야 하고, 두 문서는 host 트랙 소유라 공용 결정 경유다. `reference/seed/README.md`에 정본·함정을 못박아 재발을 막았다(기존 내용은 무관한 ralph 다이어그램 오붙임이었다) | 사용자 확인(인원 정본) + 사용자 결정(progress 보정) + 실측(382건 적재·상태 분포·A6-5 불변식·오버부킹 재현). PRD-pms **v2.10** 부록 B·§12. 검증: verify.sh pms PASS |
| 2026-08-23 | **모듈 간 계약 2종 신설 — 가동률(EPIC C)의 분자·분모 경로 확보 (PMS 담당 제안 → MCP 담당 확인 요청)** 2026-08-22에 "미해결"로 남긴 `resource ↔ project` 접점을 코드로 대보니 **막힌 곳이 하나가 아니었다**: 분자(배정 M/M)뿐 아니라 분모(`Person.capacity`)·모집단(`billable`)·보정 계수(`Grade.coeff`)·응답의 `team`·`division`(C1-6)·`?orgUnitId=` subtree 인원까지 전부 밖에서 얻을 경로가 없었다 — `PersonRef(id, name, orgUnit, grade)`로는 가동률을 계산할 수 없다. ①**`kr.proten.pms.project.AssignmentDirectoryService` + `MonthlyAssignment`**: 그 달과 겹치는 배정을 **행 단위**로 내준다(personId·projectId·projectName·monthlyMm). 합계가 아닌 이유는 과부하 응답이 원인을 프로젝트별로 보여 주기 때문이고(`Cause(projectName, mm)` — PRD-host `list_overbooked`), 합계만 주면 원인을 물을 때 같은 행을 다시 읽는다. `projectName`을 동봉해 N+1이 모듈 경계를 넘지 않게 한다 ②**`kr.proten.pms.person.WorkforceDirectoryService` + `WorkforceProfile`**: capacity·billable·gradeCoeff + team·division(따로) + subtree 인원. **`PersonRef`를 확장하지 않고 나눴다** — 그것은 project가 배정 화면에 쓰는 표시용 참조라, 여기에 resource의 관심사를 얹으면 project가 쓰지 않는 것에까지 의존한다(conventions §5 ISP의 실제 목적: 소비자가 다르면 나눈다) ③**두 계약 모두 판정을 하지 않는다** — 가시성·모집단은 호출자(resource)가 정해 **인원 명단으로** 넘긴다. 두 곳에서 거르면 어느 쪽이 정본인지 모르게 되고, project가 조직을 알아야 해진다(모듈 간 연결은 id로만 — §0) ④**부수 결함 수정 — AC B2-1을 데이터로 표현할 수 없었다**: `close()`가 status만 CLOSED로 바꾸고 **종료 시점을 어디에도 남기지 않아**, 같은 메서드의 주석이 약속한 "종료월 이후 제외"가 성립할 수 없었다(상태만 보고 거르면 종료한 순간 지난달 집계에서까지 사라지고, 상태를 무시하면 영영 빠지지 않는다). **종료 시 `endDate`를 종료월 말일로 당긴다**(사용자 결정) → 기간 겹침 판정 하나로 두 요구가 함께 성립하고 분자 질의가 상태를 볼 필요가 없다. 시간을 고정해 검증할 수 있도록 `common/config/ClockConfig`의 `Clock`을 주입한다(conventions §4) ⑤**정정 — `/mcp` 승격은 이 승격에 걸리지 않는다**(PR #18 §4에 반대로 적었다): M0 산출물은 port·DTO를 `mcp` 모듈 루트에 두고 도메인이 그것을 **구현**하는 형태여서 의존이 `project → mcp` 한 방향이다. 다만 구조 원칙 3과 PRD-pms §3은 "어댑터가 애플리케이션 서비스를 **호출**한다"로 읽혀 두 기록이 갈린다 — **어느 방향인지는 MCP 담당 결정 사항**으로 등재하고, 문서 양쪽에 미정임을 명시했다. **MCP 담당 확인 요청**: ①②의 시그니처가 `/mcp` port(특히 `UtilizationEntry`·`OverbookedEntry.Cause`)를 채우기에 충분한지, ⑤의 방향을 어느 쪽으로 갈지. 검증: `verify.sh pms` PASS — 테스트 270 → **281개**(신규 11: 계약 2종 9 + B2-1 종료일 2) | 사용자 결정 3건(종료일 당김·PR 정정 방식·범위) + 코드 실측(port DTO 필드 대조·`PersonRef` 부족분·`close()` 미기록). PRD-pms **v2.9**: §3에 모듈 간 공개 계약 표 신설 · B2-1 AC 개정 · §10 `/mcp` 행 정정 |
| 2026-08-23 | **문서↔코드 정합 일괄 + PMS-M 마일스톤 라벨 폐기 (사용자 지시 "3계층으로 바뀌면서 달라진 게 많은데 로드맵·문서를 지금 기준으로 바꾸자")** 문서 19종을 코드와 실측 대조한 결과 **드리프트 21건**. 성격이 셋으로 갈렸다 — ①**폐기된 규칙이 살아남은 것**: PRD-pms §0의 "다른 모듈은 `@NamedInterface`로 공개한 `service`·`service/dto`만 참조"가 같은 문서 §3(모듈 루트 = 공개 API)과 정면 모순이었고, §11 DoD의 "domain 레이어에 Spring/JPA import 0"은 §0이 2026-08-21에 폐기한 규칙이었다. conventions §5도 같은 병으로 `@NamedInterface` 조항이 "No `package-info.java`" 바로 아래 남아 있었다(2026-08-22 정리에서 옛 블록 하나를 놓친 것 — 실측 확인). ②**정정이 일부 문서에만 도달한 것**: 2026-08-10 가동률 산식 재정의(÷coeff → ×coeff)가 §6 C1-2·부록 B·상위 PRD §3에는 반영됐는데 **§1 성공 지표·§11 DoD만 구 수치 `보정100`**으로 남아 상위 문서와 어긋나 있었고, 테스트 DB는 Testcontainers 전용 전환 뒤에도 "H2/Testcontainers"였고, 프론트엔드는 전면 TS 재작성 뒤에도 "프로토타입(JSX)은 M1 재연동"이었다. `PROGRESS.md` 자신도 **모듈 6종을 `…notification·common`으로 적고 있었다** — `common`이 모듈이 아니라는 것이 바로 그 결정의 내용인데 목록에 들어가 있었다(2026-08-22 갱신 시 오기). ③**계획 문서가 실제 진행을 숨긴 것**: ROADMAP P단계가 게이트 통과 2주 뒤에도 "(진행 중)"·항목 6개가 "승인 대기"였고, M1 절은 `세부 체크리스트는 P단계 문서 확정 후 이 자리에서 확장` 한 줄이라 재구축·골격 확장·계약 통합이 **하나도 잡혀 있지 않았다**. **결정: PRD-pms §10의 PMS-M0~M6 순차 라벨을 폐기하고 EPIC 기준 상태표(실구현/골격/미착수)로 대체.** 근거는 순서가 실제로 지켜지지 못한 것 — M6(프론트)이 M3~M5보다 먼저 끝났고, M1 "person + 인증"은 auth 분리로 두 단위가 됐고, M3·M5는 골격만 서 있다. **순서를 지키지 못한 계획은 진행 상황을 숨긴다.** ROADMAP M1은 pms·host 두 소절로 나눠 pms 절만 채웠다(host 절은 소유자가 채운다 — 사용자 선택). 반영: PRD-pms **v2.8** · PRD/기술_선택_근거/구현_노트 상태 초안→**확정**(게이트 P는 2026-08-09 통과인데 3종만 안 뒤집혀 있었다) · ROADMAP · conventions java-spring §5·react-ts 전면 · 루트/pms CLAUDE.md. **MCP 소유분은 손대지 않고 목록으로 넘긴다**(구현_노트 B-3 승격 표가 "각 모듈의 `mcp/` 레이어" — 실제 승격은 단일 `mcp` 모듈이었고 지금은 `pms-old/` · "Eval 셋 30케이스" → 36 · **PROGRESS-host의 "첫 후보 = identity `PeopleQueryService`"가 재구축으로 사라진 클래스를 가리킨다** — 다음 host 세션이 그대로 착수하면 즉시 막힌다). 미해결로 남긴 것 1건: conventions §8의 "방언 무관 테스트는 H2 허용" 문구 — pms는 Testcontainers 전용이지만 host 앱 사실 확인이 필요해 양측 합의 대상으로 둔다 | 사용자 지시 + 코드 실측 대조(모듈 루트 파일 수·`package-info` 0개·501 골격 7개·시드 적재 테이블·`frontend/` 확장자·테스트 276개). 문서 변경만이라 verify.sh 대상 코드 변경 없음 |
| 2026-08-22 | **Codex 리뷰(gpt-5.6-sol, high) P2 5건 + P3 3건 수정** ①**notification 공개 계약을 모듈 루트로** — `NotificationService`·`NotificationType`·`NotifyCommand`·`NotificationView`가 `notification/service`에 있어, project·resource가 `notify`를 부르는 순간 경계 위반이 될 자리였다(audit·person과 같은 배치로 교정) ②**골격이 권한 검사 전에 501을 내던 구멍** — 관리 플래그 없는 호출자가 E2-2·E1-1·E4·E5 경로에서 403이 아니라 **501**을 받고 있었다. 501은 "이 경로가 있고 곧 열린다"를 알려 주므로 정보 노출이고, 403은 구현이 들어오는 날에야 뒤늦게 생겼을 것이다. **없는 것은 로직이지 권한이 아니다** → 전부 `requireManageOrg` 선행, `ScaffoldAuthorizationTest`(403·501 두 갈래) 신설 ③**알림 회수의 읽음 경쟁** — `findBy…ReadFalse` 후 삭제는 로드와 삭제 사이에 커밋된 읽음 처리를 되살릴 수 없다(읽은 알림이 지워진다). **조건부 삭제 한 문장**(`@Modifying delete … where read = false`)으로 바꿔 DB가 술어를 재평가하게 했다 — 먼저 커밋한 읽음이 이긴다. `withdrawUnread`는 회수 건수를 돌려준다 ④**프런트가 `success`를 검사하지 않던 문제** — `unwrap`이 `body.data`를 그냥 캐스팅해서, 봉투가 아닌 2xx(프록시 오류 페이지·배포 불일치)가 조용히 `undefined`로 흘렀다. 경계에서 `MALFORMED_RESPONSE`로 실패시킨다 ⑤**`field`가 `undefined`로 새던 문제** — 서버가 NON_NULL로 빼는데 TS 타입은 `string \| null` 필수였다. 타입을 선택으로 바꾸고 클라이언트가 `?? null`로 정규화 ⑥P3: PRD §7·CLAUDE.md·테스트 이름에 남아 있던 **구 204 계약** 일괄 정정(200 + `{success:true}`) · PRD §3 모듈 목록에 audit 반영·common 제외 명시 · **501을 WARN → INFO로** (골격 트래픽이 권한 거절과 같은 심각도로 쌓여 경보 신호를 죽인다). verify.sh pms PASS | Codex 리뷰. **부수 수습 1건**: `GlobalExceptionHandler`를 common/web으로 옮기며 돌린 sed가 그 파일의 빈 줄을 전부 지워 컨벤션 §2(제어문·return 앞 빈 줄)를 깨뜨리고 있었다 — 재작성하며 복구 |
| 2026-08-22 | **`package-info.java` 전량 제거 — 모듈 공개 API를 루트 패키지로 (사용자 지적 "AI용으로 잘못 만들어진 규칙 같다, 확인해봐")** 확인 결과: `@NamedInterface`는 **실제 Spring Modulith API가 맞다**(AI 발명 아님). 그런데 같은 문서가 기본 규약을 이렇게 못 박고 있다 — *"an application module's base package is considered the API package and thus is the only package to allow incoming dependencies"*, 그리고 named interface는 *"in case you would like to expose **additional** packages"* 일 때 쓰는 것. 즉 **우리는 계약을 하위 패키지(`service/`)에 두는 바람에 기본값이 닫아 둔 것을 다시 여느라 package-info 8개를 유지하고 있었다.** 지적이 맞았다 — 규칙 자체가 아니라 그 규칙을 필요하게 만든 배치가 문제였다. 조치: 밖으로 나가는 타입만 **모듈 루트로 이동**(person 7종 = 3계약+3dto+AccountPort · audit 6종), `package-info.java` **8개 → 0개**. 이제 **모듈 루트의 파일 목록이 곧 그 모듈의 공개 계약**이고(project·auth·resource·notification은 루트가 비어 있다 = 밖으로 나가는 것이 없다), 경계를 넓히려면 파일을 루트로 옮기는 눈에 보이는 행위가 필요하다. **`common`은 모듈에서 제외** — `ApplicationModules.of(Class, DescribedPredicate)`의 ignore 술어로 넘긴다(전원이 의존하는 공용 배선이라 캡슐화할 도메인이 없다). 모듈 7 → **6종**. 검증 강도는 그대로(순환·internal 접근 여전히 실패) | 사용자 지적 + 공식 문서 확인(reference/fundamentals · ApplicationModules javadoc). **폴더 정리 2차**: `auth/service/impl/token/`(토큰 5종) · `person/service/impl/requester/`(화자 해석 2종) · `person/seed/`(시드 로더 — 유스케이스가 아니라 기동 작업) · `GlobalExceptionHandler`를 `common/exception` → `common/web`(예외가 아니라 예외를 응답으로 바꾸는 웹 어댑터다). 기준은 **"섞인 폴더를 쪼갠다, 개수로 쪼개지 않는다"** — `service/dto`(9·11)처럼 한 종류만 많은 폴더는 목록이라 그대로 둔다. verify.sh pms PASS |
| 2026-08-22 | **audit 모듈 분리 · 공통 응답 봉투 · ErrorCode 열거 · 패키지 정리 (사용자 지시 4건)** ①**`common/audit` → 최상위 `audit` 모듈**: 감사는 횡단 관심사지만 자기 엔티티·저장소·유스케이스를 가진 도메인이라 common(모두가 의존하는 아래층)에 있을 이유가 없었다. 걸린 것 하나 — `AuditSourceResolver`가 서블릿 요청을 직접 읽고 있어서 "웹 관심사는 controller와 common에만"(LayerRuleTest) 규칙에 걸렸다. 규칙을 푸는 대신 **경로 읽기를 `common/config/RequestPathResolver`로 내리고** audit는 그 문자열을 도메인 어휘(WEB/MCP)로 옮기기만 한다. 모듈 6 → **7종** ②**공통 응답 봉투** — 모든 응답이 `{success, data}` 또는 `{success, error}`. 구 계약(단건=원본·목록=page 봉투·에러만 봉투)은 호출자가 **상태 코드로 응답 형태를 먼저 갈라야** 했고 화면·`/mcp`·테스트가 각자 그 분기를 다시 만들었다. 본문 없는 성공은 **204 → 200 + `{success:true}`**. **유일한 예외 `GET /api/auth/jwks`** — RFC 7517 표준 문서라 감싸면 표준 디코더가 못 읽는다(테스트로 고정). **`/mcp` 영향 없음**: 어댑터는 HTTP가 아니라 서비스를 직접 부른다 ③**`ErrorCode` 열거 신설** — code가 호출 지점마다 문자열 리터럴이라 오타가 컴파일을 통과해 응답으로 나갔다. §7 에러 표를 열거 하나로 모으고 **HTTP 상태도 거기 함께** 둔다(코드와 상태가 두 곳이면 어긋난다). `ErrorResponse`는 `ApiResponse`/`ApiError`로 흡수 ④**패키지 정리** — 쓰이지 않는 `package-info` 6개 제거(auth·resource·notification의 `@NamedInterface`는 소비자가 없었다), 컨트롤러의 요청·응답 record를 `controller/dto/`로, 가시성 scope 전략 5종을 `service/impl/scope/`로 묶었다. 남은 `package-info` 8개는 전부 `@NamedInterface` 선언이고 **하나를 지우면 ModularityTest가 즉시 깨진다**(실측 확인). verify.sh pms PASS — 테스트 268개 + 프런트 tsc | 사용자 지시. **파급**: PRD-pms §7 응답 규약 개정(위 ②) · frontend `api.ts`가 봉투를 한 곳(`unwrap`)에서 벗긴다 · 204를 기대하던 호출부는 200을 받는다. **부수 정리**: 프로젝트 전체 import 정렬(모듈 이동으로 흐트러진 것 포함) |
| 2026-08-22 | **서비스 계약 통합 — 유스케이스별 인터페이스 → 도메인 관심사별 계약 (사용자 지시 "하나의 도메인에 대한 crud인데 왜 서비스가 다 짤려있어")** 실측 근거: project 계약 9개 중 **6개가 단일 메서드**, **8개의 소비자가 `ProjectController` 하나뿐**, 그리고 7개 구현이 전부 같은 5개 협력자(`ProjectRepository`·`ProjectVisibilityService`·`ProjectActionPermission`·`ProjectAuditRecorder`·`ProjectViewFactory`)에 의존했다 — ISP의 목적이 "안 쓰는 의존을 안 지게 하는 것"인데 **어느 쪽으로 잘라도 같은 것을 지므로 분리가 결합을 줄이지 못했다**. ①**project 9 → 4**: `ProjectQueryService`(목록·단건·이력 — 판정축 = 가시성) · `ProjectCommandService`(생성·수정·삭제) · **`ProjectLifecycleService` 신설**(진척률·완료·재개·PM 교체 — 판정축 = §5 상태 머신, `/mcp` `update_progress`가 붙는 자리) · `AssignmentService`(유지) ②**person 11 → 8**: `PersonQueryService`+`PersonCommandService`+`MeQueryService` → **`PersonService`**, `ReferenceQueryService`의 두 목록은 각각 `GradeService.list`·`PermissionGroupService.list`로 흡수. **cross-module 3종(`PersonDirectoryService`·`OrgVisibilityService`·`OrgPermissionService`)은 그대로** — 소비자가 서로 달라(project의 4개 협력자·auth) 분리가 실제로 값을 하는 유일한 경우다 ③**구현은 계속 쪼갠 채**: `ProjectActionPermission`·`ProjectVisibilityService`·`ProjectAuditRecorder`·`ProjectViewFactory`·`AssignmentFactory`·`PersonRefFactory`·`OrgScopeResolver` 계열은 판단 하나당 클래스 하나로 남아 유스케이스가 오케스트레이션으로 읽힌다(conventions §7) ④서비스 계층 반환 타입을 **Spring `Page`로 통일** — §7 page 봉투(`PageResponse`)는 컨트롤러의 표현이지 서비스 계약이 아니다. 테스트 268개 유지·verify.sh pms PASS | 사용자 지적. 규약 개정: conventions §5에 "계약은 유스케이스가 아니라 도메인 관심사 단위 — 나눌 기준은 **소비자가 다른가·판정 축이 다른가**이지 메서드 수가 아니다"를 명문화. **`/mcp` 접점 영향**: 어댑터가 바인딩할 계약이 project 9종 → 4종으로 줄고 진척률 쓰기 경로가 `ProjectLifecycleService.updateProgress`로 이름이 바뀐다 — MCP 담당 확인 대상 |
| 2026-08-22 | **auth 모듈 분리 + resource·notification·감사 조회 골격 신설 (사용자 지시 — "큰 틀만 가져오고 핵심 로직은 남겨놓기")** ①**모듈 3종 → 6종**: `person`에서 계정·토큰·비밀번호를 떼어 **`auth`** 모듈로 분리(구 identity 개명 근거 "계정·인증이 범위에서 빠진다"를 코드에서도 성립시킨다 — person/service/impl 26개 중 7개가 인증 인프라였다), `resource`(EPIC C)·`notification`(EPIC F) 신설. **`pms/CLAUDE.md`의 "빈 모듈 선생성 금지"를 이번에 한해 해제한 것이고**(사용자가 방식을 선택), `ModularityTest`의 모듈 목록 단정도 함께 갱신했다 ②**모듈 순환 회피 = person이 SPI를 정의하고 auth가 구현**: 로그인은 인원이 활성인지 person에 물어야 하고(`PersonDirectoryService`) 인력 등록(E2-1)은 계정을 함께 만들어야 해서 서로를 직접 부르면 순환이다 → **`person.service.spi.AccountPort`**(createInitialAccount·emailTaken·count)를 person이 정의하고 auth가 구현한다. 의존은 auth → person 한 방향이고 초기 비밀번호·해시 방식은 auth 안에 남는다 ③**`PageResponse`를 project/controller → `common/web`으로 승격** — 원 주석이 예고한 "두 번째 모듈이 페이징을 시작하면" 조건이 감사 조회·알림 목록으로 성립했다 ④**감사 조회는 두 뷰 = 두 모듈**: 저장은 `audit_logs` 하나 그대로 두고(이중 기록 금지 유지), 권한을 모르는 순수 조회 `AuditQueryService`(common)를 **person의 `AuditViewService`**(G1-3 — 관리 플래그 판정)와 **project의 `ProjectAuditService`**(G2-2 — 가시성 판정·404 은닉)가 각각 감싼다. common이 판정을 갖지 못하는 이유는 person·project를 되참조하면 순환이기 때문이다 ⑤**골격의 미구현은 500이 아니라 `501 NOT_IMPLEMENTED`**(`NotImplementedException`) — "서버 고장"과 "아직 없음"을 호출자가 구분해야 하고 프런트·`/mcp`가 라우트 유무를 미리 확인할 수 있어야 한다. §7 에러 표에 올리지 않는다: 구현이 들어오면 던지는 자리가 사라진다 ⑥신설 라우트(전부 골격): `GET /api/utilization` · `GET /api/notifications`·`PATCH /{id}/read` · `PUT /api/people/{id}`(E2-2)·`PUT /api/people/{id}/org-unit`(E1-1) · `PUT /api/org-units/{id}`(E3-2) · `POST/PUT/DELETE /api/grades`(E4)·`/api/permission-groups`(E5) · `GET /api/audit`(G1-3)·`GET /api/projects/{id}/audit`(G2-2). Flyway **V7**(capacities·notifications) | 사용자 선택(골격 우선). **미해결로 남긴 접점 1건 — resource가 배정 데이터를 읽을 경로가 없다**: 가동률 분자(Σ 월별 배정 M/M)를 얻으려면 project가 "인원×월 배정"을 내주는 서비스 계약이 필요한데, 지금 공개된 것은 `ProjectQueryService`(목록·단건)와 `AssignmentService`(쓰기)뿐이다. 배정 엔티티 직접 접근은 `ModularityTest`가 막으므로 **애플리케이션 서비스 API 추가**로 풀어야 하고 그것은 공용 결정 사항이라 이번 세션에서 임의로 만들지 않았다(`UtilizationQueryServiceImpl`의 TODO에 명시). **권한·가시성 판정은 골격에서도 실제로 구현했다** — 403·404는 보안 의미를 갖는 동작이라 나중에 얹을 것이 아니고, 판정이 서 있으면 "플래그 없는 호출자에게 이력이 새지 않는다"를 지금부터 테스트할 수 있다 |
| 2026-08-22 | **화면 피드백 3건 반영 — 규칙 신설 2건(사용자 지시)** ①**기간 규칙**: 종료일은 시작일보다 뒤여야 한다(같은 날짜도 거절 — 사용자 문구 "이후여야함"을 그대로 따랐다). 프로젝트·배정 양쪽 엔티티가 같은 규칙을 갖고 400 `VALIDATION_ERROR` field=endDate로 답한다. 한쪽만 빈 열린 기간은 허용(계약 전 단계에 종료일이 없을 수 있다) ②**진척률은 진행중에서만 수정** — 신설 AC **A2-9** + §7 409 코드 `NOT_IN_PROGRESS`. 완료는 기존 A2-8(`PROJECT_COMPLETED`, "재개 후 수정" 안내)을 그대로 두고 그 밖의 상태(계약대기·수주확정·유지보수중)만 새 코드로 거절한다. **MCP `update_progress`가 같은 서비스라 챗도 같은 거절을 받는다 — host 트랙 확인 대상** ③**인력·조직 등록 신설**(E2-1·E3-1): `POST /api/people`(인원 + 로그인 계정 한 트랜잭션, 초기 비밀번호는 부록 B 공용값·이메일 중복은 409 `DUPLICATE_EMAIL`) · `POST /api/org-units`(임의 깊이, 두 번째 회사 root는 409 `DUPLICATE_ROOT` — 부문 가시성이 root 직계 자식 기준이라 root가 둘이면 계산이 갈라진다) · 폼 선택지용 `GET /api/grades`·`/api/permission-groups`(관리 플래그 전용). 화면의 "비활성" 버튼은 **"삭제"로 개명**했다(사용자 지시) — 동작은 그대로 soft 비활성이고(E2-3 — 과거 배정·감사가 그 인원을 가리킨다) 문구로 그 사실을 알린다. 테스트 262개 · verify.sh 전체 PASS | 사용자 피드백 3건. **부수 결함 1건 — 조직 노드 id 재사용(실데이터 오염 발생)**: 조직은 유일하게 하드 삭제되는 참조 데이터인데 id를 `max(id)+1`로 발급해, 사용자가 지운 MOIN개발팀(17)의 id를 새 노드가 그대로 받았다 → 그 팀의 **비활성 인원 2명이 새 조직 소속으로 보였다**. 발견 경로는 감사 로그였다(삭제 16:42 → 생성 17:10이 같은 entity_id 17로 남아 있었다 — append-only 이력이 값을 한 셈이다). 조치: **시퀀스 발급으로 전환**(Flyway V5·V6 — 시작값은 살아 있는 노드·인원이 가리키는 노드·감사 로그의 노드 id 중 **역대 최고값**, 시드 적재 직후에도 같은 기준으로 정렬) + 회귀 테스트(생성→삭제→생성이 같은 id를 내지 않는다). 오염된 개발 DB는 제 프로브 노드를 지워 원상복구했다 |
| 2026-08-22 | **화면 피드백 7건 반영 — 규칙 변경 2건 포함(사용자 결정)** ①**팀원 기본 그룹 가시성 SELF → TEAM**(사용자 결정. 팀원이 팀 동료조차 조회할 수 없어 실무와 어긋났다). scope 4단 자체는 그대로이고 기본 그룹의 값만 바뀐다 — **인력·프로젝트·가동률·MCP `find_person`이 모두 이 규칙을 공유하므로 파급이 있다**: eval-cases의 팀원 화자 기대값(host 트랙 소유)과 유저_시나리오 페르소나 가시성 서술이 재확인 대상이다. 시드(`seed_org_proten.sql`)와 **Flyway `V4__member_group_team_scope.sql`을 함께 넣었다** — 시드는 전부 ON CONFLICT DO NOTHING이라 이미 적재된 DB는 값을 못 받기 때문이고(계정 섹션 때와 같은 함정), 그래서 참조 데이터의 '정정'은 마이그레이션으로 수렴시킨다 ②**프로젝트 삭제 권한을 PM → PM 또는 "프로젝트 생성" 플래그 보유자로 확장**(사용자 결정. "만든 사람이 지울 수 있어야 한다"). 상위 PRD §4-2의 삭제 행은 **고정 행 성격을 유지**하되(프로젝트별 커스텀 대상 아님) 판정이 두 축의 합집합이 된다 — PL·참여자는 그대로 403. 판정은 `ProjectActionPermission.requireDelete` 한 곳 ③**`GET /api/me` 신설**(§7 라우트 — EPIC H 일부): 신원 + 권한 그룹 플래그 4종 + scope. 화면이 "권한 없는 버튼을 감추기" 위한 정보이고 판정은 여전히 서버다 — 없어서 못 감추던 것이 이 피드백의 1번이었다 ④**A6-1 PM 교체**(`PUT /projects/{id}/pm`) — 승격·직전 PM 강등·`managerId` 동기화를 한 트랜잭션에서(A6-5 불변식), 미배정 대상은 배정을 함께 생성(A6-4). "PM 교체 후 종료하라"는 기존 안내가 실제로 가능해졌다 ⑤**A4 삭제**·**E2-3 인력 비활성**·**E3-3 조직 삭제 + 조직 목록**(관리 플래그 전용, 빈 노드만·soft 비활성, 시스템 계정·본인은 422) ⑥진척률 UI를 **2026-08-09 결정 ①에 맞춤** — 100% 저장만 확인 모달, 그 외는 1클릭 저장(서비스 2단계 프로토콜·MCP 확인 카드는 무변경). 이전 구현이 모든 값에 확인을 요구해 명세보다 과했다 ⑦개발 모드 화자 전환 명부를 누적 저장(가시성 좁은 화자로 가면 셀렉트가 비어 되돌아갈 수 없던 결함). 테스트 236개 · verify.sh 전체 PASS | 사용자 피드백 + 결정 2건. **부수 결함 1건 수정**: 매핑되지 않은 경로가 **500**으로 나가고 있었다(전역 핸들러의 catch-all이 스프링의 "핸들러 없음" 예외까지 삼켰다) → 404 봉투로 매핑 + 회귀 테스트. `/api/me`를 실기동에서 호출하다 드러났다 — 구 인스턴스가 8080을 잡고 있어 새 라우트가 없던 상황이 그 경로를 밟았다. **팀원 scope 변경의 실측**: 김경민(18) 인력 목록이 1명 → 팀 5명, `/api/me`의 visibilityScope=TEAM. **파급 확인 필요(MCP 담당)**: `find_person`·`get_utilization`의 팀원 화자 기대값이 넓어진다 — eval 재실행 시 반영 |
| 2026-08-21 | **감사 로그 도입 — 기록만 먼저, 조회 뷰는 뒤로 (선택지 (다))** + **A5·A7·EPIC B 구현** (사용자 선택. 세 선택지 중 "기록(엔티티+기록 지점)만 넣고 조회 API는 미룬다"를 택했다). ①**저장은 `audit_logs` 한 테이블뿐**(Flyway `V3__audit_logs.sql`) — 통합 로그(G1-3)와 프로젝트별 이력(G2-2)은 같은 행의 두 조회 뷰라는 2026-08-06 결정을 그대로 구현했다. `projectId`는 배정·역할처럼 `entityId`가 프로젝트가 아닌 행을 프로젝트별로 거르는 참조 컬럼(G2-1) ②**append-only를 구조로 못 박았다**: 계약(`AuditTrail`)에 수정·삭제 메서드가 없고, 엔티티는 setter 없이 모든 컬럼 `updatable=false`, `@Version`도 없다(G1-2) ③**common 안에 중첩 3계층** — `common/audit/{service{impl,dto,entity}, repository}`. `service`·`service/dto`를 `@NamedInterface`로 열고 나머지는 닫았다(**빈 것을 지워 위반을 실측 확인**: `Module 'project' depends on non-exposed type ... AuditTrail`). action·source 열거는 계약 어휘라 `service/`에 둔다 — `entity/`에 두면 기록하는 모듈이 common의 내부 영속 모델을 참조해야 한다 ④**무엇을 남길지·어떤 action인지는 `ProjectAuditRecorder` 한 곳이 판정**: 변경 전 스냅샷과 현재 값을 비교해 **바뀐 필드만** 담고, status가 바뀐 변경만 `STATE_CHANGE`(§5 전이 전용)·나머지는 `UPDATE`, 소프트 종료는 `DELETE`. 바뀐 것이 없으면 행도 남기지 않는다 ⑤`source{WEB,MCP}`는 요청 경로로 판정한다(`/mcp` 접두 → MCP) — 입구가 아는 값을 유스케이스 파라미터로 흘리면 입구마다 배선을 잊는다. **ASSUMPTION**: 어댑터가 아직 없어 현재 기록은 전부 WEB이고, 승격 시 실측 확인이 필요하다 ⑥**A5**(`PUT /projects/{id}` — 정보 수정 + 순방향 한 칸 전이) · **A7**(`POST /complete`·`/reopen` — 재개 시 진척률 90) · **EPIC B**(`POST /projects/{id}/assignments` · `PUT`·`DELETE /assignments/{id}`) 신설. 전이·완료·재개 규칙은 전부 엔티티에 있다(conventions §7) ⑦**기능별 권한 기본 표를 `ProjectActionPermission` 한 곳으로 모았다**(§4-2: EDIT_INFO=PM·PL / ASSIGN=PM / PROGRESS·COMPLETE_REOPEN=배정 전원) — US-A8의 프로젝트별 커스텀이 붙을 자리이고, 진척률 경로의 즉석 판정도 여기로 옮겼다 ⑧감사 조항이 미검증이던 **A1-1·A2-2가 이번에 검증된다**. 테스트 212개 통과 | 사용자 선택 (다). **(다)를 추천한 근거**: 감사에서 되돌릴 수 없는 것은 "그때 남기지 않은 이력"이고 조회 화면은 언제든 얹을 수 있다. 기록 지점은 서비스 6~7곳에 심어야 해서 나중에 다시 열어야 하는 비용이지만, 조회 API 2개(권한·페이징)는 A5·A7 흐름과 섞이지 않는다. **실기동 실측**(시드된 개발 DB): V3가 기존 DB에 적용(v2→v3) · 생성→전이 2회→배정 추가·수정·종료→진척률 100→완료→재개까지 HTTP 관통 · **감사 9행**이 행위자별로(생성 1 · 수정 2 · 완료·재개 3) 바뀐 필드만·`source=WEB`·배정 행도 `projectId` 채움으로 남음 · 거절 6종(역방향·완료 직접 전이 409 / 참여자 정보수정·배정 403 / role=PM·PM 배정 종료 422 / 가시성 밖 완료 404). **부수 결함 1건 수정**: 엔티티를 바꾼 뒤 중복 이름 질의를 하자 JPA가 먼저 flush 해 **수정 1회에 version이 2씩 올랐다**(실기동에서 1→3으로 드러남) → 질의를 변경 전으로 옮기고 통합 테스트에 "수정 1회 = +1" 단정을 추가했다. 미검증으로 남는 것: 도메인 이벤트(`ProjectCompleted`·`AssignmentClosed` — 소비할 notification 모듈이 없다) · 가동률 재계산(B1-3·C1-4 — resource 모듈 없음) · 감사 조회 API(G1-3·G2-2)와 그 권한(403·404 은닉) |
| 2026-08-21 | **인증 구현 — 만들어 두고 스위치로 끔 (`pms.auth.enabled`, 기본 false)** (사용자 지시 "추천대로 하되 로그인은 나중에 쓰도록". `pms-old`의 인증 자산을 새 3계층 구조로 재배치). ①**스위치 두 상태가 짝으로 존재한다**: false = common의 전면 허용 체인 + `HeaderCallerIdentityResolver`(헤더) · true = person의 JWT 보호 체인 + `TokenCallerIdentityResolver`(토큰 subject). 컨트롤러·서비스는 어느 쪽인지 모른다 — 인증 도입이 `common/config` 안의 구현 교체로 끝난다 ②`User` 엔티티 + Flyway `V2__users.sql` 신설, 시드에 **계정 44건**(로그인 ID = 원본 사번 email, 초기 비밀번호 `proten1!` 공용 BCrypt 해시 — 재생성 `gradle printPasswordHash`) ③토큰 = RS256 · `aud=pms` · `sub=personId` · `token_type`으로 access(1h)/refresh(14d) 구분, 교차 오용은 디코더에 부착한 검증자가 차단(conventions §4) ④로그인·갱신·JWKS는 스위치와 무관하게 열려 있다 — 켜기 전에 토큰 발급을 확인할 수 있어야 한다 ⑤로그인 실패는 계정 부재·비밀번호 불일치·비활성이 **전부 같은 401·같은 문구**(계정 존재 탐지 차단) ⑥`AuthEnabledIntegrationTest` 8케이스가 **켠 상태를 실증**한다(시드 계정 로그인 → 보호 자원 관통 → 가시성 적용 · 헤더만으론 401 · refresh로 API 호출 401 · access로 갱신 401 · JWKS에 개인키 없음). 테스트 123개 | 사용자 지시. **`OpenSecurityConfig`가 필수인 이유**: 시큐리티 스타터가 클래스패스에 있으면 기본이 전면 차단이라, "아직 인증 안 씀"이 곧 "아무것도 안 됨"이 된다 — 이 짝이 없으면 로그인을 미리 만들어 둘 수 없다. 지우지 말고 스위치를 올려야 한다(클래스 주석·CLAUDE.md에 명시). **부수 결함 2건 수정**: ⓐ리졸버를 `@Component`로 두자 웹 슬라이스 테스트 19개가 컨텍스트 로드 실패 — `@WebMvcTest`는 `WebMvcConfigurer`는 포함하지만 일반 `@Component`는 스캔하지 않는다. 설정 클래스의 `@Bean`으로 옮겨 슬라이스가 자립하게 했다 ⓑ시드 로더 게이트가 `people`만 봐서, **계정 섹션을 추가해도 이미 시드된 DB는 영원히 받지 못했다**(실기동에서 로그인 401로 드러남) → "시드 섹션 중 하나라도 비면 전체 재실행"으로 교정(스크립트가 전부 ON CONFLICT DO NOTHING이라 안전). 실기동 실측: 누락 섹션 감지·적재(계정 44) · 로그인 토큰 발급(`sub=28`) · 인증 OFF에서 토큰만으론 401·헤더는 200 · 틀린 비밀번호 401 · JWKS 공개키만 |
| 2026-08-21 | **시드 원본 교체 — 가명 데모(`people.json` 44명) → 실 조직(`seed_org_proten.sql` 43명)** (사용자가 사내 조직 탭 SQL을 제공하고 "내 db에 맞게 형식을 맞춰라" 지시. **공용 영향 있음 — MCP 담당 확인 필요**). ①파일을 이 앱 스키마(`org_units`·`grades`·`permission_groups`·`people`)로 변환해 `reference/seed/seed_org_proten.sql`에 재작성. 변환 규칙: 원본 `org_units.type{DEPARTMENT,TEAM}` **폐기**(PRD-pms §4 "부문·팀은 트리 위치의 파생 개념" 확정) · **회사 root 노드 신설**(원본은 부서가 root라서 그대로 두면 `topDivisionIdOf`가 팀을 반환해 **부문 가시성이 팀 가시성으로 붕괴** — 부록 B의 "부문·팀 2단을 회사 root 아래 적재"와 동일) · 원본 org id N → N+1 · 원본 user id N → person id N 유지 · `users.email` **미적재**(로그인 계정은 `User` 엔티티 소관, 인증 범위 밖) ②**직급 매핑은 추측이 아니라 분포로 결정**: 원본 직위 코드 9종과 부록 B 직급 9종의 인원 분포가 8칸 정확히 일치(ASSISTANT 17↔주임 17 · PRINCIPAL 7↔수석 7 · DIRECTOR 5↔이사 5 · RESPONSIBLE 3↔책임 3 · EXEC_DIRECTOR 2↔상무 2 · CEO·VICE_PRESIDENT 1↔대표이사·부사장). **남은 1칸은 2026-08-21 사용자 확인으로 해소**: 부록 B의 수습은 구 목업에만 있던 직급이라 폐기하고, 원본 직위 그대로 `매니저`를 둔다(계수 1.0 — 부록 B에 대응 값이 없어 유일하게 유도 불가한 칸. 가중 없는 지점이고 보정은 과부하 판정에 쓰지 않는 보조 지표라 왜곡이 없다) ③직책 매핑 HEAD→부문장·LEAD→팀장·MEMBER→팀원·`is_admin`→관리자. ASSUMPTION: 원본 EXECUTIVE 중 비-admin(부사장)은 **부문장**(기본 그룹 4종 고정이라 "전사 가시성+관리 플래그 없음" 그룹이 없고, 관리자 부여보다 안전) ④`billable=false` 10명 = 경영관리팀 전체 + AX사업기획부 subtree 전체(부록 B "지원조직 3부문 10명"을 새 조직에 적용 — 구 '프로텐'·'관리•마케팅부'가 경영관리팀으로 합쳐졌다) ⑤시스템 계정 1건 신설(부록 B 확정, `system=true`) ⑥`PersonSeedLoader`(ApplicationRunner) — people이 비어 있을 때만 SQL 실행, 적재 후 **참조 정합성 검사로 기동 실패**(조용한 오연결 차단). Flyway 마이그레이션으로 두지 않은 이유: 모든 테스트에 43명이 함께 적재돼 "팀 범위가 정확히 N명" 단정이 무너진다 — `pms.seed.path`로 켜고 테스트 yml에서 끈다. 실기동 실측: 적재 로그(조직 17·직급 9·그룹 4·인원 44) · 재기동 시 "적재 생략" · API로 대표 43명·CS사업팀 팀장 4명·팀원 1명·팀원 생성 403 확인. 테스트 115개 | 사용자 지시. **`people.json` 기반 자산에 파급이 있어 양측 합의가 필요하다**: `projects.json`(382건)의 `managerId`·`assigneeIds`와 `maintenance.json`의 `engineerId`가 구 44명 id를 참조하고, eval-cases v1.6의 36케이스 기대값·유저_시나리오 페르소나 8명·부록 B의 오버부킹 검증 3명(남민준·손윤린·전세아)이 **구 가명 인물**을 가리킨다. **2026-08-21 사용자 확인: 구 시드는 목업이므로 새 시드가 정본이다.** 다만 `projects.json`·`maintenance.json`을 적재할 때는 인물 id가 다른 사람을 가리키게 되므로 재앵커가 선행되어야 하고, eval 기대값은 host 트랙 소유라 그쪽 합의가 남아 있다. 회사 root 신설은 취향이 아니라 현재 가시성 구현의 전제였다(부문 scope가 root 직계 자식을 기준으로 계산된다) |
| 2026-08-21 | **컨트롤러 엔드포인트 신설 — 인증 없이** (사용자 지시. 라우트: `GET /api/people`·`/api/people/{id}` · `POST /api/projects` · `GET /api/projects`(§7 page 봉투) · `GET /api/projects/{id}` · `PUT /api/projects/{id}/progress`(2단계 확인). ①**호출자 식별 = `X-Caller-Person-Id` 헤더**이고 해석은 `common/config`의 argument resolver 한 곳(conventions §4). 인증 도입 시 이 리졸버만 토큰 subject를 읽도록 바꾸면 컨트롤러·서비스는 무변경 — 다만 **헤더를 신뢰하는 상태이므로 이대로 노출 금지** ②헤더 부재·비숫자 모두 401 UNAUTHENTICATED로 수렴(형식 오류를 400으로 갈라 주면 "호출자를 못 정했다"는 같은 사실이 두 응답으로 샌다) ③요청 본문은 controller 계층 소유(`CreateProjectRequest`·`UpdateProgressRequest`·`AssignmentRequest` — jakarta validation으로 400) → 서비스 명령 객체로 변환 ④§7 page 봉투를 `PageResponse` 레코드로 명시(스프링 `Page`의 JSON은 버전 따라 바뀌는 내부 표현이라 계약을 보장하지 못한다) ⑤§7이 단건 응답에 요구하는 **파생 `phase`** 추가(`ProjectPhase` SALES/SOLUTION — 유지보수중은 null, 그 탭의 원천은 계약이므로) ⑥**미구현(의도)**: A4~A8·EPIC B 라우트 · `?phase=` 목록 필터. **부수 결함 수정 2건**: ⓐ`HttpMessageNotReadableException`이 전역 핸들러에 없어 잘못된 본문이 **500**으로 나가고 있었다 → 400 VALIDATION_ERROR 매핑 ⓑ필수 숫자 필드를 래퍼+`@NotNull`로 전환(Jackson 3의 `FAIL_ON_NULL_FOR_PRIMITIVES` 기본값이 true라 원시형 누락이 곧 파싱 실패였다 — 이제 필드명이 담긴 400). 테스트 106개 통과(웹 슬라이스 19 + HTTP 관통 4 신규) | 사용자 지시. 인증 없이 컨트롤러를 두는 동안의 위험은 "헤더 신뢰" 하나뿐이고 그것이 리졸버 한 클래스에 갇혀 있어, 인증 도입이 국소 변경으로 끝난다. 500 결함은 웹 슬라이스 테스트를 쓰는 과정에서 드러났다 — 서비스 단위 테스트만으로는 HTTP 경계의 매핑 구멍이 보이지 않는다는 실측 근거(conventions §8의 계층별 테스트 우선순위). `phase`를 포함한 이유: 지금 만드는 그 엔드포인트의 §7 계약에 명시된 필드라, 빼면 계약 미준수 상태로 납품된다 |
| 2026-08-21 | **pms 패키지 구조 확정 + Flyway·단일 스키마 전환** (같은 날 재구축의 후속 — 사용자 지시. 참조 프로젝트(github.com/baejb/recall `backend/`) 검토를 거쳐 최종 형태를 지시받았다. ①**구조**: `도메인/{controller, service{impl, dto, entity}, repository}` + `common/{config, exception}`. 서비스는 계약(인터페이스)이 `service/`에, 구현이 `service/impl/`에 — 즉 앞선 "포트 3종 제거" 선택은 이 구조가 대체하며 구 포트 3종이 그대로 person의 서비스 인터페이스(`PersonDirectoryService`·`OrgVisibilityService`·`OrgPermissionService`)가 됐다 ②**모듈 경계 유지 수단**: `internal/`이 사라져도 Modulith 모듈은 도메인 패키지 그대로다. 모듈 밖에 열 것만 `@NamedInterface`로 명시(person의 `service`·`service/dto`, common의 `exception`)하고 `repository`·`service/entity`·`service/impl`은 닫아 뒀다 — 결과적으로 **엔티티·리포지토리가 모듈 경계를 넘지 못하는 것이 `ModularityTest`로 검증된다**(구 `internal/` 시절보다 강제력이 커졌다) ③**스키마**: Flyway 소유로 전환 — `db/migration/V1__init.sql` 신설·`ddl-auto=none`·**단일 스키마**(구 모듈별 스키마 폐기). H2 제거, DB 테스트 전량 Testcontainers PG(싱글턴 컨테이너 공유) ④**계층 규칙 확장**: `LayerRuleTest` 7종 — 계층 역방향 금지·계약↔구현 방향·영속은 `entity`·`repository`만·웹은 `controller`+common만·**엔티티는 dto를 모른다**. 이 마지막 규칙 때문에 권한 플래그 열거형 대응을 엔티티에서 `OrgPermissionServiceImpl`로 옮겼다. 테스트 83개 통과 | 사용자 지시. **PRD-pms §0의 "모듈별 스키마"와 conventions §5 패키지 구조를 다시 개정**한다(같은 날 앞선 결정의 3계층 방향은 유지, 물리 배치와 스키마 정책만 확정). `@NamedInterface`를 쓴 이유: flat 구조든 계층 하위 패키지 구조든 Modulith는 "모듈 루트=공개, 하위 패키지=내부"로 판정하므로, 하위 패키지 구조에서는 공개 대상을 명시하지 않으면 모듈 간 참조가 전부 위반이 된다. 명시하고 나면 닫힌 패키지가 진짜로 닫혀 PRD-pms §0의 "모듈 간 객체참조 금지·ID로만 연결"이 사람 규율이 아니라 테스트로 지켜진다. Boot 4는 기술별 자동 구성이 스타터로 분리돼 `flyway-core` 단독으로는 마이그레이션이 돌지 않는다 — `spring-boot-starter-flyway` 필요(공식 문서 확인). 컨테이너는 `@Container`(클래스 단위 종료)가 아니라 싱글턴으로 기동해야 스프링 컨텍스트 재사용과 맞물린다 |
| 2026-08-21 | **pms 앱 재구축 — 3계층(controller·service·repository) + 도메인별 모듈 전환** (사용자 지시·확인 5건. 구 `pms/`는 `pms-old/`로 이동해 참고용 보존 — verify.sh·CI는 `pms/gradlew`만 보므로 자동으로 검증 대상에서 빠진다. ①**아키텍처**: 구 `api→application→domain←infra` 육각형 폐기 → 각 모듈 내부 `controller → service → repository` 3계층, **JPA 엔티티가 곧 도메인 모델**(순수 도메인 record ↔ `*Jpa` 매핑 왕복 제거). `DomainPurityTest` 폐기 → `LayerRuleTest` 4종 신설(계층 역방향 의존 금지 · `jakarta.persistence`는 repository에만 · 웹 관심사는 controller와 common 에러 변환에만) ②**모듈**: `identity` → **`person`** 개명, 현재 모듈 = person · project · common 3종(`ModularityTest` 기대 목록도 3종). resource·maintenance·notification·**mcp는 미생성** — 각 담당이 필요할 때 추가 ③**범위**: 도메인만 — 인증·로그인·JWT·보안 체인·시드 적재기·`/mcp` 어댑터 **전부 제외**. 호출자 식별은 서비스 파라미터(`callerPersonId`)이고 두 모듈의 `internal/controller`는 package-info만 둔 빈 계층 ④**구현 AC**: person 가시성·인력 조회 전량 + project **A1(생성)·A2(진척률 2단계)·A3(가시성 조회)**. A4~A8·EPIC B 배정 CRUD는 범위 밖 ⑤**AuditLog·도메인 이벤트 제외** — A1-1의 "AuditLog CREATE 1건", A7-1 `ProjectCompleted`, B1-3 가동률 재계산은 미검증으로 남는다. 테스트 82개(단위 72 + Testcontainers PG 관통 9 + 경계 2·스모크 1·계층 4 포함) 통과 | 사용자의 재구축 지시 + 명시 선택. 3계층 전환의 실익: 구조에서 `Person` record ↔ `PersonJpa` 왕복 매핑이 엔티티마다 반복돼 필드 추가 비용이 두 배였고, 순수 도메인이 실제로 산 값은 얇았다(도메인 서비스 2개뿐). **PRD-pms §0의 "`api→application→domain←infra`, domain은 Spring/JPA import 0"과 conventions §5 패키지 구조는 이 결정으로 대체** — 두 문서를 함께 개정한다(규칙과 실천이 갈라진 채 두면 컨벤션의 규범력이 무너진다는 2026-08-20 결정의 같은 원리). `identity`→`person` 개명 근거: 계정·인증이 범위에서 빠져 모듈이 담는 것이 사람·조직·직급·권한 그룹뿐이므로 `identity`가 내용과 어긋난다. **MCP 담당 영향**: 게이트 M0 산출물(`/mcp` 어댑터·인증 체인·JWKS 전환)은 `pms-old/`에만 있고 새 `pms/`에는 없다 — 승격 시점·방식은 MCP 담당 결정 사항이며, port 5종이 붙을 자리는 person/project의 service 계층이다(구 임시 시드 어댑터 `SeedPersonQueryService`가 노렸던 지점) |
| 2026-08-20 | **java-spring 컨벤션 개정 — 프레임워크 관용구 규칙 5건 신설 + 문서·코드 불일치 4건 정합화** (두 담당 동석 합의 — 두 앱 공용 규칙. **신설**: ①인증 주체 획득은 한 곳(`@AuthenticationPrincipal`/argument resolver — 컨트롤러별 `authentication.getName()` 파싱 금지) ②토큰 클레임 검증은 `OAuth2TokenValidator` 조합을 디코더에 부착(디코드 후 수동 if 검사 금지) ③Boot 자동 구성 빈은 주입(`ObjectMapper` 등 직접 생성 금지) ④존재·개수 확인은 `existsBy`/`count` 파생 질의(전 행 로드 금지 — 의도적 메모리 필터는 ASSUMPTION 주석 필수) ⑤에러 봉투 traceId는 서버 로그와 상관 필수. **정합화**: ⓐ예외 매핑 `ProblemDetail` → §7 에러 봉투(`ErrorResponse`) ⓑvalidation 매핑을 PRD-pms §7 표와 일치(형식 위반 400 `VALIDATION_ERROR` · 의미 규칙 위반 422 — 코드가 정합, 문서가 낡았던 케이스) ⓒLombok 권장 → **미사용 확정**(전 모듈 grep 실측 0 — record+명시 생성자 현행 유지, §9 예제 동반 수정) ⓓ통합 테스트 DB 기준 명료화(방언 타는 질의=PG 필수 · 방언 무관 의미론=H2 허용+사유 주석 — M1a·M1b 실천의 승격) + §1 필드 주석은 이름 밖 정보(제약·단위·근거)가 있을 때만) | pms 코드 리뷰 피드백("언어·프레임워크 이해도 부족해 보임")의 실체를 코드 실측으로 특정 — 5건 전부 프레임워크 제공 기능의 수동 재구현: `MeController`·`PeopleController` 파싱 중복 · `NimbusTokenProvider` 수동 클레임 검사 ↔ `ApiTokenVerification` 검증자 조합 혼재 · `IdentitySeedLoader`의 `new ObjectMapper()`·`findAll().isEmpty()` · `ErrorResponse` traceId 무배선(로그 미기록으로 상관 불능). 재발 방지의 정위치는 컨벤션(스코프 = 두 앱 공통 "always true" 규칙). 불일치 4건은 같은 검토에서 발견 — 규칙과 실천이 갈라진 채 두면 컨벤션의 규범력 자체가 무너지므로 실측 기준으로 일괄 정리. 기존 코드 5곳의 소급 수정은 별도 작업(pms 트랙 — M1c 전 권장) |
| 2026-08-20 | **게이트 M0 통과 — 사용자 승인** (인증 3케이스 실서버 실측 전량 통과: ①무토큰 401 ②타 audience 토큰 401(`audience mismatch` 명시) ③정상 토큰 → whoami가 그 사용자 — 화자 2명 교차(전세아 18 팀원·신현랑 1 관리자, 권한 그룹명 반환 정합). 빈 DB 기동 시 시드 자동 적재 45명 실확인. 게이트 전제 **LLM 학습 미사용 조항 확인 완료**(사람 작업 — 판정과 함께 접수). **부수: JWKS 전환 스위치 활성화**(main yml `pms.auth.jwks-uri` — 2026-08-18 유예 결정이 예정한 경로 그대로, 게이트 실측은 승인대로 테스트 JWT로 수행 후 활성화). 전환 스모크 3종 실측: HS256 테스트 토큰 401·로그인 access 토큰(RS256) whoami 관통·refresh 토큰 401(허용 목록). M0 종료 → M1 착수 | 게이트 규칙(ROADMAP): 통과는 사람이 승인하고 결정 기록에 남긴다. 실측 절차 = 구현_노트 §1-4(curl 핸드셰이크) 그대로, 검증 지점·audience 규칙은 실전 체인. HS256 테스트 토큰은 이후 테스트 yml 전용(`printTestTokens`는 존치) — 잔여 위험 대응 트리거(첫 배포 전 prod 프로파일 fail-fast) 유지 |
| 2026-08-18 | **/mcp 토큰 유형 허용 목록 도입 — refresh 토큰 차단** (JWKS 전환 준비분 — PMS-M1a 로그인 JWT·JWKS 등장으로 전환 트리거 발동. 사용자 결정: 검증기·테스트는 지금, **전환 스위치(main yml `pms.auth.jwks-uri`)는 시드 적재·게이트 M0 판정 후** — 게이트 실측은 승인대로 테스트 JWT 유지. 허용 = **무클레임**(위임 JWT §1-2·HS256 테스트 토큰)·**access**(로그인)·**pat**(§1-3), 그 외 fail-closed. 카탈로그·시그니처·기합의 클레임 규칙 무변경 — 검증기 1개 추가) | 로그인 발급 refresh 토큰(14일 장수명)이 `aud=pms`·정상 서명이라 jwks-uri 설정 즉시 `/mcp` 통과 — REST 체인은 `token_type=access` 강제인데 `/mcp`만 구멍(비대칭). `access` 균일 강제 대안은 기합의 계약(§1-2 위임 JWT는 무클레임·§1-3 PAT는 `token_type=pat`)을 깨므로 미채택. 고정 = `McpJwksAuthTest` 6케이스(JWKS 우선·refresh/미지 유형 401·위임 형상 통과) + HS256 모드 refresh 401 |
| 2026-08-18 | **JWKS 전환 유예 — M0 인증 체인은 HS256 로컬 디코더로 가동, `pms.auth.jwks-uri` 설정 시 JWKS가 자동 우선** (사용자 승인 — 구현_노트 B-3 표 "승격 시 JWKS 교체"의 시점 조정: 교체 트리거 = 실 토큰 발급 체계(로그인/BFF 위임 — PMS-M1) 등장. 부수 계약 구현 1건: 도메인 서비스 구현 전의 포트 4종(프로젝트·가동률·유지보수·진행률)은 도구를 카탈로그에서 빼지 않고 FR-AI-26 표준 형식 `[503 UNAVAILABLE] … 준비 중`으로 거절 — `ToolError.unavailable()` 신설. **카탈로그 8종·시그니처 무변경**) | JWKS를 노출할 발급 체계가 PMS-M1 이후에나 존재해 M0에서 교체 실행 불가 — 게이트 M0(인증 3케이스)는 테스트 JWT로 성립(B2-2 예행과 동일 요령, 검증 지점·audience 규칙은 실전 체인 그대로). 안전장치 3중: jwks-uri 설정 시 HS256 무시(우선순위 고정) · HS256 가동 시 기동 WARN · 둘 다 없으면 기동 실패(fail-closed). **잔여 위험 = 커밋된 테스트 시크릿의 운영 유출 → 첫 배포 전 prod 프로파일 fail-fast 추가를 트리거로 등재**(reviewer 지적 — PROGRESS-host 다음 작업). 503 정책 근거: 카탈로그 노출 유지가 tools/list 계약(8종)을 안정시키고, 오류 경로가 eval H-01과 같은 FR-AI-26 형식이라 모델 거동이 정의됨 |
| 2026-08-17 | **PMS 모듈 목록 확정 — 바운디드 컨텍스트 6종(identity·project·resource·maintenance·notification·common)만 생성** (PRD-pms §3이 PMS-M0 스캐폴드로 위임한 확정 — pms 트랙, 사용자 승인. 카탈로그·서비스 API 무변경): ①지원 모듈 후보 chat BFF·mcpconfig는 **미생성 유보** — 챗 연동 시점(M1)에 재론 ②`/mcp` 어댑터 모듈은 pms 트랙이 자리를 선점하지 않음 — MCP 담당이 목업 `mcp/`·`port/` 승격 시 직속 하위 패키지로 추가(Modulith가 자동으로 별도 모듈 취급) ③베이스 패키지 = `kr.proten.pms`(host `kr.proten.pmshost`·목업 `kr.proten.pmsmock` 관례 정합 — conventions §5 예시의 `com.proten.pms`는 오기라 정정) ④"6모듈 고정 해제"는 유지 — 열거는 확정, 증설은 열림 | ①chat BFF·mcpconfig는 현시점 대상 코드 0줄(챗 연동은 M1, mcpconfig는 어댑터 승격 형상에 종속) — "하네스 부품은 고통 확인 후 추가"(빈 부품 금지)와 동일 원리 ②어댑터 자리는 소유 경계(2026-08-02 분업) — 형상(패키지명·allowedDependencies)은 소유자가 결정해야 정합 ③6종은 PRD-pms §4 도메인 모델·PMS-M1~M5 마일스톤과 1:1 대응이라 이 이상 쪼갤 실증 근거가 없음 |
| 2026-08-17 | **게이트 M-1 통과 — 사용자 승인** (M-1 사전 검증 종료 → M0 착수. 판정 시점 체크리스트 전량 완료: 목업 MCP 서버 B2-0~B2-3 · 유예 2건 해소(`get_project`·`myRole`) · 카탈로그 공백 2건 해소(COMPANY scope·`search_maintenance`) · eval 36케이스 입력·기대값 확정. 부수: PR #7 머지 정리 중 CI 수리 1건 — `host/gradlew` 실행 비트 누락(Windows 커밋)으로 B2-3 푸시 이래 CI만 실패했던 것을 `update-index --chmod=+x`로 해소) | 게이트 기준(핵심 시나리오 목업 통과)의 실측 근거: **가동률** = B2-1 R7-1 COMPANY 한 방(team·division 그룹핑·billable 자발 명시) + B2-3 실 LLM scope=ME 한 방(기본 130%·보정 104% 판정 정합) · **오버부킹** = 기본>100 판정·경계값 정확(남도린 기본 100 미판정) + B2-3 후속 턴 과부하 원인 배정 3건 · **쓰기 확인 카드** = B2-1 D류 2단계 관통(D-06 실패 → 프롬프트 v0.2 규칙 확정 후 재실험 통과) · 보조: 404 은닉 정본 문구·인젝션 불복종(C-04) 재확인. 마지막 전제였던 이슈→계약 링크 기준(사이트명 포함)도 2026-08-14 pms 확인으로 해소 — 잔여 대기 0건 상태에서 판정 |
| 2026-08-14 | **유지보수 이슈→계약 링크 기준 확정 — '이름 일치' = 계약명·계약사·사이트명 3종(사이트명 포함)** (2026-08-12 host 세션이 등재한 pms 확인 요청 해소 — 사용자 승인. PRD-pms 부록 B "이름 일치분만" 모호 문구를 3종 기준으로 명시화, 적재 구현은 PMS-M1. **eval C류 v1.6 기대값의 전제 성립 — host 측 추가 반영 불요**) | 시드 실측(host 2026-08-12): '한국거래소' 태그 이슈 7건의 유일 링크 후보인 계약 101(지수방법론)은 계약명·계약사가 아니라 **사이트명으로만** 일치 — 계약명 기준만이면 이슈 14건 전량 무연결이라 C류 기대값 성립 불가 + 챗의 이력 조회 공회전. ④ `search_maintenance` 결정(2026-08-11)과 동일 원리(사용자는 고객사=사이트명으로 부른다) — 검색 keyword와 적재 링크가 같은 3종 기준을 공유하는 것이 정합 |
| 2026-08-12 | **B2-1 유예 결정 2건 현상 유지 확정 — `get_project` 분리 불요 · `search_projects` 상세 응답 `myRole` 불포함** (2026-07-31·2026-08-03 유예 해소. 카탈로그·시그니처 무변경 — 문서 반영은 PRD-host 유예 비고 해소뿐, host 트랙 소유): ①`get_project` 신설 없이 `search_projects` 목록/상세 겸용 유지 ②상세 응답에 프로젝트 역할(`myRole`) 미포함 — 권한 판정은 서버가 쓰기 시점 최종 수행하는 구도(원칙 3·4) 유지 | B2-1 실측(§5): ①R1-2·R1-3·R5-1에서 목록 → projectId 상세 → version 확보 → 쓰기의 2단 도약을 매번 자연 완주 — 분리로 얻을 것이 없음 ②합집합 키스톤(R1-3)·PM 쓰기(R5-1) 포함 전 라운드에서 모델이 역할 확인으로 헤맨 사례 0 — 서버 판정 신뢰 구도가 그대로 작동. 둘 다 유예 시점에 예정한 해소 경로(실험 근거 먼저) 그대로 — 카탈로그 변경(four-in-one 재작업) 없이 종결 |
| 2026-08-11 | **가동률 전사 scope 공백 해소 — `get_utilization` scope에 `COMPANY` 추가 + 응답에 조직 정보 동봉** (B2-1 판단 ③ 확정 — 카탈로그 변경, ~~MCP 담당 확인 대기~~ → **확인 완료(2026-08-12) — 양측 합의 성립**. host 반영분 = PRD-host FR-AI-11·도구 description·eval A-01 확장·목업 코드로 host 트랙 소유. pms 반영분 = PRD-pms v2.7 C1-6·§7): ①scope 열거 = ME/MY_TEAM/DIVISION/**COMPANY**/PERSON — `orgUnitId` 파라미터화(B안)·열거+id 하이브리드(C안) 미채택 ②`UtilizationEntry`에 **team·division 추가**(웹 `GET /api/utilization` 응답도 동일) ③전사 가시성 없는 호출자의 `COMPANY` = **404 은닉**(MY_TEAM·DIVISION 현행과 동일 — 자기 범위 축소 미채택) ④타부문 지정·중간 노드 집계는 미해결 존치 — 열거 확장은 하위 호환이라 고통 확인 시 `ORG_UNIT` 증설 가능 | ①**기능 추가가 아니라 정의 미반영**: 상위 PRD §3 "가동률 집계 대상"·PRD-pms C1-5가 이미 "팀·부문·**전사** 집계"를 정의하고 있었다 — 카탈로그만 뒤처진 상태 ②웹은 이미 `orgUnitId`(root=전사)로 지원 → "챗=화면 동일 판정" 원칙의 의도치 않은 위반 ③현 열거는 부문·팀 2단 고정 시절 어휘 — 결정 ⑧(2026-08-09 임의 깊이 트리)에 웹만 따라갔다(teamId→orgUnitId) ④B2-1 R3-1 실측: 모델이 `scope=COMPANY`를 **지어내 호출**(422 교정) — 자연 발화와 모델 기대가 같은 공백을 지시. 오용(F5) 없이 한계 정직 안내라 안전성은 통과, 완결성만 결손. ADMIN의 DIVISION 무의미(모집단 공집합 → `[]`)도 같은 실험에서 실증 ⑤**조직 정보 동봉 근거**: R3-1 발화가 "부문별로"인데 `UtilizationEntry`에 소속이 없어 COMPANY만 추가해선 미완결(44명 평면 목록 → 부문 파악에 find_person 반복). B2-1 §5가 scope 공백으로만 분류했던 부분을 결정 단계에서 발견 ⑥**B안 미채택**: `orgUnitId`는 조직 id 확보 경로가 없어 판단 ④와 동형 문제를 유발하고 도구 신설 압력을 만든다 — 관찰된 고통만 메우는 것이 "하네스 부품은 고통 확인 후 추가" 원칙 ⑦**404 은닉**: 구조 원칙 3 + 기존 MY_TEAM/DIVISION 일관성. 축소안은 모델이 전사 결과로 오해하면 F5 |
| 2026-08-11 | **유지보수 id 확보 경로 공백 해소 — `search_maintenance` 도구 신설 (카탈로그 7종 → 8종)** (B2-1 판단 ④ 확정 — 카탈로그 변경, ~~MCP 담당 확인 대기~~ → **확인 완료(2026-08-12) — 양측 합의 성립**. 상위 PRD §6 "도구명·설명·시스템 프롬프트·Eval 한 세트" 적용 — host 반영분은 host 트랙 소유이며 eval **C-01 재실험 필요**. pms 반영분 = PRD-pms v2.7 D4-1·§7): ①`search_maintenance(keyword?, status?)` → 계약 id·계약명·계약사·상태·기간·**매칭 사이트** 반환 ②keyword 매칭 범위 = **계약명·계약사·사이트명(고객사)** 부분 일치 ③가시성 판정·404 은닉 **없음**(D4-3 전사 공개 계승) ④웹 `GET /api/maintenance/contracts`(D4-1)에도 **동일 keyword 검색 추가** ⑤ⓐ(description 보강 단독)·ⓑ(프로젝트 상세에 계약 id 동봉)·ⓒ-2(기존 도구 확장) 미채택 | ①**ⓑ는 시드 실측 커버 0건**: `reference/seed/maintenance.json` 계약 **105건 전부 `sourceProjectId=null`**(시트 실데이터라 이관 이력 없음) — ⓑ가 커버하는 이관 생성 계약은 PMS-M4에나 존재한다. B2-1 §5의 "부분해" 평가를 실측으로 **현시점 무용**으로 정정 ②**사이트명 키 필수(결정 단계 신규 발견)**: 가온아이 1계약에 고객사 사이트 45개 — 사용자는 "가천대길병원"으로 부르는데 그 문자열은 계약명·계약사에 없고 사이트명에만 있다. B2-1 R1-7이 이를 못 잡은 이유는 목업 계약명("한국수출입은행 규정관리 유지보수")과 발화가 우연히 일치했기 때문 ③웹 D4-1이 이미 계약 검색을 보유 → 판단 ③과 동형의 카탈로그 지연. 웹 필터에도 사이트명이 없어 같은 한계를 공유하므로 동시 해소("챗=화면 동일 판정") ④`find_person`("id를 모를 때 사용")과 동일한 **id 확보 전용 도구 패턴** 답습 — 사람·프로젝트에는 있고 유지보수만 없던 비대칭 해소 ⑤D4-3(전사·404 은닉 없음)이라 가시성 로직이 불필요 → 신설 비용·검증 위험 최소 ⑥**ⓒ-2 미채택**: 반환 엔티티가 계약 목록 ↔ 이슈 목록으로 상이해 한 도구 두 모드는 description 복잡·모델 혼동을 유발한다. `search_projects`의 목록/상세 겸용은 **동일 엔티티**라 성립한 것이므로 선례가 아니다 ⑦8종화 위험 낮음 — B2-1에서 7종 도구 선택 실패 0건, R1-7도 오입력 없이 멈춰 경로만 주면 되는 상태 |
| 2026-08-10 | **가동률 의미 재정의 — 배정 M/M = 실투입 계획** (상위 PRD §3 재정의·PRD-pms v2.6. ~~MCP 담당 확인 대기~~ → **확인 완료(2026-08-10 동석 리뷰)** — 카탈로그·`get_utilization` 시그니처 무변경, 의미 변화: `overbooked` = **기본**>100(구 보정>100 대체) · 보정 산식 ÷coeff→**×coeff**(단가 가중 보조 지표, 과부하 판정 미사용·명칭 유지) · 가동률 eval 기대값(§8 유예분)의 전제): ①배정 M/M = **실투입 계획**(1.0=해당 월 풀타임), 계약 배분 숫자 금지 — 계약 관점은 프로젝트 `contractMm`에만(B1-5 UI 레이블 명시) ②과부하 판정·팀/부문/전사 집계 정본 = 기본 가동률 ③**PM 역할 하드 제외 미채택** — PM 자동 배정 기본 M/M=0(A6-7)+실투입 의미로 "관리형 PM 부하 미표시" 충족, 하드 제외는 실무형 PM 과부하 사각+부하 은닉 우회 ④시드 확정 2건: 월별 M/M 부여 규칙(실무자=PM 외 참여자·없으면 PM 본인, contractMm 안분 근사 — 2026-08 기준 오버부킹 3명(남민준 190%·손윤린 182%·전세아 133%) 시뮬레이션 검증) · billable=false = 프로텐·AX사업기획부·관리•마케팅부 3부문 10명(부록 B) | pms 담당 실무 검토: 계약 M/M 배분(예: 차장 2/사원 1)이 실투입(체크만 vs 전담 개발)과 괴리 — 계약 숫자를 배정 M/M에 넣으면 과부하 경보 오발·여유 인력 오판으로 가동률의 존재 이유("누가 여유 있나") 전복. 구 ÷coeff 보정은 배정 M/M이 단가 기준일 때만 성립하는 환산이라 실투입 의미에선 "시간 100% 찬 수석이 67%"로 왜곡. gradeCoeff·US-E4·기본/보정 토글은 ×coeff 재정의로 폐기 수술 없이 보존. 시드 실측: 진행중 34건 중 1인 프로젝트 다수(PM=실무자)라 PM 일률 제외 불가 |
| 2026-08-09 | **게이트 P 리뷰 기획 결정 8건 일괄 채택 — 프로토타입 동작대로** (게이트 P 승인과 같은 자리(두 담당 동석 리뷰)에서 결정됐으나 게이트 기록에 누락 → 소급 등재. ④⑦ 공용 변경 포함 MCP 합의는 리뷰 동석으로 성립. 상위 PRD §4 재작성·PRD-pms v2.5 반영): ①**웹 진척률 2단계 완화** — 웹 UI는 100% 저장만 확인 모달, 그 외 값은 1클릭 저장(confirmed=true 단건 호출). 서비스 프로토콜·MCP `update_progress` 2단계는 무변경 — 2026-08-02 "웹=MCP 동일 2단계" 결정의 부분 변경(동일 서비스는 유지, UI 확인 강도만 분리) ②권한 조정 감사 단위 = 저장 1건(A8-2 현행과 정합 — 확인만, 변경 없음) ③engagement 한국어 명칭 확정 = 원격·상주·부분상주 ④**시스템 관리자 계정** — admin@proten.co.kr, 관리자 그룹 소속의 회사 고유 계정, 삭제·수정 불가, 인력·가동률·배정 목록 제외. 대표 개인의 관리자 그룹 소속은 유지(수습 주체) ⑤**팀·직급 관리 채택**(US-E3·E4 신설 — 2026-08-06 "설정 편집 탭 승격 안 함" 결정 대체) ⑥**OFFSITE 폐지** — engagement 3종으로 축소, 시드 32건은 적재 시 원격으로 흡수 ⑦**orgRole → 편집 가능한 권한 그룹 일반화**(2026-08-03 "orgRole 기능 플래그 미채택"·PRD-pms Out of Scope "orgRole 커스텀 추가/편집" **대체**) — 그룹 = 가시성 scope 4단(전사/부문/팀/본인) + **프로젝트 밖** 기능 플래그 4종(프로젝트 생성·계약 관리·전 프로젝트 관리=PM 간주·사용자/조직/권한 관리). 그룹 신설/수정/삭제(소속 인원 있으면 삭제 거절)·**관리자 그룹은 시스템 고정**(자기 잠금 방지)·판정·가시성·404 은닉이 그룹 정의를 따름. 시드 orgRole 4종은 기본 그룹 4종으로 적재. **host 접점**: 도구 카탈로그·시그니처 변경 없음, `whoami` 응답의 orgRole → 권한 그룹명 — PRD-host·eval 반영은 host 트랙 소유 ⑧**조직 임의 깊이 트리** — 회사(root)→부문→팀→임의 깊이(Division·Team 2단 고정 해제, identity 모델 변경), 인원·프로젝트·하위 있으면 노드 삭제 거절, **팀 가시성 = 하위 조직 subtree 포함** | 화면 프로토타입 3라운드 리뷰에서 실사용 검증으로 도출(세부 경위는 PROGRESS-pms 2026-08-09 세션 로그 1~3차). ①100% 외 값까지 2단계는 마찰 과다 — 100%만 완료 유도 접점이라 확인 가치가 있음 ④감사 actor·수습 주체를 개인(대표) 퇴사와 절연 ⑦고정 4단 orgRole은 조직별 권한 편차를 못 담음 — 프로젝트별 권한 커스텀(2026-08-06)과 같은 원리(기본값+조정)를 조직 축에 적용 ⑧조직 개편·파트 신설을 도메인 변경 없이 수용. 기록 누락은 pms 세션(2026-08-09)에서 발견, 사용자 확인(전부 채택·합의 성립) 후 등재 |
| 2026-08-09 | **게이트 P 통과 — 사용자 승인** (문서 5종: 상위 PRD · PRD-host v2.1 · PRD-pms v2.4 · 기술_선택_근거 v2 · 구현_노트 v1.0 + 유저_시나리오 v1.1 + eval-cases v1.1 — 2026-08-06 개정분 포함 일괄. 미해결의 "ADMIN(대표) 가시성 = 전사"도 상위 PRD §4 포함분으로 함께 확정). P단계 종료 → M-1 착수 | 게이트 규칙(ROADMAP): 통과는 사람이 승인하고 결정 기록에 남긴다. 작성물 전량 완료 + 공용 변경 4건 양측 합의 성립 + 화면 프로토타입(2026-08-09 머지) 리뷰 보조 상태에서 승인 |
| 2026-08-06 | **공용 변경 4건 MCP 담당 확인 완료** (완료/재개 전이 재설계 · billable 모집단 · 프로젝트별 권한 커스텀 · 유지보수 재설계 — 양측 합의 성립, 공용 문서 변경 확정). host 관점 영향 확정: ①`update_progress` 거동 변화 수용 — 100% 저장 시 전이 없음(completable 안내)·완료 상태 `409 PROJECT_COMPLETED`(eval D-08·SC-23 선반영분 유효) ②`get_utilization` 팀·부문·전사 집계 모집단 = billable ③참여자 진척률 off 프로젝트에서 `update_progress` 403 — 챗은 거절 전달 ④`list_maintenance_logs`는 projectId 단순화 불가(계약/이슈 id — M-1 카탈로그 공백 항목의 도메인 입력 확정) · 구 `/maintenances/{id}/logs` → 이슈·코멘트 API 대체 · 프로젝트 목록 `?phase=` 파라미터 추가. 도구 카탈로그(7종)·시그니처 변경 없음 — host 측 문서(PRD-host·eval) 반영은 host 트랙 소유 | 게이트 P 승인의 전제(공용 변경은 양측 합의 — 협업 경계 규칙). 두 담당 동석 리뷰로 확인, pms 담당 전달(2026-08-06) |
| 2026-08-06 | **유지보수 재설계 — 계약/사이트/이슈 3층 + phase 탭** (PRD-pms v2.4 — §4 maintenance 모델·EPIC D 재작성(US-D1~D4)·§7 API 재편·부록 A/B. 상위 PRD §4-2 표 "이관/이력"→"이관"·§4-3 계약 등록 행 추가. **MCP 담당 확인 대기** — 접점: ①구 미해결 "프로젝트:Maintenance 1:1 vs 1:N" 답 = 프로젝트:계약 **1:1**(이관 경로)이나 **프로젝트 없는 계약(OEM 직접 등록) 존재 → `list_maintenance_logs`의 projectId 단순화 불가**, 계약/이슈 id 유지 ②구 `/maintenances/{id}/logs` → 이슈·코멘트 API로 대체 ③프로젝트 목록 `?phase=` 파라미터+파생 필드 추가). ①`MaintenanceContract` — 연 단위 계약, sourceProjectId nullable, 이관+직접 등록 입구 2개 ②`MaintenanceSite` — 계약:사이트 1:N, **담당 엔지니어 정본 = 사이트**(이슈 기본 배정 원천) ③`MaintenanceIssue` — 구 게시판 대체, type{장애,문의,요청}·상태{접수,처리중,고객확인대기,완료}·append-only 코멘트(구 MaintenanceLog 불변식 계승) ④탭은 category 컬럼 없이 **phase=status 파생**(영업={계약대기,수주확정}·솔루션={진행중,완료}), 유지보수 탭=계약 목록 ⑤권한: 계약 등록·수정=orgRole(프로젝트 생성 동일 열거)·이슈=전사·이관=PM 유지 ⑥미채택: sales/SalesInfo·정기점검 모델링(계약 정보 필드만)·만료 임박 알림 ⑦시드=2026 유지보수 시트 실데이터(마스킹 없음) | pms 담당 실무 검토(시트+구 이슈 게시판 실측): 유지보수 계약은 연 단위이고 계약:고객사 1:N(가온아이 1계약 ~45사이트), OEM 채널 계약은 원천 프로젝트 부재 — 완료 프로젝트 1:1 파생 모델로는 등록 자체 불가. 45건 분해는 계약금액·기간·갱신 정보 45배 복제라 기각. "누가 뭘 담당하는지 안 보임" 고통의 정본을 사이트 engineerId로 구조화(실측: 열린 이슈 23건 중 할당 1건 — 사실상 무배정 운영). 열림/닫힘 2상태는 미배정·처리중·대기 구분 불가. category 컬럼은 status와 원본 이중화라 파생값으로. 정기점검·만료 알림 미채택은 "특이사항 없으면 기록 안 함" 실무 + 고통 확인 후 추가 원칙 |
| 2026-08-06 | **프로젝트별 권한 커스텀 — §4-2 표를 기본값으로 전환** (상위 PRD §4-1 판정식·§4-2 확장, PRD-pms v2.3 — US-A8·`ProjectPermissionOverride`·`GET/PUT /projects/{id}/permissions`·부록 A 권한 패널. **MCP 담당 확인 대기** — 카탈로그·`update_progress` 시그니처 변경 없음, 거동 변화 1건: 참여자 진척률을 끈 프로젝트에서 `update_progress` 403 → 챗은 거절 전달. **기본값 무변경이라 기존 시나리오·eval 기대값 영향 없음**). ①전역 권한 관리 화면은 두지 않고 **프로젝트 설정에서 PM이 역할별 기능 토글**로 조정 ②조정 가능 = PL·참여자 × {정보 수정·배정/M/M·진척률·완료 처리·재개} 양방향 ③고정 = PM 열 전체·조회 행·삭제/이관 행 ④완료·재개는 한 토글 ⑤역할 신설(+권한)은 미채택 — 3단 고정 ⑥override만 저장(행 부재=기본값)·`Project.version` 공용·감사 로그·기본값 복원 | pms 담당 실무 검토(PRD 리뷰): 고정 표가 모든 프로젝트에 맞지 않음 — 다부문 프로젝트는 PL에 배정 위임이, 민감 프로젝트는 참여자 진척률 잠금이 필요. 프로젝트 역할은 "프로젝트마다 개별 판정"(§4 원칙)이므로 관리 UI도 프로젝트 스코프가 정합 — 전역 탭은 판정 단위와 어긋난다. PM 열·조회·삭제/이관 고정은 자기 잠금 방지 + ADMIN 수습 보장(§4-1 치환) + 비가역 안전장치(완료 재설계 근거) 보존. 완료·재개 묶음은 "사고 낸 사람이 수습한다" 대칭 유지 |
| 2026-08-06 | **완료 전이 재설계 — 100% 자동 전이 폐지, 명시적 완료 처리·재개** (PRD-pms v2.2 — §5·A2-3·A2-8·US-A7·US-F3 신설, 상위 PRD §4-2 표 확장. **MCP 담당 확인 대기** — `update_progress` 시그니처·카탈로그 변경은 없으나 거동 변화: 100% 저장 시 전이 없음(완료 처리 안내)·완료 상태 수정 `409 PROJECT_COMPLETED`. eval D-08·SC-23 기대값 pms 세션에서 선반영). ①완료 처리 `POST /complete`(progress=100 전제)·재개 `POST /reopen`(완료→진행중, progress=90 리셋, 사유 없음 — AuditLog가 담당) 모두 **배정 전원**(PM·PL·참여자) ②완료 상태 진척률 직접 수정 거절 — 재개 후 수정 ③유지보수중 재개 불가 ④100%인 채 7일 방치 시 PM·PL 리마인드, 재개 시 미읽음 회수(F3) ⑤완료·재개는 MCP 도구 미노출(원칙 5) | 자동 전이는 참여자의 진척률 쓰기(A2)가 상태 전이 권한(A5-3이 참여자 403)을 우회하는 부수효과 — 생애주기에서 가장 되돌리기 어려운 전이가 뒷문으로 열려 있었다. 실무에서 100%≠완료(검수·납품 잔여)이고, 완료를 PM에 묶으면 시드 PM 편중(1인 128건)에서 병목. 재개를 완료와 같은 권한으로 묶는 근거: 사고 낼 수 있는 사람이 수습도 해야 한다(비대칭이면 참여자 오조작마다 보고 체인 발생). 진짜 비가역(이관)은 PM 전용 유지로 안전장치 보존 |
| 2026-08-06 | **가동률 집계 모집단 = `Person.billable`** (상위 PRD §3 신설 — 공용 문서 변경, **MCP 담당 확인 대기**. PRD-pms: 도메인 필드·C1-5·부록 B 적재 규칙. 구 미해결 "HQ_TEAM 제외 여부"를 일반화해 해소). 팀·부문·전사 집계와 `overbooked` 목록의 모집단은 billable=true만, 개인 지정 조회는 무관. false 지정 팀 목록은 시드 적재 정책과 함께(PMS-M1 전) | 시드 실측: 진행중 프로젝트 배정 인원 **18/44명** — 대표 1명만 빼도 영업·기획마케팅·경영관리 등 지원 인력의 0%가 전사·부문 평균을 계속 왜곡한다. "대표 제외"가 아니라 모집단 정의가 진짜 문제. `get_utilization` 전사/부문 scope(M-1 실험)가 "무엇의 평균인가"를 답할 전제이기도 하다 |
| 2026-08-06 | **PL 복수 허용 — API 제약 해제** (상위 PRD §4-2 확정, PRD-pms A1-7 삭제·A6-7 `MULTIPLE_PL` 삭제·에러 표 정리) | 실무 확인(pms 담당): 다부문 프로젝트(4개 팀 걸침)에서 파트별 리드가 실제 존재. 스키마는 기수용이라 마이그레이션·접점 변경 없음 — 유예 결정(2026-08-03)이 예정한 해제 경로 그대로 |
| 2026-08-03 | **eval 셋 30 → 36케이스 확장** (P단계 ⑤ 이관 — `docs/evals/eval-cases.md`. eval은 host 재량이나 유저_시나리오 §9가 "비중 조정 vs 30 초과 확장"을 이관 시 결정으로 위임했고 ROADMAP 문구("분류·비중 유지")를 구체화하므로 기록). 구 30의 분류·비중은 감축 없이 유지하고 덧붙임: **쓰기 확인 3→8**(SC-11 참여자 쓰기 · SC-21 대상 모호 · SC-22 취소 · SC-23 범위 밖 값/완료 전이 2건 + 구 D-03 일괄 승계) · **오류/점검(FR-AI-26) 1 신설**. 인젝션(SC-24)·자격증명(SC-25)은 구 C-04·G-03 자리 승계라 증설 없음. G1 합격선은 비율 유지(≥90% = 33/36) | "분류·비중 유지"를 지키는 방법은 감축 재배분이 아니라 추가 — 30을 고수하면 성공 경로(의도 해석 ≥90%의 분모)를 깎아 실패·오남용 5종을 욱여넣어야 한다. 추가 6건의 회귀 비용은 무시 수준(프롬프트 1자 변경 시 전체 재실행이 이미 규칙)이고, 쓰기 실패 경로는 전부 F3(1건 = 출시 차단) 등급이라 검증 밀도를 높일 가치가 가장 큰 곳 |
| 2026-08-03 | **권한 모델 확정분 MCP 담당 확인 완료** (양측 합의 성립 — 공용 문서 변경 확정). host 관점 영향: 도구 카탈로그·`update_progress` 2단계 확인 프로토콜 **변경 없음**(참여자 진척률 수정은 기존 US-A2 동작 유지). **`whoami`는 유효 권한 미반영**(현행 유지 — id·이름·팀·부문·orgRole만 반환). 프로젝트별 역할 노출(`search_projects` 상세 응답 `myRole`)은 **M-1 목업 실험 후 결정**으로 유예 | 프로젝트 역할은 프로젝트마다 개별 판정이라 전역 응답인 `whoami`에 유효 권한을 실을 수 없음(전역으로 의미 있는 orgRole은 이미 반환). 권한 판정은 서버가 쓰기 시점에 최종 수행(원칙 3·4) — LLM이 권한을 몰라 생기는 최악은 거절 응답 전달이지 우회가 아님. 응답 스키마 변경은 협업 접점(four-in-one 재작업)이라 `get_project` 분리와 동일 원칙(실험 근거 먼저)으로 유예 |
| 2026-08-03 | **권한 모델 확정 — 합집합 판정 + 프로젝트 역할 3단** (상위 `PRD.md` §4 재작성). ①**판정은 합집합**: `canDo = orgPerm(orgRole) OR projectPerm(프로젝트 역할)` — orgRole을 선행 게이트로 쓰지 않고, 프로젝트 역할이 위로만 덮어쓴다(축소 없음) ②**프로젝트 역할 = PM / PL / 참여자 3단**(구 "관리자/담당자" 대체 — orgRole ADMIN과 명칭 충돌 해소). PM 1명 필수 · PL 복수 가능 · 프로젝트마다 개별 판정 ③권한 배분: 정보 수정 PM·PL / 배정·M/M 입력 PM만 / 진척률 PM·PL·참여자 모두(`Project.progress`가 단일 값이라 "본인 몫" 부분 수정 개념 없음 — 기존 US-A2·`update_progress` 동작 유지) / 삭제 PM만 ④**orgRole은 프로젝트 밖 행위만** 담당 — 프로젝트 생성(TEAM_LEAD·DIVISION_HEAD·ADMIN 열거, 서열 미정의) · 조직/사용자/직급 관리(ADMIN). 프로토타입 기능 플래그 5종 미채택, **부문장 `editProgress:false` 폐기** ⑤가시성은 프로젝트 역할이 확장하되 **프로젝트 컨텍스트 내로 한정**(타 팀 인원의 프로젝트 밖 데이터는 조직 가시성 규칙 유지) ⑥PL 복수는 스키마로 수용하고 API 제약으로 유예 ⑦시드 적재: `managerId`→PM, 나머지 `assigneeIds`→참여자, **PL은 미지정**(시드에 근거 없음) ⑧ADMIN은 전 프로젝트에서 PM과 동일 권한(PM 부재·퇴사 수습) ⑨enum 값은 `{PM, PL, PARTICIPANT}` — orgRole `MEMBER`와 이름 충돌 회피 | 시드 실측: **382건 중 91건(24%)이 PM의 orgRole=MEMBER** — orgRole 선행 게이트면 24%가 막힌다. `managerId`가 `assigneeIds`에 305/305 포함 → PM도 참여자, 배정 테이블의 역할 컬럼 하나로 표현 가능. PM 편중(1인 128건·55건·36건)은 PM이 총괄/영업 성격이고 실무 리드가 별도임을 시사 → PL 계층 신설 근거. 프로토타입 `canEdit`(orgRole `editProgress` 선행) ↔ `canManage`(`pmId` 선행) 비대칭은 같은 원인의 실제 결함(`frontend/src/store.jsx:175-188` — 프로토타입에 PL 개념은 없음). 복수 PL은 나중에 필요해질 때의 비용(스키마 마이그레이션+API+MCP 도구 응답=협업 접점 변경)이 지금 열어두는 비용(조인 1회)보다 크게 비대칭 |
| 2026-08-02 | **프로토타입 대조 검토 반영** (`frontend/` = 구 백엔드 실연동본, 사용자 승인): ①웹 진행률도 MCP와 동일 2단계 confirmed 프로토콜(`PUT /progress`) ②내 계정(프로필·비밀번호·알림 설정, EPIC H)·사용자 CRUD(US-E2)·감사 조회 API(G1-3) 채택 ③**도구 카탈로그에 `whoami` 추가**(6→7종, FR-AI-16 — `GET /api/me` 동일 서비스) ④chat BFF 접점(`/api/chat`·`/feedback`) 명세 ⑤SSE `?access_token=` 인증 명시 ⑥화면 3종(홈·인력·설정) 부록 A 추가 ⑦email 로그인 + 초기 비밀번호 `proten1!` 확정 ⑧**배정은 기간 모델 유지**(프로토타입 월별 upsert는 재연동 시 조정) ⑨상태 전이 서버 강제 AC(US-A5) ⑩유지보수 경로 `/api/maintenances/{id}/logs` 통일. **권한 모델 재기술(orgRole 기능 플래그·managerId·HQ 집계 제외)은 pms 담당 결정 보류** | 프로토타입은 실연동으로 검증된 동작 — 명세로 승격하는 쪽이 재작업 최소. whoami는 카탈로그 변경(협업 접점)이라 본 기록 경유 |
| 2026-08-02 | **PRD 3문서 체계**: 상위 `docs/PRD.md`(공유 정의 — 용어·권한 모델·가동률 산식·대상 사용자·시드 기준·협업 접점. **공용 소유**, 변경은 결정 기록 경유) + `docs/PRD-host.md`(AI 어시스턴트, host 트랙 소유) + `docs/PRD-pms.md`(PMS 구현명세, pms 트랙 소유 — 구 "PMS — AI 구현용 PRD" v1.0 현행화 이관, 원본 전사 `reference/PMS_구현용_PRD_v1.0.md`, 충돌 7건 수정은 ROADMAP P단계 항목에 명세). 겹치는 정의는 상위가 유일 원본, 하위는 참조만 | 두 하위 문서는 독자·변경 주기가 다르고(도구/Eval vs 도메인/AC) 소유 경계가 트랙으로 분리됨. 겹치는 정의의 원본 방향 문제(host↔pms)는 상위 단일 원본으로 해소(당초 "원본=PRD-pms" 등재를 같은 날 정정). PDF 원본은 세션 밖에서 소실되므로 전사 보존 |
| 2026-08-02 | **대상 사용자 정정**: 솔루션사업부 40명(구 문서 전제) → **프로텐 전 직원**(시드 44명·7부문). 조직 단위는 부문(division), orgRole = ADMIN/DIVISION_HEAD/TEAM_LEAD/MEMBER | 사용자 정정 + `reference/seed/people.json` 근거. 구 문서 기반 작업 시 잔재 주의 |
| 2026-08-02 | **2인 분업 전환**: MCP 담당 = host 앱 + pms 내 `/mcp` 어댑터, PMS 담당 = 도메인·서비스·프론트. 모노리포 유지, 하네스는 디렉터리 스코프로 분리(host/·pms/ CLAUDE.md, verify 스코프 인자, PROGRESS 트랙 분리) | 원칙 2(임베디드 `/mcp`) 때문에 MCP 담당도 PMS 코드베이스 안에서 작업 — 리포 분리 시 어댑터 소유권·계약 문서 공유가 애매. 두 개발자 모두 Claude Code 사용. 협업 경계 = MCP 도구 카탈로그 + 애플리케이션 서비스 API, 변경은 이 결정 기록 경유 |
| 2026-08-02 | **구 conventions 3종 이관** (`docs/conventions/` — java-spring·react-ts·git-workflow). v3 정합화: ralph·`/verify`·boundary-reviewer·단일 PROGRESS 참조를 현행(verify.sh 스코프·reviewer·트랙 파일)으로 수정, CI 전제는 "CI 도입 전 verify 결과 첨부"로. 전량 주입 대신 스코프 CLAUDE.md에서 참조. Modulith 모듈 목록은 구 설계 승계 표기 — M0 스캐폴드에서 확정 | 2인 협업 + 원격 개설로 git 워크플로 규칙의 고통 시점 도래(증설 예약 발동). 코드 컨벤션은 M0 전 동료 온보딩 자료로 함께 이관 |
| 2026-07-31 | **새 폴더(pms_mcp_v3)에서 완전 재시작.** 기존 pms_mcp는 읽기 전용 참고 | 구 저장소의 중복 문서 부채(가이드 고유 가치 ~40%)·2인용 ruleset과 절연. 결정과 재사용 자산만 승계 |
| 2026-07-31 | 스택: **Boot 4.1 + Modulith 2.1** + Spring AI 2.0.0 | Boot 4.0은 2026-12 OSS EOL. Spring AI 2.0.0이 양쪽 지원, 신규 시작이라 상향 비용 없음 |
| 2026-07-31 | **MCP stateless 지향 설계** (구조 원칙 7) — 구현은 2025-11-25 스펙, 세션·Sampling·Roots·Logging 비의존 | 2026-07-28 스펙 대개정 출시 확인(stateless화, 12개월 유예). Java SDK 신 스펙 지원은 모니터링 항목 |
| 2026-07-31 | PRD v2.0에 저비용 3종: `scope=ME` · 조회 도구 version 반환 · 도구 오류/점검 응답 규칙 FR | version 미반환은 쓰기 시나리오가 도구 카탈로그만으로 완결되지 않는 결함. 공백 4건(투입 조회·유지보수 만료/SR·기간 범위·스킬/지연)은 후순위 후보 |
| 2026-07-31 | 개발 기준 모델 **claude-sonnet-5** (G3에서 단가·학습 미사용 조항과 재확인) | 비용·지연·Eval이 모델에 묶여 있어 기준 모델 없이는 수치 검증 불가 |
| 2026-07-31 | `get_project` 분리 — **M-1 목업 실험 후 결정** | 도구 카탈로그 변경은 four-in-one 재작업. 실험 근거 확보가 먼저 |
| 2026-07-31 | LLM **학습 미사용 조항 확인 = M0 게이트 전제** (사람 작업) | 법무·구매 리드타임 — 실패 시 대안(국내 리전·온프렘) 검토 시간 확보 |
| (승계 07-13) | 재사용 자산은 시드·프론트만, 백엔드는 재작성 | 데이터·UI는 코드 품질과 무관한 자산 |
| (승계 07-18) | 지시 파일 영어, 기록물 한국어 | LLM 지시문은 영어가 안정적. 기록물은 사람용 |
| (승계, v2 학습) | 하네스 부품은 고통 확인 후 추가 — 빈 섹션·선제 부품 금지. ralph·conventions·CI는 증설 예약 목록에 | 완성본 선이식이 이해도 저하와 유지 부채의 원인 (todo-cli 샌드박스 학습 결론) |

## 미해결 이슈

- **`update_progress`가 카탈로그에 열려 있다 — G1 이월 조건의 미구현분 (MCP 담당)**: 2026-08-26 G1 통과는 "`update_progress`는 G2 전까지 사내 공개 대상이 아니다"를 조건으로 단다(결정 기록). 그런데 도구는 2026-08-23부터 `/mcp` 카탈로그에 실연결돼 있어 **지금 사내 공개하면 쓰기 도구가 같이 열린다**. **사내 공개 착수 전에 카탈로그에서 제외하는 스위치가 필요하다**(구조 원칙 5의 방어가 프롬프트 준수에만 걸려 있는 상태이고, F3는 v0.3 3회차 중 2회차에 재현됐다 — 즉 열어 두면 확인 없는 쓰기가 실제로 난다). 사내 공개 일정이 잡히면 그때가 착수 시점이다
- **`/mcp` 오류 응답이 Java 스택 트레이스 전문을 싣는다 (MCP 담당 — G1을 막지 않음)**: 2026-08-26 실측 — `initialize` 없이 `tools/list`를 치면 400과 함께 `stackTrace` 배열이 `io.modelcontextprotocol`·Spring Security 필터 체인·Tomcat 내부까지 그대로 응답 본문에 나온다. 거절 자체는 정상이나(세션 미수립) **내부 구조가 인증 실패 경로에서 노출되는 것은 별개 문제**다. `GlobalExceptionHandler`가 아니라 MCP 전송 계층이 만드는 응답이라 어댑터 밖이고, 사내망 전제라 지금 위험은 낮다. 사내 공개 전 정리 대상
- ~~**읽기 전용 도구 6종이 `destructiveHint=true`로 광고된다**~~ — **2026-08-25 해소**(MCP 담당, 결정 기록): 어댑터가 `annotations`를 선언해 조회 6도구+`whoami` **7종**(6이 아니라 7이었다 — `whoami`가 빠져 있었다)에 `readOnlyHint=true`·`destructiveHint=false`·`openWorldHint=false`를, `update_progress`에는 `destructiveHint=true`·`idempotentHint=false`를 박았다. 카탈로그 힌트를 도구별로 단정하는 통합 테스트로 고정했다(`McpAdapterIntegrationTest`). **채점층 세우기 전에 넣었다** — 도구 표면이 바뀌면 eval 전량 재실행이므로, 기준 회차를 돌린 뒤에 고치면 그 회차가 버려진다. 원래 문구는 아래에 남긴다: **읽기 전용 도구 6종이 `destructiveHint=true`로 광고된다 (MCP 담당 — G1 준비의 four-in-one 대상)**: `/mcp` 어댑터가 `@McpTool`에 annotations를 선언하지 않아 SDK 기본값이 붙는다 — 8종 **전부** `readOnlyHint=false`·`destructiveHint=true`다(2026-08-24 eval 러너가 카탈로그를 받아 오다 실측). 실제로 파괴적인 것은 `update_progress` 하나뿐이고 나머지 6종은 순수 조회다. 지금 모델 행동에 문제가 관측되지는 않았으나(쓰기 5관측에서 미확인 실행 0건) **힌트는 클라이언트가 확인 UX를 붙이는 근거**이므로 사실과 달라서는 안 된다. 카탈로그 표면이라 고치면 eval 전량 재실행 트리거 — 프롬프트·도구 문구와 **한 번에** 손대는 four-in-one에 등재한다
- **`scope=COMPANY`가 문서와 구현이 갈린다 (양측 합의 사항 — G1을 막지 않는다)**: PRD-host FR-AI-11은 "전사 가시성 없는 화자의 `COMPANY` = **404 은닉**"(2026-08-11 결정 ③, 목업 B2-1 §6에서 실측 통과)인데, 실 구현 `UtilizationPopulation.aggregateScope()`는 **조용히 화자의 가시 범위로 좁힌다**. 팀원이 "전사 가동률"을 물으면 자기 팀 인원만 오고 모델은 그것을 전사로 답할 수 있다 — `UtilizationScope.from()`이 모르는 낱말을 거절하는 이유("틀린 범위의 답을 맞는 답으로 받는다")와 같은 실패 모양이다. 반대 근거도 있다: 도구 description이 이미 "조회 가능한 범위는 서버가 판정한다"고 예고하고, 집계 질의에 404는 숨길 엔티티가 없어 어색하다. **G1 36케이스에는 없다**(확장 후보). 정하는 방향에 따라 고칠 곳이 갈린다 — 404로 가면 `resource`(PMS 담당), 현행 유지면 PRD-host FR-AI-11 문구(MCP 담당)
- **이슈를 계약 없이 찾는 경로가 카탈로그에 없다(MCP 담당 — 지금은 문제 아님)**: 계약에 붙지 않은 이슈(시드 14건 중 7건)에 도달하는 길은 **사용자가 이슈 번호를 직접 주는 것**뿐이다. eval C-04가 바로 그 경로이고(이슈 id 직접 제공형) 원본 시트·게시판 번호를 쓰던 실무와도 맞아 **G1을 막지 않는다**. 담당자·상태 축의 이슈 검색이 필요해지면 카탈로그 확장이라 양측 합의 사항이다
- ~~**가동률 계약에 조직 id가 없다 — 챗의 `scope=MY_TEAM`·`DIVISION`이 막힌다**~~ — **해소**(2026-08-23 도메인 루트 계약 승격에 포함 · 2026-08-24 MCP 담당 확인: `WorkforceProfile`에 `teamOrgUnitId`·`divisionOrgUnitId` 2종이 도착했다. 이제 화자로부터 조직 id를 유도해 `/api/utilization`의 `orgUnitId`로 넘길 수 있다). 원 기록:: `WorkforceProfile`이 `team`·`division`을 이름으로만 준다. 웹은 `?orgUnitId=`를 받으므로 영향이 없고 **EPIC C 착수도 막지 않는다** — MCP 경로만 막힌다. 제안·근거는 2026-08-23 결정 기록(#19 확인) 행에
- ~~**PR #20·#21이 main에 도달하지 않았다 (2026-08-23 실측)**~~ — **해소**(PR #24 `recover/m1-stack-to-main` 머지로 스택 복구. 2026-08-23 재확인: main에 projects 시드 로더·인원 정본·seed README 경고가 모두 있고 적재도 실동한다). 재발 방지분 `ci.yml`의 base 제한 완화는 host 트랙 하네스 소소분으로 남아 있다. 원 기록:: 두 PR의 base가 main이 아니라 앞 PR의 브랜치였다(#20 → `feat/m1-cross-module-contracts`, #21 → `feat/m1-projects-seed`). #19가 먼저 머지된 뒤라 #20의 머지 커밋은 이미 끝난 브랜치에 들어갔고, main에는 **projects 시드 382건·인원 정본 확정·`reference/seed/README.md` 함정 경고가 없다**(main의 README는 아직 ralph 다이어그램 오붙임 상태). 같은 이유로 **CI도 한 번도 돌지 않았다** — `ci.yml`이 `pull_request: branches: [main]`이라 base가 main이 아니면 발동하지 않는다(머지 조건 "CI green"이 조용히 비어 있었다). 복구: 두 브랜치가 origin에 살아 있고 #19가 squash가 아닌 머지 커밋이라, `feat/m1-projects-seed` → main PR을 새로 열면 diff가 정확히 #20 내용이다. 재발 방지로 `ci.yml`의 base 제한 완화 검토
- ~~**eval 코퍼스가 실 서버에 없는 인물을 가리킨다 — 재앵커 필요**~~ — **2026-08-23 해소**(host 트랙, 결정 기록): eval-cases **v1.7**·유저_시나리오 **v1.5** 전량 재앵커 + 앵커 정본 `docs/evals/seed-anchor-map.md`(+ 재현 SQL) 신설. 인물 참조 체계 질문도 실측으로 답했다 — `projects.json`·`maintenance.json`은 **id로만** 연결되고 그 id는 실 명부(`seed_org_proten.sql`) 기준으로 읽힌다. **PMS 담당에게 남은 것 = PRD-pms 부록 B 잔여 3곳**(아래 새 항목)
- ~~**PRD-pms 부록 B의 구 익명 이름 3곳 — PMS 담당 작업**~~ — **2026-08-24 해소**(PRD-pms v2.15). ①인원수·조직명·예외 전부 실측으로 교체(경영관리팀 4 + AX사업기획부 6 = false 10 + 시스템 1 / true 33 · 예외는 1명이 아니라 5명) ②실무 3명 이름 재앵커(배성수 26·김민환 27·남진식 28) ③"정태휘" → 김영삼(id 13). **②의 판단 필요분은 데이터를 고치는 쪽으로 결정**(사용자): 이슈 작성자 두 명이 서로 뒤바뀌어 있어(시트 남진식의 7건이 배성수에게) `maintenance.json`을 교환하고 이미 적재된 DB는 **Flyway V10**이 수렴시킨다. D3 이슈 쓰기가 열리기 전이라 모든 행이 시드 적재분인 지금이 마지막 시점이었다. 원 항목: (2026-08-23 host 재앵커에서 발견. `docs/PRD-pms.md`는 PMS 담당 소유라 host가 고치지 않았다): ①**§553 `Person.billable` false 목록** — 조직 이름이 재구축 전 것이고(`프로텐`·`관리•마케팅부`는 현 조직에 없다) 인원수도 어긋난다: 문서 "10명 false / **34명** true"인데 실측은 **false 10 + 시스템 계정 1 / true 33**이다. 예외 서술 "송현솔 이사의 참여 1건"도 실측 **5명**이다(장대근 24건·윤종헌 12·천용우 9·김주선 5·진희원 7 — `billable=false`인데 프로젝트 배정 보유. 집계는 `billable=true`만 보므로 eval 기대값에는 영향 없다) ②**§554 engineerId 매핑** — "실무 3명(노도온 26·한은율 27·송수람 28)"은 id 기준으로 **배성수·김민환·남진식**이고, 뒤의 "작성 엔지니어 매핑 남진식→노도온·배성수→송수람"은 시트 원본 이름과 시드 인물이 섞여 있어 판단이 필요하다 ③**§177** "정태휘가 1171%로 왜곡된다" → id 13 = **김영삼**. 참고로 **§558은 이미 재앵커돼 있다**(PMS 담당이 `ProjectSeedLoadIntegrationTest`로 고정 — host 실측과 수치·판정이 완전히 일치했다)
- ~~**팀원 scope 변경(2026-08-22)의 host 트랙 반영**~~ — **2026-08-23 확인 완료**(재앵커와 함께): 실제 파급은 **화자 1건**이었다 — "참여가 가시성의 유일 원천" 페르소나(B-05·SC-16)가 성립하지 않게 돼 화자를 이은지(29)로 교체했다(그 프로젝트에 팀 동료가 없는 유일한 인물). 나머지는 서술 정정 3곳(B-03·E-03·SC-08·SC-15)뿐이고 기대 건수는 바뀌지 않았다 — 팀원 화자의 가시 집합이 넓어져도 케이스들이 묻는 것은 본인 배정분이었다
- ~~**진척률 진행중 제한(2026-08-22 A2-9)의 host 트랙 반영**~~ — **2026-08-23 확인 완료**: D류 8케이스의 대상 8건이 **전부 진행중**임을 실측 확인해 기대값은 그대로다. 거절 경로는 유저_시나리오 SC-23 파생으로 신설했다(앵커 = 명화공업 363/수주확정 — "상태를 바꿔 주겠다"는 제안 금지가 채점 지점이다). 시스템 프롬프트의 쓰기 거절 안내 반영은 **G1 준비 시**(프롬프트는 도구 문구·eval과 four-in-one이라 한 번에 손댄다)

- ~~**유지보수 이슈→계약 링크 기준 — pms 확인 필요(PMS-M1 적재 전)**~~ — **2026-08-14 해소: 사이트명 포함 확정**(계약명·계약사·사이트명 3종 — 결정 기록, PRD-pms 부록 B 명시화). eval C류 v1.6 전제 성립, 적재 구현은 PMS-M1
- MCP Java SDK의 2026-07-28 스펙 지원 릴리스 모니터링 (지원 시 구현 노트·검증 절차 갱신)
- ~~구 PRD 열린 질문 #1·#3~~ — 2026-08-02 PRD-host v2.0에서 처리 완료(#1 30일 명문화, #3 M3 이월 명시)
- ~~PRD 2문서 분리에 대한 PMS 담당 합의 확인~~ — 2026-08-02 확인 완료, PRD-pms v2.0 작성 완료
- ~~ADMIN(대표) 조직 가시성 = 전사~~ — 상위 PRD §4에 반영(시드 orgRole 근거), 2026-08-09 게이트 P 통과로 최종 승인(결정 기록)
- ~~**권한 모델 재기술 여부 (pms 담당 결정)**~~ — 2026-08-03 결정 완료(합집합 판정 + PM/PL/참여자 3단, 위 결정 기록). ~~MCP 담당 확인 대기~~ — 2026-08-03 확인 완료(`whoami` 유효 권한 미반영·`myRole`은 M-1 유예 — 위 결정 기록)
- ~~**가동률 집계에서 대표 직속(HQ_TEAM '프로텐') 제외 여부**~~ — 2026-08-06 해소: `Person.billable` 모집단으로 일반화(상위 PRD §3 — 결정 기록). ~~MCP 담당 확인 대기~~ 2026-08-06 확인 완료(결정 기록)
- ~~시드 적재 정책 잔여(PMS-M1 전)~~ — **2026-08-10 전량 해소**: ~~배정 월별 M/M 부여 규칙·billable=false 팀 목록~~ 확정(가동률 재정의 결정 기록)·~~유지보수 시트→JSON 변환+engineerId 매핑~~ 완료(`reference/seed/maintenance.json` — 계약 105·사이트 157·이슈 14, PRD-pms 부록 B). OFFSITE→원격·기본 그룹 4종·시스템 계정·계정 규칙 포함 전 규칙 확정 — 남은 것은 적재 구현 시 수행뿐(PMS-M1)
- ~~권한 그룹 일반화(2026-08-09 결정 ⑦)·가동률 의미 재정의(2026-08-10)의 host 측 문서 반영~~ — **2026-08-10 전량 완료**: 결정 ⑦ = PRD-host v2.2·eval-cases v1.2(`whoami` orgRole → 권한 그룹명) · 가동률 재정의 = **PRD-host v2.3·eval-cases v1.3**(`overbooked` 판정 기본>100·보정 ×coeff 보조 지표) + 목업 산식·billable 반영. 가동률 eval **기대값 확정**은 원래 일정대로 M-1 eval 항목에서(입력 후보 = PRD-pms 부록 B 오버부킹 3명)
- ~~**유저_시나리오 §2·§8 동종 갱신 (host 재량분)**~~ — **2026-08-10 해소**(host 세션 — 유저_시나리오 v1.3·eval-cases v1.4): §2 전세아 기본 133% 확정치·§8 유예 전량 해소 표기. 반영 중 발견·수정 2건: SC-17/F-01·F-02 "무배정 월" 전제가 시드 규칙 확정으로 붕괴(2026-12 활성 16건 관통 → 2027-12/2015-01 교체), D-05/SC-21 매칭 기대값 3건→화자 가시성 내 2건(B2-1 실측)
- ~~**완료 상태 프로젝트의 진척률 수정 허용 여부**~~ — 2026-08-06 해소: 거절(`409 PROJECT_COMPLETED`) + 재개 후 수정, 완료 전이 재설계에 포함(결정 기록). ~~MCP 담당 확인 대기~~ 2026-08-06 확인 완료. 카탈로그 공백 2건(전사/타부문 가동률 scope · `list_maintenance_logs` id 의미)은 ROADMAP M-1 항목으로 등재 — ~~`list_maintenance_logs` id는 pms 도메인 확인 필요~~ **2026-08-06 해소**(유지보수 재설계 결정 기록): 프로젝트:계약 1:1(이관)이나 프로젝트 없는 계약(OEM 직접 등록) 존재 → projectId 단순화 불가, 계약/이슈 id 유지. ~~MCP 담당 확인 대기~~ 2026-08-06 확인 완료(결정 기록)

## 세션 로그 (트랙 분리 이전 공용 기록 — 2026-08-02 이후는 트랙 파일에)

### 2026-07-31 — v3 신설: 하네스 초기 구성 + 자산·결정 승계
- 완료: 하네스 구성(CLAUDE.md 헌법·PROGRESS/ROADMAP·/next·/wrap-up·reviewer 에이전트·guarded verify.sh·Stop 훅·권한). 재사용 자산 이관(reference/seed, frontend). 결정 기록 승계(신규 7 + 승계 3)
- 검증: verify.sh 사전 코드 단계 SKIP 정상, Stop 훅 exit 0, bash -n 통과
- 미해결: 위 미해결 이슈 2건
- 다음 작업: P단계 ① PRD v2.0 작성
