# 진행 상태 원장 (PROGRESS — 공용)

> **모든 세션은 이 파일 + 자기 트랙 파일을 읽는 것으로 시작한다.**
> 세션 로그·트랙별 다음 작업은 `PROGRESS-host.md` / `PROGRESS-pms.md`에 기록한다.
> 이 파일에는 공용 상태와, 양쪽에 영향을 주는 결정만 남긴다.

## 현재 상태 (2026-08-10)

- **단계:** **M-1(사전 검증 — 목업)** — 게이트 P 통과(2026-08-09). P단계 산출물 확정본: 상위 PRD · **PRD-host v2.2**(결정 ⑦ 반영 — 2026-08-10) · **PRD-pms v2.6**(가동률 의미 재정의 + 시드 정책 완결 — 2026-08-10 결정 기록, MCP 확인 완료) · 기술_선택_근거 v2 · 구현_노트 v1.0 · 유저_시나리오 v1.1 · **eval-cases v1.2**. **시드 적재 정책 전량 확정**(`reference/seed/` — people·projects·maintenance). **목업 MCP 서버 스캐폴드 + B2-0 배관 검증 통과**(2026-08-10, host 트랙 — `pms-mcp-mock/`, 도구 7종·테스트 32개). **가동률 재정의 host 반영 완료**(2026-08-10 — PRD-host v2.3·eval-cases v1.3·목업 산식). **B2-1 자연어 검증 완료**(2026-08-10 동석 실험 + 전 라운드 재실행 — 조회·권한·경계·인젝션 통과, **실패 2건: D-06 취소 후 확인 생략(F3 상당)·A-06 모호 질의 임의 해석(F5 후보) — 둘 다 확인 규칙 계열·비결정 재현** → 시스템 프롬프트 §4 필수 확정, `docs/evals/B2-1-자연어검증.md`. 유예 2건 = 현상 유지 권고 · 카탈로그 공백 2건 = 실증). **B2-1 후속 결정 ③④ pms 측 결정 완료**(2026-08-11 — `get_utilization` scope에 `COMPANY` 추가 + 응답 조직 정보 · `search_maintenance` 신설로 카탈로그 7종→8종. 둘 다 **MCP 담당 확인 대기**, PMS 측 반영은 PRD-pms v2.7). 다음: **B2-1 후속 결정 4건 확정** — ③④ MCP 측 확인(양측 합의 성립) + ①②(현상 유지 — `get_project` 분리 불요·`myRole` 불포함) 등재 → eval 입력·기대값 확정(A-01 확장·C-01 재실험 포함) → B2-2 JWT
- **협업:** 2인 — MCP 담당(host 앱 + pms 내 `/mcp` 어댑터) / PMS 담당(도메인·서비스·프론트)
- **코드:** 없음(새 시작). 재사용 자산: `reference/seed/`(사원 44·프로젝트 382 JSON), `frontend/`(React 프로토타입, M1에서 재연동)
- **차단 요소:** 없음

## 결정 기록 (Decision Log)

> 신규 결정 + 구 프로젝트에서 승계한 결정. 승계분은 (승계) 표기 — 원 기록은 `C:\Projects\pms_mcp\docs\PROGRESS.md`.

| 날짜 | 결정 | 근거 |
|------|------|------|
| 2026-08-11 | **가동률 전사 scope 공백 해소 — `get_utilization` scope에 `COMPANY` 추가 + 응답에 조직 정보 동봉** (B2-1 판단 ③ 확정 — 카탈로그 변경, **MCP 담당 확인 대기**. host 반영분 = PRD-host FR-AI-11·도구 description·eval A-01 확장·목업 코드로 host 트랙 소유. pms 반영분 = PRD-pms v2.7 C1-6·§7): ①scope 열거 = ME/MY_TEAM/DIVISION/**COMPANY**/PERSON — `orgUnitId` 파라미터화(B안)·열거+id 하이브리드(C안) 미채택 ②`UtilizationEntry`에 **team·division 추가**(웹 `GET /api/utilization` 응답도 동일) ③전사 가시성 없는 호출자의 `COMPANY` = **404 은닉**(MY_TEAM·DIVISION 현행과 동일 — 자기 범위 축소 미채택) ④타부문 지정·중간 노드 집계는 미해결 존치 — 열거 확장은 하위 호환이라 고통 확인 시 `ORG_UNIT` 증설 가능 | ①**기능 추가가 아니라 정의 미반영**: 상위 PRD §3 "가동률 집계 대상"·PRD-pms C1-5가 이미 "팀·부문·**전사** 집계"를 정의하고 있었다 — 카탈로그만 뒤처진 상태 ②웹은 이미 `orgUnitId`(root=전사)로 지원 → "챗=화면 동일 판정" 원칙의 의도치 않은 위반 ③현 열거는 부문·팀 2단 고정 시절 어휘 — 결정 ⑧(2026-08-09 임의 깊이 트리)에 웹만 따라갔다(teamId→orgUnitId) ④B2-1 R3-1 실측: 모델이 `scope=COMPANY`를 **지어내 호출**(422 교정) — 자연 발화와 모델 기대가 같은 공백을 지시. 오용(F5) 없이 한계 정직 안내라 안전성은 통과, 완결성만 결손. ADMIN의 DIVISION 무의미(모집단 공집합 → `[]`)도 같은 실험에서 실증 ⑤**조직 정보 동봉 근거**: R3-1 발화가 "부문별로"인데 `UtilizationEntry`에 소속이 없어 COMPANY만 추가해선 미완결(44명 평면 목록 → 부문 파악에 find_person 반복). B2-1 §5가 scope 공백으로만 분류했던 부분을 결정 단계에서 발견 ⑥**B안 미채택**: `orgUnitId`는 조직 id 확보 경로가 없어 판단 ④와 동형 문제를 유발하고 도구 신설 압력을 만든다 — 관찰된 고통만 메우는 것이 "하네스 부품은 고통 확인 후 추가" 원칙 ⑦**404 은닉**: 구조 원칙 3 + 기존 MY_TEAM/DIVISION 일관성. 축소안은 모델이 전사 결과로 오해하면 F5 |
| 2026-08-11 | **유지보수 id 확보 경로 공백 해소 — `search_maintenance` 도구 신설 (카탈로그 7종 → 8종)** (B2-1 판단 ④ 확정 — 카탈로그 변경, **MCP 담당 확인 대기**. 상위 PRD §6 "도구명·설명·시스템 프롬프트·Eval 한 세트" 적용 — host 반영분은 host 트랙 소유이며 eval **C-01 재실험 필요**. pms 반영분 = PRD-pms v2.7 D4-1·§7): ①`search_maintenance(keyword?, status?)` → 계약 id·계약명·계약사·상태·기간·**매칭 사이트** 반환 ②keyword 매칭 범위 = **계약명·계약사·사이트명(고객사)** 부분 일치 ③가시성 판정·404 은닉 **없음**(D4-3 전사 공개 계승) ④웹 `GET /api/maintenance/contracts`(D4-1)에도 **동일 keyword 검색 추가** ⑤ⓐ(description 보강 단독)·ⓑ(프로젝트 상세에 계약 id 동봉)·ⓒ-2(기존 도구 확장) 미채택 | ①**ⓑ는 시드 실측 커버 0건**: `reference/seed/maintenance.json` 계약 **105건 전부 `sourceProjectId=null`**(시트 실데이터라 이관 이력 없음) — ⓑ가 커버하는 이관 생성 계약은 PMS-M4에나 존재한다. B2-1 §5의 "부분해" 평가를 실측으로 **현시점 무용**으로 정정 ②**사이트명 키 필수(결정 단계 신규 발견)**: 가온아이 1계약에 고객사 사이트 45개 — 사용자는 "가천대길병원"으로 부르는데 그 문자열은 계약명·계약사에 없고 사이트명에만 있다. B2-1 R1-7이 이를 못 잡은 이유는 목업 계약명("한국수출입은행 규정관리 유지보수")과 발화가 우연히 일치했기 때문 ③웹 D4-1이 이미 계약 검색을 보유 → 판단 ③과 동형의 카탈로그 지연. 웹 필터에도 사이트명이 없어 같은 한계를 공유하므로 동시 해소("챗=화면 동일 판정") ④`find_person`("id를 모를 때 사용")과 동일한 **id 확보 전용 도구 패턴** 답습 — 사람·프로젝트에는 있고 유지보수만 없던 비대칭 해소 ⑤D4-3(전사·404 은닉 없음)이라 가시성 로직이 불필요 → 신설 비용·검증 위험 최소 ⑥**ⓒ-2 미채택**: 반환 엔티티가 계약 목록 ↔ 이슈 목록으로 상이해 한 도구 두 모드는 description 복잡·모델 혼동을 유발한다. `search_projects`의 목록/상세 겸용은 **동일 엔티티**라 성립한 것이므로 선례가 아니다 ⑦8종화 위험 낮음 — B2-1에서 7종 도구 선택 실패 0건, R1-7도 오입력 없이 멈춰 경로만 주면 되는 상태 |
| 2026-08-10 | **가동률 의미 재정의 — 배정 M/M = 실투입 계획** (상위 PRD §3 재정의·PRD-pms v2.6. ~~MCP 담당 확인 대기~~ → **확인 완료(2026-08-10 동석 리뷰)** — 카탈로그·`get_utilization` 시그니처 무변경, 의미 변화: `overbooked` = **기본**>100(구 보정>100 대체) · 보정 산식 ÷coeff→**×coeff**(단가 가중 보조 지표, 과부하 판정 미사용·명칭 유지) · 가동률 eval 기대값(§8 유예분)의 전제): ①배정 M/M = **실투입 계획**(1.0=해당 월 풀타임), 계약 배분 숫자 금지 — 계약 관점은 프로젝트 `contractMm`에만(B1-5 UI 레이블 명시) ②과부하 판정·팀/부문/전사 집계 정본 = 기본 가동률 ③**PM 역할 하드 제외 미채택** — PM 자동 배정 기본 M/M=0(A6-7)+실투입 의미로 "관리형 PM 부하 미표시" 충족, 하드 제외는 실무형 PM 과부하 사각+부하 은닉 우회 ④시드 확정 2건: 월별 M/M 부여 규칙(실무자=PM 외 참여자·없으면 PM 본인, contractMm 안분 근사 — 2026-08 기준 오버부킹 3명(남민준 190%·손윤린 182%·전세아 133%) 시뮬레이션 검증) · billable=false = 프로텐·AX사업기획부·관리•마케팅부 3부문 10명(부록 B) | pms 담당 실무 검토: 계약 M/M 배분(예: 차장 2/사원 1)이 실투입(체크만 vs 전담 개발)과 괴리 — 계약 숫자를 배정 M/M에 넣으면 과부하 경보 오발·여유 인력 오판으로 가동률의 존재 이유("누가 여유 있나") 전복. 구 ÷coeff 보정은 배정 M/M이 단가 기준일 때만 성립하는 환산이라 실투입 의미에선 "시간 100% 찬 수석이 67%"로 왜곡. gradeCoeff·US-E4·기본/보정 토글은 ×coeff 재정의로 폐기 수술 없이 보존. 시드 실측: 진행중 34건 중 1인 프로젝트 다수(PM=실무자)라 PM 일률 제외 불가 |
| 2026-08-09 | **게이트 P 리뷰 기획 결정 8건 일괄 채택 — 프로토타입 동작대로** (게이트 P 승인과 같은 자리(두 담당 동석 리뷰)에서 결정됐으나 게이트 기록에 누락 → 소급 등재. ④⑦ 공용 변경 포함 MCP 합의는 리뷰 동석으로 성립. 상위 PRD §4 재작성·PRD-pms v2.5 반영): ①**웹 진척률 2단계 완화** — 웹 UI는 100% 저장만 확인 모달, 그 외 값은 1클릭 저장(confirmed=true 단건 호출). 서비스 프로토콜·MCP `update_progress` 2단계는 무변경 — 2026-08-02 "웹=MCP 동일 2단계" 결정의 부분 변경(동일 서비스는 유지, UI 확인 강도만 분리) ②권한 조정 감사 단위 = 저장 1건(A8-2 현행과 정합 — 확인만, 변경 없음) ③engagement 한국어 명칭 확정 = 원격·상주·부분상주 ④**시스템 관리자 계정** — admin@proten.co.kr, 관리자 그룹 소속의 회사 고유 계정, 삭제·수정 불가, 인력·가동률·배정 목록 제외. 대표 개인의 관리자 그룹 소속은 유지(수습 주체) ⑤**팀·직급 관리 채택**(US-E3·E4 신설 — 2026-08-06 "설정 편집 탭 승격 안 함" 결정 대체) ⑥**OFFSITE 폐지** — engagement 3종으로 축소, 시드 32건은 적재 시 원격으로 흡수 ⑦**orgRole → 편집 가능한 권한 그룹 일반화**(2026-08-03 "orgRole 기능 플래그 미채택"·PRD-pms Out of Scope "orgRole 커스텀 추가/편집" **대체**) — 그룹 = 가시성 scope 4단(전사/부문/팀/본인) + **프로젝트 밖** 기능 플래그 4종(프로젝트 생성·계약 관리·전 프로젝트 관리=PM 간주·사용자/조직/권한 관리). 그룹 신설/수정/삭제(소속 인원 있으면 삭제 거절)·**관리자 그룹은 시스템 고정**(자기 잠금 방지)·판정·가시성·404 은닉이 그룹 정의를 따름. 시드 orgRole 4종은 기본 그룹 4종으로 적재. **host 접점**: 도구 카탈로그·시그니처 변경 없음, `whoami` 응답의 orgRole → 권한 그룹명 — PRD-host·eval 반영은 host 트랙 소유 ⑧**조직 임의 깊이 트리** — 회사(root)→부문→팀→임의 깊이(Division·Team 2단 고정 해제, identity 모델 변경), 인원·프로젝트·하위 있으면 노드 삭제 거절, **팀 가시성 = 하위 조직 subtree 포함** | 화면 프로토타입 3라운드 리뷰에서 실사용 검증으로 도출(세부 경위는 PROGRESS-pms 2026-08-09 세션 로그 1~3차). ①100% 외 값까지 2단계는 마찰 과다 — 100%만 완료 유도 접점이라 확인 가치가 있음 ④감사 actor·수습 주체를 개인(대표) 퇴사와 절연 ⑦고정 4단 orgRole은 조직별 권한 편차를 못 담음 — 프로젝트별 권한 커스텀(2026-08-06)과 같은 원리(기본값+조정)를 조직 축에 적용 ⑧조직 개편·파트 신설을 도메인 변경 없이 수용. 기록 누락은 pms 세션(2026-08-09)에서 발견, 사용자 확인(전부 채택·합의 성립) 후 등재 |
| 2026-08-09 | **게이트 P 통과 — 사용자 승인** (문서 5종: 상위 PRD · PRD-host v2.1 · PRD-pms v2.4 · 기술_선택_근거 v2 · 구현_노트 v1.0 + 유저_시나리오 v1.1 + eval-cases v1.1 — 2026-08-06 개정분 포함 일괄. 미해결의 "ADMIN(대표) 가시성 = 전사"도 상위 PRD §4 포함분으로 함께 확정). P단계 종료 → M-1 착수 | 게이트 규칙(ROADMAP): 통과는 사람이 승인하고 결정 기록에 남긴다. 작성물 전량 완료 + 공용 변경 4건 양측 합의 성립 + 화면 프로토타입(2026-08-09 머지) 리뷰 보조 상태에서 승인 |
| 2026-08-06 | **공용 변경 4건 MCP 담당 확인 완료** (완료/재개 전이 재설계 · billable 모집단 · 프로젝트별 권한 커스텀 · 유지보수 재설계 — 양측 합의 성립, 공용 문서 변경 확정). host 관점 영향 확정: ①`update_progress` 거동 변화 수용 — 100% 저장 시 전이 없음(completable 안내)·완료 상태 `409 PROJECT_COMPLETED`(eval D-08·SC-23 선반영분 유효) ②`get_utilization` 팀·부문·전사 집계 모집단 = billable ③참여자 진척률 off 프로젝트에서 `update_progress` 403 — 챗은 거절 전달 ④`list_maintenance_logs`는 projectId 단순화 불가(계약/이슈 id — M-1 카탈로그 공백 항목의 도메인 입력 확정) · 구 `/maintenances/{id}/logs` → 이슈·코멘트 API 대체 · 프로젝트 목록 `?phase=` 파라미터 추가. 도구 카탈로그(7종)·시그니처 변경 없음 — host 측 문서(PRD-host·eval) 반영은 host 트랙 소유 | 게이트 P 승인의 전제(공용 변경은 양측 합의 — 협업 경계 규칙). 두 담당 동석 리뷰로 확인, pms 담당 전달(2026-08-06) |
| 2026-08-06 | **유지보수 재설계 — 계약/사이트/이슈 3층 + phase 탭** (PRD-pms v2.4 — §4 maintenance 모델·EPIC D 재작성(US-D1~D4)·§7 API 재편·부록 A/B. 상위 PRD §4-2 표 "이관/이력"→"이관"·§4-3 계약 등록 행 추가. **MCP 담당 확인 대기** — 접점: ①구 미해결 "프로젝트:Maintenance 1:1 vs 1:N" 답 = 프로젝트:계약 **1:1**(이관 경로)이나 **프로젝트 없는 계약(OEM 직접 등록) 존재 → `list_maintenance_logs`의 projectId 단순화 불가**, 계약/이슈 id 유지 ②구 `/maintenances/{id}/logs` → 이슈·코멘트 API로 대체 ③프로젝트 목록 `?phase=` 파라미터+파생 필드 추가). ①`MaintenanceContract` — 연 단위 계약, sourceProjectId nullable, 이관+직접 등록 입구 2개 ②`MaintenanceSite` — 계약:사이트 1:N, **담당 엔지니어 정본 = 사이트**(이슈 기본 배정 원천) ③`MaintenanceIssue` — 구 게시판 대체, type{장애,문의,요청}·상태{접수,처리중,고객확인대기,완료}·append-only 코멘트(구 MaintenanceLog 불변식 계승) ④탭은 category 컬럼 없이 **phase=status 파생**(영업={계약대기,수주확정}·솔루션={진행중,완료}), 유지보수 탭=계약 목록 ⑤권한: 계약 등록·수정=orgRole(프로젝트 생성 동일 열거)·이슈=전사·이관=PM 유지 ⑥미채택: sales/SalesInfo·정기점검 모델링(계약 정보 필드만)·만료 임박 알림 ⑦시드=2026 유지보수 시트 실데이터(마스킹 없음) | pms 담당 실무 검토(시트+구 이슈 게시판 실측): 유지보수 계약은 연 단위이고 계약:고객사 1:N(가온아이 1계약 ~45사이트), OEM 채널 계약은 원천 프로젝트 부재 — 완료 프로젝트 1:1 파생 모델로는 등록 자체 불가. 45건 분해는 계약금액·기간·갱신 정보 45배 복제라 기각. "누가 뭘 담당하는지 안 보임" 고통의 정본을 사이트 engineerId로 구조화(실측: 열린 이슈 23건 중 할당 1건 — 사실상 무배정 운영). 열림/닫힘 2상태는 미배정·처리중·대기 구분 불가. category 컬럼은 status와 원본 이중화라 파생값으로. 정기점검·만료 알림 미채택은 "특이사항 없으면 기록 안 함" 실무 + 고통 확인 후 추가 원칙 |
| 2026-08-06 | **프로젝트별 권한 커스텀 — §4-2 표를 기본값으로 전환** (상위 PRD §4-1 판정식·§4-2 확장, PRD-pms v2.3 — US-A8·`ProjectPermissionOverride`·`GET/PUT /projects/{id}/permissions`·부록 A 권한 패널. **MCP 담당 확인 대기** — 카탈로그·`update_progress` 시그니처 변경 없음, 거동 변화 1건: 참여자 진척률을 끈 프로젝트에서 `update_progress` 403 → 챗은 거절 전달. **기본값 무변경이라 기존 시나리오·eval 기대값 영향 없음**). ①전역 권한 관리 화면은 두지 않고 **프로젝트 설정에서 PM이 역할별 기능 토글**로 조정 ②조정 가능 = PL·참여자 × {정보 수정·배정/M/M·진척률·완료 처리·재개} 양방향 ③고정 = PM 열 전체·조회 행·삭제/이관 행 ④완료·재개는 한 토글 ⑤역할 신설(+권한)은 미채택 — 3단 고정 ⑥override만 저장(행 부재=기본값)·`Project.version` 공용·감사 로그·기본값 복원 | pms 담당 실무 검토(PRD 리뷰): 고정 표가 모든 프로젝트에 맞지 않음 — 다부문 프로젝트는 PL에 배정 위임이, 민감 프로젝트는 참여자 진척률 잠금이 필요. 프로젝트 역할은 "프로젝트마다 개별 판정"(§4 원칙)이므로 관리 UI도 프로젝트 스코프가 정합 — 전역 탭은 판정 단위와 어긋난다. PM 열·조회·삭제/이관 고정은 자기 잠금 방지 + ADMIN 수습 보장(§4-1 치환) + 비가역 안전장치(완료 재설계 근거) 보존. 완료·재개 묶음은 "사고 낸 사람이 수습한다" 대칭 유지 |
| 2026-08-06 | **완료 전이 재설계 — 100% 자동 전이 폐지, 명시적 완료 처리·재개** (PRD-pms v2.2 — §5·A2-3·A2-8·US-A7·US-F3 신설, 상위 PRD §4-2 표 확장. **MCP 담당 확인 대기** — `update_progress` 시그니처·카탈로그 변경은 없으나 거동 변화: 100% 저장 시 전이 없음(완료 처리 안내)·완료 상태 수정 `409 PROJECT_COMPLETED`. eval D-08·SC-23 기대값 pms 세션에서 선반영). ①완료 처리 `POST /complete`(progress=100 전제)·재개 `POST /reopen`(완료→진행중, progress=90 리셋, 사유 없음 — AuditLog가 담당) 모두 **배정 전원**(PM·PL·참여자) ②완료 상태 진척률 직접 수정 거절 — 재개 후 수정 ③유지보수중 재개 불가 ④100%인 채 7일 방치 시 PM·PL 리마인드, 재개 시 미읽음 회수(F3) ⑤완료·재개는 MCP 도구 미노출(원칙 5) | 자동 전이는 참여자의 진척률 쓰기(A2)가 상태 전이 권한(A5-3이 참여자 403)을 우회하는 부수효과 — 생애주기에서 가장 되돌리기 어려운 전이가 뒷문으로 열려 있었다. 실무에서 100%≠완료(검수·납품 잔여)이고, 완료를 PM에 묶으면 시드 PM 편중(1인 128건)에서 병목. 재개를 완료와 같은 권한으로 묶는 근거: 사고 낼 수 있는 사람이 수습도 해야 한다(비대칭이면 참여자 오조작마다 보고 체인 발생). 진짜 비가역(이관)은 PM 전용 유지로 안전장치 보존 |
| 2026-08-06 | **가동률 집계 모집단 = `Person.billable`** (상위 PRD §3 신설 — 공용 문서 변경, **MCP 담당 확인 대기**. PRD-pms: 도메인 필드·C1-5·부록 B 적재 규칙. 구 미해결 "HQ_TEAM 제외 여부"를 일반화해 해소). 팀·부문·전사 집계와 `overbooked` 목록의 모집단은 billable=true만, 개인 지정 조회는 무관. false 지정 팀 목록은 시드 적재 정책과 함께(PMS-M1 전) | 시드 실측: 진행중 프로젝트 배정 인원 **18/44명** — 대표 1명만 빼도 영업·기획마케팅·경영관리 등 지원 인력의 0%가 전사·부문 평균을 계속 왜곡한다. "대표 제외"가 아니라 모집단 정의가 진짜 문제. `get_utilization` 전사/부문 scope(M-1 실험)가 "무엇의 평균인가"를 답할 전제이기도 하다 |
| 2026-08-06 | **PL 복수 허용 — API 제약 해제** (상위 PRD §4-2 확정, PRD-pms A1-7 삭제·A6-7 `MULTIPLE_PL` 삭제·에러 표 정리) | 실무 확인(pms 담당): 다부문 프로젝트(4개 팀 걸침)에서 파트별 리드가 실제 존재. 스키마는 기수용이라 마이그레이션·접점 변경 없음 — 유예 결정(2026-08-03)이 예정한 해제 경로 그대로 |
| 2026-08-03 | **eval 셋 30 → 36케이스 확장** (P단계 ⑤ 이관 — `docs/evals/eval-cases.md`. eval은 host 재량이나 유저_시나리오 §9가 "비중 조정 vs 30 초과 확장"을 이관 시 결정으로 위임했고 ROADMAP 문구("분류·비중 유지")를 구체화하므로 기록). 구 30의 분류·비중은 감축 없이 유지하고 덧붙임: **쓰기 확인 3→8**(SC-11 참여자 쓰기 · SC-21 대상 모호 · SC-22 취소 · SC-23 범위 밖 값/완료 전이 2건 + 구 D-03 일괄 승계) · **오류/점검(FR-AI-26) 1 신설**. 인젝션(SC-24)·자격증명(SC-25)은 구 C-04·G-03 자리 승계라 증설 없음. G1 합격선은 비율 유지(≥90% = 33/36) | "분류·비중 유지"를 지키는 방법은 감축 재배분이 아니라 추가 — 30을 고수하면 성공 경로(의도 해석 ≥90%의 분모)를 깎아 실패·오남용 5종을 욱여넣어야 한다. 추가 6건의 회귀 비용은 무시 수준(프롬프트 1자 변경 시 전체 재실행이 이미 규칙)이고, 쓰기 실패 경로는 전부 F3(1건 = 출시 차단) 등급이라 검증 밀도를 높일 가치가 가장 큰 곳 |
| 2026-08-03 | **권한 모델 확정분 MCP 담당 확인 완료** (양측 합의 성립 — 공용 문서 변경 확정). host 관점 영향: 도구 카탈로그·`update_progress` 2단계 확인 프로토콜 **변경 없음**(참여자 진척률 수정은 기존 US-A2 동작 유지). **`whoami`는 유효 권한 미반영**(현행 유지 — id·이름·팀·부문·orgRole만 반환). 프로젝트별 역할 노출(`search_projects` 상세 응답 `myRole`)은 **M-1 목업 실험 후 결정**으로 유예 | 프로젝트 역할은 프로젝트마다 개별 판정이라 전역 응답인 `whoami`에 유효 권한을 실을 수 없음(전역으로 의미 있는 orgRole은 이미 반환). 권한 판정은 서버가 쓰기 시점에 최종 수행(원칙 3·4) — LLM이 권한을 몰라 생기는 최악은 거절 응답 전달이지 우회가 아님. 응답 스키마 변경은 협업 접점(four-in-one 재작업)이라 `get_project` 분리와 동일 원칙(실험 근거 먼저)으로 유예 |
| 2026-08-03 | **권한 모델 확정 — 합집합 판정 + 프로젝트 역할 3단** (상위 `PRD.md` §4 재작성). ①**판정은 합집합**: `canDo = orgPerm(orgRole) OR projectPerm(프로젝트 역할)` — orgRole을 선행 게이트로 쓰지 않고, 프로젝트 역할이 위로만 덮어쓴다(축소 없음) ②**프로젝트 역할 = PM / PL / 참여자 3단**(구 "관리자/담당자" 대체 — orgRole ADMIN과 명칭 충돌 해소). PM 1명 필수 · PL 복수 가능 · 프로젝트마다 개별 판정 ③권한 배분: 정보 수정 PM·PL / 배정·M/M 입력 PM만 / 진척률 PM·PL·참여자 모두(`Project.progress`가 단일 값이라 "본인 몫" 부분 수정 개념 없음 — 기존 US-A2·`update_progress` 동작 유지) / 삭제 PM만 ④**orgRole은 프로젝트 밖 행위만** 담당 — 프로젝트 생성(TEAM_LEAD·DIVISION_HEAD·ADMIN 열거, 서열 미정의) · 조직/사용자/직급 관리(ADMIN). 프로토타입 기능 플래그 5종 미채택, **부문장 `editProgress:false` 폐기** ⑤가시성은 프로젝트 역할이 확장하되 **프로젝트 컨텍스트 내로 한정**(타 팀 인원의 프로젝트 밖 데이터는 조직 가시성 규칙 유지) ⑥PL 복수는 스키마로 수용하고 API 제약으로 유예 ⑦시드 적재: `managerId`→PM, 나머지 `assigneeIds`→참여자, **PL은 미지정**(시드에 근거 없음) ⑧ADMIN은 전 프로젝트에서 PM과 동일 권한(PM 부재·퇴사 수습) ⑨enum 값은 `{PM, PL, PARTICIPANT}` — orgRole `MEMBER`와 이름 충돌 회피 | 시드 실측: **382건 중 91건(24%)이 PM의 orgRole=MEMBER** — orgRole 선행 게이트면 24%가 막힌다. `managerId`가 `assigneeIds`에 305/305 포함 → PM도 참여자, 배정 테이블의 역할 컬럼 하나로 표현 가능. PM 편중(1인 128건·55건·36건)은 PM이 총괄/영업 성격이고 실무 리드가 별도임을 시사 → PL 계층 신설 근거. 프로토타입 `canEdit`(orgRole `editProgress` 선행) ↔ `canManage`(`pmId` 선행) 비대칭은 같은 원인의 실제 결함(`frontend/src/store.jsx:175-188` — 프로토타입에 PL 개념은 없음). 복수 PL은 나중에 필요해질 때의 비용(스키마 마이그레이션+API+MCP 도구 응답=협업 접점 변경)이 지금 열어두는 비용(조인 1회)보다 크게 비대칭 |
| 2026-08-02 | **프로토타입 대조 검토 반영** (`frontend/` = 구 백엔드 실연동본, 사용자 승인): ①웹 진행률도 MCP와 동일 2단계 confirmed 프로토콜(`PUT /progress`) ②내 계정(프로필·비밀번호·알림 설정, EPIC H)·사용자 CRUD(US-E2)·감사 조회 API(G1-3) 채택 ③**도구 카탈로그에 `whoami` 추가**(6→7종, FR-AI-16 — `GET /api/me` 동일 서비스) ④chat BFF 접점(`/api/chat`·`/feedback`) 명세 ⑤SSE `?access_token=` 인증 명시 ⑥화면 3종(홈·인력·설정) 부록 A 추가 ⑦email 로그인 + 초기 비밀번호 `proten1!` 확정 ⑧**배정은 기간 모델 유지**(프로토타입 월별 upsert는 재연동 시 조정) ⑨상태 전이 서버 강제 AC(US-A5) ⑩유지보수 경로 `/api/maintenances/{id}/logs` 통일. **권한 모델 재기술(orgRole 기능 플래그·managerId·HQ 집계 제외)은 pms 담당 결정 보류** | 프로토타입은 실연동으로 검증된 동작 — 명세로 승격하는 쪽이 재작업 최소. whoami는 카탈로그 변경(협업 접점)이라 본 기록 경유 |
| 2026-08-02 | **PRD 3문서 체계**: 상위 `docs/PRD.md`(공유 정의 — 용어·권한 모델·가동률 산식·대상 사용자·시드 기준·협업 접점. **공용 소유**, 변경은 결정 기록 경유) + `docs/PRD-host.md`(AI 어시스턴트, host 트랙 소유) + `docs/PRD-pms.md`(PMS 구현명세, pms 트랙 소유 — 구 "PMS — AI 구현용 PRD" v1.0 현행화 이관, 원본 전사 `reference/PMS_구현용_PRD_v1.0.md`, 충돌 7건 수정은 ROADMAP P단계 항목에 명세). 겹치는 정의는 상위가 유일 원본, 하위는 참조만 | 두 하위 문서는 독자·변경 주기가 다르고(도구/Eval vs 도메인/AC) 소유 경계가 트랙으로 분리됨. 겹치는 정의의 원본 방향 문제(host↔pms)는 상위 단일 원본으로 해소(당초 "원본=PRD-pms" 등재를 같은 날 정정). PDF 원본은 세션 밖에서 소실되므로 전사 보존 |
| 2026-08-02 | **대상 사용자 정정**: 솔루션사업부 40명(구 문서 전제) → **프로텐 전 직원**(시드 44명·7부문). 조직 단위는 부문(division), orgRole = ADMIN/DIVISION_HEAD/TEAM_LEAD/MEMBER | 사용자 정정 + `reference/seed/people.json` 근거. 구 문서 기반 작업 시 잔재 주의 |
| 2026-08-02 | **2인 분업 전환**: MCP 담당 = host 앱 + pms 내 `/mcp` 어댑터, PMS 담당 = 도메인·서비스·프론트. 모노리포 유지, 하네스는 디렉터리 스코프로 분리(host/·pms/ CLAUDE.md, verify 스코프 인자, PROGRESS 트랙 분리) | 원칙 2(임베디드 `/mcp`) 때문에 MCP 담당도 PMS 코드베이스 안에서 작업 — 리포 분리 시 어댑터 소유권·계약 문서 공유가 애매. 두 개발자 모두 Claude Code 사용. 협업 경계 = MCP 도구 카탈로그 + 애플리케이션 서비스 API, 변경은 이 결정 기록 경유 |
| 2026-08-02 | **구 conventions 3종 이관** (`docs/conventions/` — java-spring·react-ts·git-workflow). v3 정합화: ralph·`/verify`·boundary-reviewer·단일 PROGRESS 참조를 현행(verify.sh 스코프·reviewer·트랙 파일)으로 수정, CI 전제는 "CI 도입 전 verify 결과 첨부"로. 전량 주입 대신 스코프 CLAUDE.md에서 참조. Modulith 모듈 목록은 구 설계 승계 표기 — M0 스캐폴드에서 확정 | 2인 협업 + 원격 개설로 git 워크플로 규칙의 고통 시점 도래(증설 예약 발동). 코드 컨벤션은 M0 전 동료 온보딩 자료로 함께 이관 |
| 2026-07-31 | **새 폴더(pms_mcp_v3)에서 완전 재시작.** 기존 pms_mcp는 읽기 전용 참고 | 구 저장소의 중복 문서 부채(가이드 고유 가치 ~40%)·2인용 ruleset과 절연. 결정과 재사용 자산만 승계 |
| 2026-07-31 | 스택: **Boot 4.1 + Modulith 2.1** + Spring AI 2.0.0 | Boot 4.0은 2026-12 OSS EOL. Spring AI 2.0.0이 양쪽 지원, 신규 시작이라 상향 비용 없음 |
| 2026-07-31 | **MCP stateless 지향 설계** (구조 원칙 7) — 구현은 2025-11-25 스펙, 세션·Sampling·Roots·Logging 비의존 | 2026-07-28 스펙 대개정 출시 확인(stateless화, 12개월 유예). Java SDK 신 스펙 지원은 모니터링 항목 |
| 2026-07-31 | PRD v2.0에 저비용 3종: `scope=ME` · 조회 도구 version 반환 · 도구 오류/점검 응답 규칙 FR | version 미반환은 쓰기 시나리오가 도구 카탈로그만으로 완결되지 않는 결함. 공백 4건(투입 조회·유지보수 만료/SR·기간 범위·스킬/지연)은 후순위 후보 |
| 2026-07-31 | 개발 기준 모델 **claude-sonnet-5** (G3에서 단가·학습 미사용 조항과 재확인) | 비용·지연·Eval이 모델에 묶여 있어 기준 모델 없이는 수치 검증 불가 |
| 2026-07-31 | `get_project` 분리 — **M-1 목업 실험 후 결정** | 도구 카탈로그 변경은 four-in-one 재작업. 실험 근거 확보가 먼저 |
| 2026-07-31 | LLM **학습 미사용 조항 확인 = M0 게이트 전제** (사람 작업) | 법무·구매 리드타임 — 실패 시 대안(국내 리전·온프렘) 검토 시간 확보 |
| (승계 07-13) | 재사용 자산은 시드·프론트만, 백엔드는 재작성 | 데이터·UI는 코드 품질과 무관한 자산 |
| (승계 07-18) | 지시 파일 영어, 기록물 한국어 | LLM 지시문은 영어가 안정적. 기록물은 사람용 |
| (승계, v2 학습) | 하네스 부품은 고통 확인 후 추가 — 빈 섹션·선제 부품 금지. ralph·conventions·CI는 증설 예약 목록에 | 완성본 선이식이 이해도 저하와 유지 부채의 원인 (todo-cli 샌드박스 학습 결론) |

## 미해결 이슈

- MCP Java SDK의 2026-07-28 스펙 지원 릴리스 모니터링 (지원 시 구현 노트·검증 절차 갱신)
- ~~구 PRD 열린 질문 #1·#3~~ — 2026-08-02 PRD-host v2.0에서 처리 완료(#1 30일 명문화, #3 M3 이월 명시)
- ~~PRD 2문서 분리에 대한 PMS 담당 합의 확인~~ — 2026-08-02 확인 완료, PRD-pms v2.0 작성 완료
- ~~ADMIN(대표) 조직 가시성 = 전사~~ — 상위 PRD §4에 반영(시드 orgRole 근거), 2026-08-09 게이트 P 통과로 최종 승인(결정 기록)
- ~~**권한 모델 재기술 여부 (pms 담당 결정)**~~ — 2026-08-03 결정 완료(합집합 판정 + PM/PL/참여자 3단, 위 결정 기록). ~~MCP 담당 확인 대기~~ — 2026-08-03 확인 완료(`whoami` 유효 권한 미반영·`myRole`은 M-1 유예 — 위 결정 기록)
- ~~**가동률 집계에서 대표 직속(HQ_TEAM '프로텐') 제외 여부**~~ — 2026-08-06 해소: `Person.billable` 모집단으로 일반화(상위 PRD §3 — 결정 기록). ~~MCP 담당 확인 대기~~ 2026-08-06 확인 완료(결정 기록)
- ~~시드 적재 정책 잔여(PMS-M1 전)~~ — **2026-08-10 전량 해소**: ~~배정 월별 M/M 부여 규칙·billable=false 팀 목록~~ 확정(가동률 재정의 결정 기록)·~~유지보수 시트→JSON 변환+engineerId 매핑~~ 완료(`reference/seed/maintenance.json` — 계약 105·사이트 157·이슈 14, PRD-pms 부록 B). OFFSITE→원격·기본 그룹 4종·시스템 계정·계정 규칙 포함 전 규칙 확정 — 남은 것은 적재 구현 시 수행뿐(PMS-M1)
- ~~권한 그룹 일반화(2026-08-09 결정 ⑦)·가동률 의미 재정의(2026-08-10)의 host 측 문서 반영~~ — **2026-08-10 전량 완료**: 결정 ⑦ = PRD-host v2.2·eval-cases v1.2(`whoami` orgRole → 권한 그룹명) · 가동률 재정의 = **PRD-host v2.3·eval-cases v1.3**(`overbooked` 판정 기본>100·보정 ×coeff 보조 지표) + 목업 산식·billable 반영. 가동률 eval **기대값 확정**은 원래 일정대로 M-1 eval 항목에서(입력 후보 = PRD-pms 부록 B 오버부킹 3명)
- ~~**유저_시나리오 §2·§8 동종 갱신 (host 재량분)**~~ — **2026-08-10 해소**(host 세션 — 유저_시나리오 v1.3·eval-cases v1.4): §2 전세아 기본 133% 확정치·§8 유예 전량 해소 표기. 반영 중 발견·수정 2건: SC-17/F-01·F-02 "무배정 월" 전제가 시드 규칙 확정으로 붕괴(2026-12 활성 16건 관통 → 2027-12/2015-01 교체), D-05/SC-21 매칭 기대값 3건→화자 가시성 내 2건(B2-1 실측)
- ~~**완료 상태 프로젝트의 진척률 수정 허용 여부**~~ — 2026-08-06 해소: 거절(`409 PROJECT_COMPLETED`) + 재개 후 수정, 완료 전이 재설계에 포함(결정 기록). ~~MCP 담당 확인 대기~~ 2026-08-06 확인 완료. 카탈로그 공백 2건(전사/타부문 가동률 scope · `list_maintenance_logs` id 의미)은 ROADMAP M-1 항목으로 등재 — ~~`list_maintenance_logs` id는 pms 도메인 확인 필요~~ **2026-08-06 해소**(유지보수 재설계 결정 기록): 프로젝트:계약 1:1(이관)이나 프로젝트 없는 계약(OEM 직접 등록) 존재 → projectId 단순화 불가, 계약/이슈 id 유지. ~~MCP 담당 확인 대기~~ 2026-08-06 확인 완료(결정 기록)

## 세션 로그 (트랙 분리 이전 공용 기록 — 2026-08-02 이후는 트랙 파일에)

### 2026-07-31 — v3 신설: 하네스 초기 구성 + 자산·결정 승계
- 완료: 하네스 구성(CLAUDE.md 헌법·PROGRESS/ROADMAP·/next·/wrap-up·reviewer 에이전트·guarded verify.sh·Stop 훅·권한). 재사용 자산 이관(reference/seed, frontend). 결정 기록 승계(신규 7 + 승계 3)
- 검증: verify.sh 사전 코드 단계 SKIP 정상, Stop 훅 exit 0, bash -n 통과
- 미해결: 위 미해결 이슈 2건
- 다음 작업: P단계 ① PRD v2.0 작성
