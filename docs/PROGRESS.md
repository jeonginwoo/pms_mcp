# 진행 상태 원장 (PROGRESS — 공용)

> **모든 세션은 이 파일 + 자기 트랙 파일을 읽는 것으로 시작한다.**
> 세션 로그·트랙별 다음 작업은 `PROGRESS-host.md` / `PROGRESS-pms.md`에 기록한다.
> 이 파일에는 공용 상태와, 양쪽에 영향을 주는 결정만 남긴다.

## 현재 상태 (2026-08-03)

- **단계:** P(기획) — **게이트 P 대상 문서 5종 + 유저 시나리오 작성 완료**(문서 5종 2026-08-02~03 · 유저_시나리오 2026-08-03: 챗 25종 + PMS 웹 여정 6종, 소유 공용 — 챗은 host·웹 §10은 pms 재량, 승인 대기). 권한 모델 확정(2026-08-03, 상위 PRD §4 재작성 — 양측 합의 성립). 다음: eval-cases 이관 → 게이트 P 승인
- **협업:** 2인 — MCP 담당(host 앱 + pms 내 `/mcp` 어댑터) / PMS 담당(도메인·서비스·프론트)
- **코드:** 없음(새 시작). 재사용 자산: `reference/seed/`(사원 44·프로젝트 382 JSON), `frontend/`(React 프로토타입, M1에서 재연동)
- **차단 요소:** 없음

## 결정 기록 (Decision Log)

> 신규 결정 + 구 프로젝트에서 승계한 결정. 승계분은 (승계) 표기 — 원 기록은 `C:\Projects\pms_mcp\docs\PROGRESS.md`.

| 날짜 | 결정 | 근거 |
|------|------|------|
| 2026-08-03 | **권한 모델 확정분 MCP 담당 확인 완료** (양측 합의 성립 — 공용 문서 변경 확정). host 관점 영향: 도구 카탈로그·`update_progress` 2단계 확인 프로토콜 **변경 없음**(참여자 진척률 수정은 기존 US-A2 동작 유지). **`whoami`는 유효 권한 미반영**(현행 유지 — id·이름·팀·부문·orgRole만 반환). 프로젝트별 역할 노출(`search_projects` 상세 응답 `myRole`)은 **M-1 목업 실험 후 결정**으로 유예 | 프로젝트 역할은 프로젝트마다 개별 판정이라 전역 응답인 `whoami`에 유효 권한을 실을 수 없음(전역으로 의미 있는 orgRole은 이미 반환). 권한 판정은 서버가 쓰기 시점에 최종 수행(원칙 3·4) — LLM이 권한을 몰라 생기는 최악은 거절 응답 전달이지 우회가 아님. 응답 스키마 변경은 협업 접점(four-in-one 재작업)이라 `get_project` 분리와 동일 원칙(실험 근거 먼저)으로 유예 |
| 2026-08-03 | **권한 모델 확정 — 합집합 판정 + 프로젝트 역할 3단** (상위 `PRD.md` §4 재작성). ①**판정은 합집합**: `canDo = orgPerm(orgRole) OR projectPerm(프로젝트 역할)` — orgRole을 선행 게이트로 쓰지 않고, 프로젝트 역할이 위로만 덮어쓴다(축소 없음) ②**프로젝트 역할 = PM / PL / 참여자 3단**(구 "관리자/담당자" 대체 — orgRole ADMIN과 명칭 충돌 해소). PM 1명 필수 · PL 복수 가능 · 프로젝트마다 개별 판정 ③권한 배분: 정보 수정 PM·PL / 배정·M/M 입력 PM만 / 진척률 PM·PL·참여자 모두(`Project.progress`가 단일 값이라 "본인 몫" 부분 수정 개념 없음 — 기존 US-A2·`update_progress` 동작 유지) / 삭제 PM만 ④**orgRole은 프로젝트 밖 행위만** 담당 — 프로젝트 생성(TEAM_LEAD·DIVISION_HEAD·ADMIN 열거, 서열 미정의) · 조직/사용자/직급 관리(ADMIN). 프로토타입 기능 플래그 5종 미채택, **부문장 `editProgress:false` 폐기** ⑤가시성은 프로젝트 역할이 확장하되 **프로젝트 컨텍스트 내로 한정**(타 팀 인원의 프로젝트 밖 데이터는 조직 가시성 규칙 유지) ⑥PL 복수는 스키마로 수용하고 API 제약으로 유예 ⑦시드 적재: `managerId`→PM, 나머지 `assigneeIds`→참여자, **PL은 미지정**(시드에 근거 없음) ⑧ADMIN은 전 프로젝트에서 PM과 동일 권한(PM 부재·퇴사 수습) ⑨enum 값은 `{PM, PL, PARTICIPANT}` — orgRole `MEMBER`와 이름 충돌 회피 | 시드 실측: **382건 중 91건(24%)이 PM의 orgRole=MEMBER** — orgRole 선행 게이트면 24%가 막힌다. `managerId`가 `assigneeIds`에 305/305 포함 → PM도 참여자, 배정 테이블의 역할 컬럼 하나로 표현 가능. PM 편중(1인 128건·55건·36건)은 PM이 총괄/영업 성격이고 실무 리드가 별도임을 시사 → PL 계층 신설 근거. 프로토타입 `canEdit`(orgRole `editProgress` 선행) ↔ `canManage`(`pmId` 선행) 비대칭은 같은 원인의 실제 결함(`frontend/src/store.jsx:175-188` — 프로토타입에 PL 개념은 없음). 복수 PL은 나중에 필요해질 때의 비용(스키마 마이그레이션+API+MCP 도구 응답=협업 접점 변경)이 지금 열어두는 비용(조인 1회)보다 크게 비대칭 |
| 2026-08-02 | **프로토타입 대조 검토 반영** (`frontend/` = 구 백엔드 실연동본, 사용자 승인): ①웹 진행률도 MCP와 동일 2단계 confirmed 프로토콜(`PUT /progress`) ②내 계정(프로필·비밀번호·알림 설정, EPIC H)·사용자 CRUD(US-E2)·감사 조회 API(G1-3) 채택 ③**도구 카탈로그에 `whoami` 추가**(6→7종, FR-AI-16 — `GET /api/me` 동일 서비스) ④chat BFF 접점(`/api/chat`·`/feedback`) 명세 ⑤SSE `?access_token=` 인증 명시 ⑥화면 3종(홈·인력·설정) 부록 A 추가 ⑦email 로그인 + 초기 비밀번호 `proten1!` 확정 ⑧**배정은 기간 모델 유지**(프로토타입 월별 upsert는 재연동 시 조정) ⑨상태 전이 서버 강제 AC(US-A5) ⑩유지보수 경로 `/api/maintenances/{id}/logs` 통일. **권한 모델 재기술(orgRole 기능 플래그·managerId·HQ 집계 제외)은 pms 담당 결정 보류** | 프로토타입은 실연동으로 검증된 동작 — 명세로 승격하는 쪽이 재작업 최소. whoami는 카탈로그 변경(협업 접점)이라 본 기록 경유 |
| 2026-08-02 | **PRD 3문서 체계**: 상위 `docs/PRD.md`(공유 정의 — 용어·권한 모델·가동률 산식·대상 사용자·시드 기준·협업 접점. **공용 소유**, 변경은 결정 기록 경유) + `docs/PRD-host.md`(AI 어시스턴트, host 트랙 소유) + `docs/PRD-pms.md`(PMS 구현명세, pms 트랙 소유 — 구 "PMS — AI 구현용 PRD" v1.0 현행화 이관, 원본 전사 `reference/PMS_구현용_PRD_v1.0.md`, 충돌 7건 수정은 ROADMAP P단계 항목에 명세). 겹치는 정의는 상위가 유일 원본, 하위는 참조만 | 두 하위 문서는 독자·변경 주기가 다르고(도구/Eval vs 도메인/AC) 소유 경계가 트랙으로 분리됨. 겹치는 정의의 원본 방향 문제(host↔pms)는 상위 단일 원본으로 해소(당초 "원본=PRD-pms" 등재를 같은 날 정정). PDF 원본은 세션 밖에서 소실되므로 전사 보존 |
| 2026-08-02 | **대상 사용자 정정**: 솔루션사업부 40명(구 문서 전제) → **프로텐 전 직원**(시드 44명·7부문). 조직 단위는 부문(division), orgRole = ADMIN/DIVISION_HEAD/TEAM_LEAD/MEMBER | 사용자 정정 + `reference/seed/people.json` 근거. 구 문서 기반 작업 시 잔재 주의 |
| 2026-08-02 | **2인 분업 전환**: MCP 담당 = host 앱 + pms 내 `/mcp` 어댑터, PMS 담당 = 도메인·서비스·프론트. 모노리포 유지, 하네스는 디렉터리 스코프로 분리(host/·pms/ CLAUDE.md, verify 스코프 인자, PROGRESS 트랙 분리) | 원칙 2(임베디드 `/mcp`) 때문에 MCP 담당도 PMS 코드베이스 안에서 작업 — 리포 분리 시 어댑터 소유권·계약 문서 공유가 애매. 두 개발자 모두 Claude Code 사용. 협업 경계 = MCP 도구 카탈로그 + 애플리케이션 서비스 API, 변경은 이 결정 기록 경유 |
| 2026-08-02 | **구 conventions 3종 이관** (`docs/conventions/` — java-spring·react-ts·git-workflow). v3 정합화: ralph·`/verify`·boundary-reviewer·단일 PROGRESS 참조를 현행(verify.sh 스코프·reviewer·트랙 파일)으로 수정, CI 전제는 "CI 도입 전 verify 결과 첨부"로. 전량 주입 대신 스코프 CLAUDE.md에서 참조. Modulith 모듈 목록은 구 설계 승계 표기 — M0 스캐폴드에서 확정 | 2인 협업 + 원격 개설로 git 워크플로 규칙의 고통 시점 도래(증설 예약 발동). 코드 컨벤션은 M0 전 동료 온보딩 자료로 함께 이관 |
| 2026-07-31 | **새 폴더(pms_mcp_v3)에서 완전 재시작.** 기존 pms_mcp는 읽기 전용 참고 | 구 저장소의 중복 문서 부채(가이드 고유 가치 ~40%)·2인용 ruleset과 절연. 결정과 재사용 자산만 승계 |
| 2026-07-31 | 스택: **Boot 4.1 + Modulith 2.1** + Spring AI 2.0.0 | Boot 4.0은 2026-12 OSS EOL. Spring AI 2.0.0이 양쪽 지원, 신규 시작이라 상향 비용 없음 |
| 2026-07-31 | **MCP stateless 지향 설계** (구조 원칙 7) — 구현은 2025-11-25 스펙, 세션·Sampling·Roots·Logging 비의존 | 2026-07-28 스펙 대개정 출시 확인(stateless화, 12개월 유예). Java SDK 신 스펙 지원은 모니터링 항목 |
| 2026-07-31 | PRD v2.0에 저비용 3종: `scope=ME` · 조회 도구 version 반환 · 도구 오류/점검 응답 규칙 FR | version 미반환은 쓰기 시나리오가 도구 카탈로그만으로 완결되지 않는 결함. 공백 4건(투입 조회·유지보수 만료/SR·기간 범위·스킬/지연)은 후순위 후보 |
| 2026-07-31 | 개발 기준 모델 **claude-sonnet-5** (G3에서 단가·학습 미사용 조항과 재확인) | 비용·지연·Eval이 모델에 묶여 있어 기준 모델 없이는 수치 검증 불가 |
| 2026-07-31 | `get_project` 분리 — **M-1 목업 실험 후 결정** | 도구 카탈로그 변경은 four-in-one 재작업. 실험 근거 확보가 먼저 |
| 2026-07-31 | LLM **학습 미사용 조항 확인 = M0 게이트 전제** (사람 작업) | 법무·구매 리드타임 — 실패 시 대안(국내 리전·온프렘) 검토 시간 확보 |
| (승계 07-13) | 재사용 자산은 시드·프론트만, 백엔드는 재작성 | 데이터·UI는 코드 품질과 무관한 자산 |
| (승계 07-18) | 지시 파일 영어, 기록물 한국어 | LLM 지시문은 영어가 안정적. 기록물은 사람용 |
| (승계, v2 학습) | 하네스 부품은 고통 확인 후 추가 — 빈 섹션·선제 부품 금지. ralph·conventions·CI는 증설 예약 목록에 | 완성본 선이식이 이해도 저하와 유지 부채의 원인 (todo-cli 샌드박스 학습 결론) |

## 미해결 이슈

- MCP Java SDK의 2026-07-28 스펙 지원 릴리스 모니터링 (지원 시 구현 노트·검증 절차 갱신)
- ~~구 PRD 열린 질문 #1·#3~~ — 2026-08-02 PRD-host v2.0에서 처리 완료(#1 30일 명문화, #3 M3 이월 명시)
- ~~PRD 2문서 분리에 대한 PMS 담당 합의 확인~~ — 2026-08-02 확인 완료, PRD-pms v2.0 작성 완료
- ADMIN(대표) 조직 가시성 = 전사 — 상위 PRD §4에 반영(시드 orgRole 근거), **게이트 P에서 최종 승인**
- ~~**권한 모델 재기술 여부 (pms 담당 결정)**~~ — 2026-08-03 결정 완료(합집합 판정 + PM/PL/참여자 3단, 위 결정 기록). ~~MCP 담당 확인 대기~~ — 2026-08-03 확인 완료(`whoami` 유효 권한 미반영·`myRole`은 M-1 유예 — 위 결정 기록)
- **가동률 집계에서 대표 직속(HQ_TEAM '프로텐') 제외 여부** — 구 "권한 모델 재기술" 항목에 묶여 있던 잔여 건. 권한이 아니라 집계 규칙이므로 분리 — 상위 PRD §3 가동률 정의에 반영할지 미결
- 시드 적재 정책 잔여(PMS-M1 전) — 배정 월별 M/M 부여 규칙·유지보수 데모 데이터 (계정 규칙은 2026-08-02 확정: email + `proten1!` — PRD-pms 부록 B)
- **완료 상태 프로젝트의 진척률 수정 허용 여부** — US-A2에 상태 제약 AC가 없음(유저_시나리오 §7 발견 #3, 2026-08-03). eval 기대값 채점에 필요 — pms 트랙 확인 항목. 카탈로그 공백 2건(전사/타부문 가동률 scope · `list_maintenance_logs` id 의미)은 ROADMAP M-1 항목으로 등재

## 세션 로그 (트랙 분리 이전 공용 기록 — 2026-08-02 이후는 트랙 파일에)

### 2026-07-31 — v3 신설: 하네스 초기 구성 + 자산·결정 승계
- 완료: 하네스 구성(CLAUDE.md 헌법·PROGRESS/ROADMAP·/next·/wrap-up·reviewer 에이전트·guarded verify.sh·Stop 훅·권한). 재사용 자산 이관(reference/seed, frontend). 결정 기록 승계(신규 7 + 승계 3)
- 검증: verify.sh 사전 코드 단계 SKIP 정상, Stop 훅 exit 0, bash -n 통과
- 미해결: 위 미해결 이슈 2건
- 다음 작업: P단계 ① PRD v2.0 작성
