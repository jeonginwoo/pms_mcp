# 원천 문서 3종 점검 보고서 (2026-07-13)

> 대상: `PMS_AI기능_PRD.md`(v1.1) · `PMS_MCP_구현_가이드.md` · `기술_선택_근거.md` (모두 2026-07-02 작성)
> 방법: 3문서 상호 대조 + 기존 구현(`C:\Projects\pms_mcp`) 대조 + 기술 스택 현행성 웹 검증.
> 결론 먼저: **골격은 전부 유효. 폐기 대상 없음.** 아래는 갱신·결정이 필요한 지점.
>
> **후속 (2026-07-13 당일 반영 완료):** D-1=SSE 채택, D-2=개발 중 모델 무관·출시 전 확정, D-3=PRD v1.2 확정 승격. 2장 기계적 수정도 전부 반영됨. 이 보고서는 기록용으로 보존.

---

## 1. 사용자 결정 필요 (문서 수정 전에 결정부터)

### D-1. 알림 전달 — 폴링(문서) vs SSE(기존 구현) ★최중요
- 세 문서는 일관되게 **주기 폴링(30초)** 선택: 기술_선택_근거 9장, 구현 가이드 1-A/1-B ("SSE는 확장점").
- 그런데 기존 구현은 **SSE 즉시 푸시**로 만들었고, 재사용하기로 한 `frontend/`에 SSE 구독 로직이 이미 들어 있음.
- 선택지: ① 문서대로 폴링 → frontend에서 SSE 구독 제거 필요 ② 구현대로 SSE → 기술_선택_근거 9장·가이드 1-A 개정 필요.
- 결정 후 PROGRESS 결정 기록에 남길 것.

### D-2. LLM 모델 선정 (PRD 열린 질문 5 — 미결)
- 구현 가이드 예시는 `claude-sonnet-5`(현행 모델명, 유효), 기존 구현은 `claude-sonnet-4-6`(구세대).
- PRD 9장 비용 모델의 "단가는 모델 선정 후 확정"도 이 결정에 걸려 있음.
- 권고: sonnet-5 기준으로 확정하고 PRD 9장 단가 추정 갱신 + **학습 미사용 조항 확인**(출시 전제, PRD 8장)을 미해결 이슈로 등록.

### D-3. PRD 상태 승격
- PRD가 아직 "v1.1 초안 (팀 리뷰 대기)"인데 하네스·eval의 원천으로 실사용 중. 리뷰를 마치고 v1.2 확정판으로 올리든지, 최소한 "구현 기준 문서"로 상태를 바꿔 기록할 것.
- 열린 질문 1·3·4(대화 이력 보관, M3 프롬프트 1순위, 👎 리뷰 담당)도 M1 전까지 답 필요.

---

## 2. 기계적 수정 (결정 불필요 — 다음 세션에서 일괄 반영 가능)

### 구현 가이드
- **PRD 장 번호 참조가 v1.0 기준이라 어긋남** (가이드 머리말도 "PRD v1.0 초안" 기준이라 명시):
  - 6-3 "PRD 3장 시스템 프롬프트" → v1.1은 **4장**
  - 6-3 "PRD 2장 입출력 명세" → v1.1은 **3-3**
  - 7-1 "PRD 2-1: 2,000자" → v1.1은 **3-1 (FR-AI-02)**
  - 11장 "지연 목표(PRD 4장)" → v1.1은 **5-1**
  - 머리말 참고 문서 표기 "PRD(v1.0 초안)" → v1.1로
- **4-4 curl 예시 `protocolVersion: 2025-06-18`** → 현행 안정판은 **2025-11-25**. Spring AI 2.0 GA(2026-06-12)의 MCP SDK가 어느 리비전을 협상하는지 확인 후 예시 갱신.
- **2026-07-28 MCP 스펙 대개정 주의 노트 추가 권장**: 차기 리비전이 stateless화 — initialize 핸드셰이크·`Mcp-Session-Id` 제거. 가이드 4-4의 "Mcp-Session-Id를 이후 요청에 첨부" 문구와 11장 STATELESS 언급에 각주 필요. SDK가 따라오면 목업(M-1) 검증 절차도 달라질 수 있음.
- 12장 M1 체크리스트에 "Eval 셋 30케이스 작성"이 있는데, 초안은 이미 `docs/evals/eval-cases.md`로 존재 — "초안 완료, 목업으로 확정" 상태를 반영.

### PRD
- 관련 문서 표기에 이 저장소 하네스 문서(ROADMAP·eval-cases) 추가.
- 대상 사용자 "약 40명" vs 시드 데이터 44명 — 수치 통일(사소).

### 기술_선택_근거
- 1장 "6개 모듈" → 구현 가이드 3-1·7-1이 추가한 **mcpconfig·chat(BFF)** 반영해 8개로 (모듈 = 바운디드 컨텍스트 6 + 지원 모듈 2로 서술해도 됨).
- 6장 TypeScript — 재사용 frontend가 JSX임을 각주로: "프로토타입은 JS, 신규 파일부터 TS 점진 전환" (`docs/conventions/react-ts.md`와 동일 문구).
- D-1 결정에 따라 9장 개정 여부 결정.

### 하네스 연쇄 수정
- CLAUDE.md 모듈 목록 "identity·project·resource·maintenance·notification·common" → **+ chat·mcpconfig**.
- ROADMAP·PROGRESS의 "(PRD 12장)" → **PRD 11장** (PRD는 11장까지만 있음). ※ 이 항목은 점검 직후 수정 완료.

---

## 3. 검증 결과 이상 없음 (갱신 불필요)

- **버전 스택 전부 현행**: Spring AI 2.0.0 GA(2026-06-12) · Spring Boot 4.0 · Modulith 2.0 · Java 25 — 기존 구현이 이 조합으로 실제 빌드·구동됐으므로 검증된 조합.
- 구조적 원칙 6개(역할 배치·내장 서버·레이어·패스스루·2단계 쓰기·인젝션 방어): 세 문서·기존 구현·하네스 전부 일치. 흔들 것 없음.
- 도구 카탈로그(조회 5 + 쓰기 1 + whoami): PRD 3-2 = 가이드 5-2 = 기존 구현 7개 일치.
- 부록 B 목업 전략·승격 경로: 그대로 유효. M-1 진행 시 이 문서가 곧 매뉴얼.
- 기술_선택_근거의 선택 논리(모놀리식·JPA·PostgreSQL·JWT·낙관적 락 등): 조건(40명·1인 개발) 불변이므로 결론도 불변.

## 참조 (웹 검증 근거)

- Spring AI 2.0.0 GA: https://spring.io/blog/2026/06/12/spring-ai-2-0-0-GA-available-now/
- MCP 스펙 현행(2025-11-25): https://modelcontextprotocol.io/specification/2025-11-25
- MCP 2026-07-28 대개정 RC: https://blog.modelcontextprotocol.io/posts/2026-07-28-release-candidate/
