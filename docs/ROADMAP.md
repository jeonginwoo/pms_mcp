# ROADMAP

> 게이트 없이 다음 단계로 넘어가지 않는다. 게이트 통과는 **사람이 승인**하고 PROGRESS 결정 기록에 남긴다.

## P단계 — 기획 (완료 — 게이트 P 통과 2026-08-09)

> 아래 항목은 **P단계 당시의 산출 기록**이므로 버전 표기도 그때 값이다. 산출물의
> **현행 버전**(2026-08-26 실측): 상위 PRD v1.0 · PRD-host **v2.6** · PRD-pms **v2.29** ·
> 기술_선택_근거 v2.0 · 구현 노트 **v1.1** · 유저 시나리오 **v1.5** · eval-cases **v1.8**
> (+ `docs/evals/seed-anchor-map.md` **v1.1** — 2026-08-23 신설, eval 인물·수치 앵커 정본).

- [x] **PRD-host v2.0** (2026-08-02 작성 → `docs/PRD-host.md` + 공유 정의는 상위 `docs/PRD.md`로 승격, 게이트 P 통과 2026-08-09) — 구 PRD(v1.2) 기반 재작성: 허점 6종 수정(측정 불가 기준·요구 충돌·version 반환 구멍·용어 정의 누락·열린 질문 처리·시효 정보), 2026-07-31 결정 반영(`scope=ME`·version 반환·오류/점검 FR·기준 모델·get_project 조건 명시). 시스템 프롬프트 전문·마일스톤 표·비용 모델은 PRD에서 분리(소유권: 프롬프트→구현 노트, 마일스톤→이 문서, 비용→기술 근거)
- [x] **PRD-pms 현행화 이관 (pms 트랙)** (2026-08-02 작성 → `docs/PRD-pms.md`, 게이트 P 통과 2026-08-09 · 분리 결정은 담당자 합의 완료) — 구 "PMS — AI 구현용 PRD" v1.0(2026-06-21, 전사본 `reference/PMS_구현용_PRD_v1.0.md`)을 `docs/PRD-pms.md`로 이관하며 충돌 7건 수정: ①스택 Boot 4.1·Modulith 2.1 ②전제 프로텐 전사 44명·2인 개발(Out of Scope의 "MS본부 2차" 무효) ③부록 B 시드 → `reference/seed/` 44명·382프로젝트 기준 재작성(가동률 검증 케이스 실제 인물로 재지정) ④MCP를 2차가 아닌 M0 `/mcp`로(모듈 목록 6고정 해제 — M0 스캐폴드에서 확정) ⑤마일스톤 라벨 M0~M6 → **PMS-M0~M6** 개명(본 ROADMAP의 M-1~M3와 충돌 방지) ⑥알림 30초 폴링 → SSE(구 2026-07-13 결정) ⑦조직 가시성에 ADMIN(대표)=전사 추가(시드 orgRole 정합 — 상위 `docs/PRD.md` §4로 승격, 게이트 P에서 최종 승인)
- [x] **기술_선택_근거 v2** (2026-08-03 작성 → `docs/기술_선택_근거.md`, 게이트 P 통과 2026-08-09) — 버전 명기(Boot 4.1·Modulith 2.1·Spring AI 2.0.0, 근거 §3-1 신설), MCP 절 stateless 갱신(§11-6 신설), 1인 전제 → 2인·전사 44명 갱신(2차 확장 전제 삭제), "어떻게" 서술(SSE 구현·낙관적 락 절차·인증 체인)은 구현 노트로 이동 표기, 비용 모델 §13 신설(PRD-host FR-AI-06에서 소유권 이관), 구 NFR 번호 참조 제거
- [x] **구현 노트** (2026-08-03 작성 → `docs/구현_노트.md`, 게이트 P 통과 2026-08-09) — 구 가이드의 고유 가치만 이관(~40%): 인증 체인(audience validator·PAT를 JWT로·BFF 위임 토큰), 예외→도구 에러 매핑(404 은닉·409), `spring.ai.mcp.client.enabled: false`의 이유, 부록 A(ProLLMOps 판단)·부록 B(목업 전략, seed JSON 기준으로 갱신). 추가 수용: 시스템 프롬프트 전문 v0.1(PRD-host §5 위임)·SSE 인프라 공통분(기술_선택_근거 §9 위임). 구 가이드의 제거된 API(`customizeRequest`)는 현행(`httpRequestCustomizer`)으로 교체 기재
- [x] **유저 시나리오 생성** (2026-08-03 작성 → `docs/유저_시나리오.md`, 게이트 P 통과 2026-08-09) — 시드 실존 인물 페르소나 8명(합집합 키스톤: PM인데 orgRole=MEMBER인 전세아 포함) × 시나리오 25종(SC-01~25): 조회 9·쓰기/권한 7·경계 응답 4·**실패/오남용 5**(쓰기 대상 모호·확인 카드 취소·범위 밖 값/완료 전이 미고지·도구 결과 인젝션(원칙 6)·자격증명 입력), eval 30케이스 분류 커버리지 표(§9) 포함. **PMS 웹 여정 6종(WS-01~06, §10 — pms 트랙 재량)** 추가: 생애주기 한 줄기(명화공업 앵커)·역할 교체·관리자/감사·알림·웹 권한 경계("챗=화면" 검증)·409 — PMS-M1~M4 데모·수용 경로, AC 참조만(재정의 없음). 가동률·유지보수 기대값은 시드 공백(월별 M/M·유지보수 데모)으로 유예(§8). 공백 3건 발견(§7 — 전사/타부문 가동률 scope 부재 · `list_maintenance_logs` id 의미 미정의 · 완료 프로젝트 진척률 수정 AC 부재) → M-1 실험·pms 확인 항목
- [x] **eval-cases 이관** (2026-08-03 작성 → `docs/evals/eval-cases.md`, 게이트 P 통과 2026-08-09) — 구 30케이스의 분류·비중을 감축 없이 유지하고 실패·오남용 6케이스 확장 = **36케이스**(쓰기 확인 3→8 · 오류/점검(FR-AI-26) 1 신설 — 유저_시나리오 §9가 이관 시로 위임한 비중 결정, PROGRESS 결정 기록). 화자를 시드 실존 인물로 고정(가시성별 채점 — 구 양시온 등 비실존 인물 폐기), 용어·모델 정합화(본부→부문 · 합집합 판정 · `scope=ME` · `whoami` 케이스 신설), 기준일 2026-08 고정. 입력 문구·기대값은 M-1 목업 후 확정
- [x] **화면 프로토타입 (pms 트랙 재량 — 게이트 P 리뷰 보조)** (2026-08-09 작성 → `prototype/`) — "기획한 기능이 맞는지" 눈으로 확인하기 위해 PRD-pms v2.4 부록 A 화면 전체를 백엔드 없이 목업 데이터(시드 44명·382건 그대로)로 재현: phase 탭·진척률 2단계·완료/재개·권한 커스텀 패널·이력 탭·유지보수 3층(계약/사이트/이슈)·가동률 billable 집계·404 은닉/403/409/422 의미론. 사용자 전환으로 44명 권한·가시성 검증 가능. 기존 `frontend/`는 참고용 보존(예전 기획 반영본) — M1 재연동 기반은 그때 결정. **명세 아님** — 충돌 시 PRD가 원본. M-1 목업(MCP 서버)과는 별개·병행
- [x] **게이트 P**: 사람이 문서 5종(상위 PRD·PRD-host·PRD-pms·기술_선택_근거·구현 노트) + 유저 시나리오 + eval 분류 승인 — **2026-08-09 통과**(사용자 승인, PROGRESS 결정 기록)

## M-1 — 사전 검증 (목업)

- [x] 목업 MCP 서버 (구현 노트 목업 전략, stateless 지향으로 검증) — **스캐폴드 + B2-0 배관 검증 완료**(2026-08-10, `pms-mcp-mock/` — 도구 7종·테스트 32개·오염 레코드 기심음). **B2-1 자연어 검증 완료**(2026-08-10 동석 실험 + 전 라운드 재실행 — 조회·권한·경계·인젝션 통과, **실패 2건(D-06·A-06 — 확인 규칙 계열)** → 프롬프트 §4·description 보강 근거, `docs/evals/B2-1-자연어검증.md`). **B2-2 JWT 권한 흐름 완료**(2026-08-13 — caller-id 프로퍼티 → HS256 토큰 `sub`, §1-1 체인 예행. 인증 3케이스·화자별 가시성·404 은닉 E2E 테스트 7종). **B2-3 자체 호스트 연결 완료**(2026-08-13 — `host/` 앱 스캐폴드 + 챗~호스트~목업 MCP 실 LLM 관통 3케이스: scope=ME 가동률·후속 턴 메모리+과부하 원인·404 은닉 정본 문구)
- [x] `get_project` 분리 여부 결정 (2026-07-31 유예) — **분리 불요 확정**(2026-08-12 결정 기록. B2-1 근거: 2단 도약 자연 완주 — §5)
- [x] `search_projects` 상세 응답 `myRole` 포함 여부 결정 (2026-08-03 유예) — **불포함 확정**(2026-08-12 결정 기록. B2-1 근거: 역할 확인 헤맴 0 — §5)
- [x] 카탈로그 공백 2건 해소 — `get_utilization` 전사/타부문 scope · `list_maintenance_logs` **id 확보 경로** — **B2-1 공백 실증 완료**(모델이 `scope=COMPANY` 지어냄 · id 미도달 — §5). **확장안 구체화·pms 측 결정 완료**(2026-08-11 결정 기록 2행 — ③ scope에 `COMPANY` 추가 + 응답 team·division, ④ `search_maintenance` 신설로 7종→8종). **MCP 담당 확인 완료 — 양측 합의 성립 + host 반영 완료**(2026-08-12 — 목업 8종화·테스트 39개·PRD-host v2.4·시스템 프롬프트 v0.2 확정·eval-cases v1.5)
- [x] eval 입력·기대값 확정 — **2026-08-12~13 완료**(eval-cases **v1.6**): B2-1 §5 승격 + 시드 시뮬레이션으로 36케이스 전량 기입(산출 전제 3건 명문화 — 가동률 모집단=진행중 배정·반올림 ±1%p·이슈→계약 링크 사이트명 기준(~~pms 확인 대기~~ → **2026-08-14 확인 완료 — 사이트명 포함 확정**, 결정 기록)). **C류 시드 재앵커**(한국수출입은행·롯데관광 계약이 시드에 부재 → 한국거래소 101). **F-01 `[]` 쟁점 해소 = 유보형 기대값 채택**(R2-5 모범 기준). **재실험 전량 통과**(2026-08-13 B2-1 §6 — `search_maintenance` 흐름·사이트명 키·COMPANY 한 방·404 은닉)
- [x] **게이트 M-1**: 핵심 시나리오(가동률·오버부킹·쓰기 확인 카드) 목업 통과 — **2026-08-17 통과**(사용자 승인, PROGRESS 결정 기록. 실측 근거: B2-1 §5·§6 + B2-3 실 LLM 관통 3케이스)

## M0 — 뼈대

- [x] PMS 백엔드 스캐폴드 (Modulith 모듈 경계 + 경계 테스트) — **2026-08-17 완료**(`pms/` Gradle 프로젝트 — Boot 4.1·Modulith 2.1.0·Java 25, 모듈 6종 확정(결정 기록)·경계 테스트 2종+도메인 순수성 ArchUnit+컨텍스트 스모크, PG(compose)/H2(테스트). **이후 무효화**: 2026-08-21 재구축이 이 스캐폴드를 대체했다 — 현행 모듈 6종은 이때의 6종과 다른 집합(person·auth·project·resource·notification·audit)이고, 테스트 DB는 H2를 버리고 Testcontainers PG 전용이며, "도메인 순수성 ArchUnit"은 3계층 전환으로 `LayerRuleTest`가 됐다(PRD-pms §0·§3))
- [x] `/mcp` 엔드포인트 + 인증 체인 (토큰 패스스루·audience) — **2026-08-18 완료**(host 트랙 — 목업 `mcp/`·`port/` 승격 = `pms/`의 7번째 모듈 `mcp`(루트=port 계약·internal=도구 8종+보안 체인), audience 검증·JWKS 우선/HS256 폴백(**전환 유예 — 결정 기록**, 트리거=실 발급 체계 등장), 임시 시드 어댑터(인력 44명 실데이터·나머지 4포트 FR-AI-26 503), 게이트 인증 3케이스 E2E 예행 포함 테스트 16개)
- [x] 시드 데이터 적재 (`reference/seed/`) — **identity분 2026-08-18 완료**(pms 트랙: `PersonSeedLoader`(구 `IdentitySeedLoader` — 2026-08-21 재구축에서 `person/seed/`로 이동·개명) — 인력 44+시스템 계정·조직 트리 18노드·직급 9·기본 그룹 4종·User 45, 기동 시 빈 DB 자동 적재·멱등·시드 id 정합 보증). projects 382건·maintenance 시드는 **아직 미적재** — 해당 EPIC(A·C·D) 구현 시 각 모듈이 적재한다(PRD-pms §10)
- [x] **LLM 학습 미사용 조항 확인 (사람 작업 — 게이트 전제)** — **2026-08-20 확인 완료**(사용자 — 게이트 M0 판정과 함께 접수)
- [x] **게이트 M0**: 인증 3케이스 (본인/타인/무토큰) — **2026-08-20 통과**(사용자 승인, PROGRESS 결정 기록. 실서버 실측: 무토큰 401·타 audience 401·정상 토큰 whoami=그 사용자 화자 2명 교차. 부수: JWKS 전환 스위치 활성화 — 전환 스모크 3종 실측)

## M1 — 조회 전용 → M2 — 안전한 쓰기 → M3 — 프롬프트·외부

- 큰 줄기: 조회 6도구 + whoami → eval 게이트(**G1**) → `update_progress` 2단계 쓰기 → 운영 검증(**G2**) → 프롬프트·확장(**G3**: 모델·단가·학습 조항 최종 확인) (조회 6도구 = 2026-08-11 결정 ④ 8종화 반영)
- **라벨 각주**(2026-08-24): 위 세 단계의 "조회/쓰기" 구분은 **챗(MCP 도구) 노출 기준**이다. PMS 웹 자체 기능의 쓰기 구현(EPIC D·E·F 등)은 M1 안에서 진행되며, M2가 여는 것은 `update_progress` 도구의 **사내 공개**다(도구 자체는 2026-08-23 실연결 완료 — G2 대기). 게이트 품질 조건은 `docs/PRD-host.md` §6-4가 원장이다
- **G1 선행이 전부 끝났다 — 다음은 게이트 실행이다**(2026-08-24 재갱신): 도구 **8종 전량 실연결**(person 2 + maintenance 2 + project 2 + **가동률 2** — 2026-08-24 배선으로 마지막 503이 사라졌다). host 몫이던 **eval 코퍼스 시드 재앵커**도 완료됐고, 앵커 수치는 **도구 관통으로 재확인**됐다(앵커 정본 §7-1). **2026-08-24 실서버 전환도 끝났다**(실 LLM 관통 14관측 — 치명 0 · 불합격 2는 문구 몫으로 G1 준비 이월). **eval 자동 실행 장치도 끝났다**(2026-08-24 — 러너·주입 3종·DB 초기화·회차 기록. 기전 6종 실측). **채점층도 끝났다**(2026-08-25 — 규칙층 + LLM-Judge `claude-opus-5`. 실행과 분리돼 회차 기록만으로 돌고 재채점이 된다). **G1은 2026-08-25에 실행해 1차 미달이었고, 2026-08-26에 통과했다**(아래 항목 · 사용자 승인). 문구 수정 3건(B-03 도구 description · A-08·F-01 프롬프트 v0.3)은 효과가 3회차에 걸쳐 확인됐다. 남아 있던 차단 사유 **미확인 쓰기(F3)** 는 면제된 것이 아니라 **G2로 이월**됐다 — F3는 `update_progress`에서만 나고 §6-4에서 그 도구를 여는 게이트가 G2이기 때문이다. **그 선행 중 규칙 3 보강은 2026-08-27에 끝났다**(프롬프트 **v0.5** — 아래 항목). **M2 착수 전에 남은 선행은 전량 36케이스 회귀 회차 하나**다.

### M1 — pms 트랙 (owner: PMS 담당)

세부 상태는 `docs/PRD-pms.md` §10(EPIC 기준 상태표)이 원장이고, 여기에는 **G1까지의 순서**만 둔다.

- [x] **project·person 루트에 가동률용 계약 신설** — **2026-08-23 완료**. `project.AssignmentDirectoryService`·`MonthlyAssignment`(그 달과 겹치는 배정을 행 단위로) + `person.WorkforceDirectoryService`·`WorkforceProfile`(capacity·billable·gradeCoeff·team·division·subtree 인원). 착수 전 가정("분자만 없다")이 실측에서 깨졌다 — 분모·모집단·계수·조직 표시까지 전부 경로가 없어 계약이 둘이 됐다. 부수로 AC B2-1 종료일 당김(`close()`가 종료 시점을 남기지 않아 "종료월 이후 제외"가 표현 불가였다). **MCP 담당 확인 요청 중**: 시그니처가 `UtilizationEntry`·`OverbookedEntry.Cause`를 채우기에 충분한가 · port 의존 방향(`project → mcp` 구현 vs `mcp → project` 호출 — 두 기록이 갈린다)
- [x] **감사 조회 2뷰** (G1-3 `/api/audit` · G2-2 `/projects/{id}/audit`) — **2026-08-23 완료**. 판정은 골격 단계에서 이미 실구현이었어서 파생 질의·스냅샷 역직렬화만 남아 있었다. 정렬은 호출자가 뒤집을 수 없게 저장소 메서드 이름이 정한다. 부수: 통합 목록의 최신순 인덱스가 없어 Flyway **V8** 신설(V3의 둘은 선행 컬럼이 달라 쓰이지 못했다)
- [x] **projects 시드 적재** — **2026-08-23 완료**. `ProjectSeedLoader`가 382건을 §5 상태 전이를 실제로 밟아 적재(배정 포함), 부록 B의 M/M 부여 규칙으로 2026-08 오버부킹을 재현. **부수 발견**: 인원 정본이 `seed_org_proten.sql`(실제 명부)인데 문서는 구 익명 명부(`people.json`) 이름을 194곳에서 참조하고 있었다 — eval 채점이 이름 대조를 포함하므로 **G1 전 재매핑 필요**(PRD-pms §12 등재)
- [x] **도메인 루트 계약 승격 (안 ② 도메인 몫)** — **2026-08-23 완료**. `maintenance.MaintenanceLookupService` · `project.ProjectLookupService` · `project.ProgressCommandService` + `WorkforceProfile` 조직 id 2종(MCP 요청) + `PersonRef.division`. 승격 전에는 `PersonTools`만 실연결이고 나머지 4도구가 503이었다. **남은 것은 `UtilizationTools`뿐**이고 그건 EPIC C 구현에 묶인다. 부수: 유지보수 계약·이슈 id를 시드 원본 번호로(eval 앵커 101이 우연에 기대던 것·ISSUE 갈래가 죽어 있던 것을 함께 해소)
- [x] **EPIC C 가동률 실구현** (C1-1~C1-6) — **2026-08-23 완료**. `UtilizationQueryServiceImpl` 본문 구현 + 모집단 판정을 `UtilizationPopulation`으로 분리(`ProjectVisibilityService` 선례 — 규칙이 개인 지정/집계 · 전사/제한 두 축으로 갈려 산식과 한 클래스에 두면 읽는 사람이 매번 되짚는다). **착수 전 오산 정정 2건**: ①"C1-4는 이벤트 재계산"으로 봤으나 골격 javadoc이 이미 답을 적어 뒀다 — 조회 시점 계산이라 구현할 것이 없고 **증명(통합 테스트)만** 남아 있었다 ②실측한 계약 공백 — 전사 scope 가시성은 `unrestricted`라 `visiblePersonIds`가 **빈 집합**이어서(제약 없음 ≠ 아무도 없음) 집계 호출자가 명단을 얻을 경로가 없었다 → `WorkforceDirectoryService.findAllAggregatablePersonIds()` 신설(공개 API 변경 — 결정 기록). 부수: 시드 테스트가 **산식을 자체 구현**하고 있어(정본 두 벌) 실서비스 호출로 교체 · M/M 합의 부동소수점 노이즈가 `기본 > 100` 판정을 뒤집을 수 있어 6자리 반올림. 테스트 347 → **368**
- [x] **EPIC D 유지보수 — 조회분(D-a)** — **2026-08-23 완료**. maintenance 모듈 신설(모듈 6 → 7종) · 엔티티 4종 + Flyway **V9** · 시드 적재(계약 105·사이트 157·이슈 14) · D4-1 목록(keyword 3종 매칭)·D4-2 상세·D4-3 전사 공개 · D3-4 이슈 조회(미배정 필터). **eval C류 앵커 2종을 통합 테스트로 고정** — 계약 101 이슈 7건 · "가천대길병원"이 사이트명으로만 45사이트 계약에 도달. 시드↔모델 괴리 결정 7건은 공용 결정 기록 참조
- [x] **`resource` 루트 계약 승격** (`get_utilization`·`list_overbooked`가 붙을 면) — **2026-08-24 완료**. **소유자를 먼저 정했다**(공용 결정 기록 — git-workflow §3 "One promotion, one owner"를 처음 실제로 밟았다: 루트 파일은 PMS 담당, 어댑터 배선은 MCP 담당). 루트 4종 신설 — `UtilizationLookupService`·`UtilizationScope`(ME·MY_TEAM·DIVISION·COMPANY·PERSON)·`UtilizationBrief`·`OverbookedBrief`(+`Cause`). **단순 이동이 아니었다 — 공백 2건이 실측으로 드러났다**: ①웹은 `?orgUnitId=`를 호출자가 주지만 챗은 scope 낱말만 오므로 "MY_TEAM이 누구인가"를 푸는 쪽이 없었다 → resource가 든다(어댑터에 두면 웹·챗의 답이 갈릴 수 있다) ②`list_overbooked`가 요구하는 `Cause(projectName, mm)`를 낼 자리가 없었다 — `UtilizationView`가 프로젝트별 행을 버린다 → **산식·모집단·`기본>100` 판정을 `UtilizationCalculator`로 뽑아** 웹은 causes를 버리고 MCP는 싣는다(정본 한 벌). 부수: `UtilizationView.overbooked()` 제거(같은 규칙이 두 곳에 있었다). 테스트 368 → **409**
- [x] **프론트엔드 미연동 화면 5종 붙이기** — **2026-08-24 완료**. 백엔드가 실구현인데 화면이 없던 지점 전부: **가동률**(`/utilization` — 월 선택·팀 필터·기본/보정·과부하만 보기) · **유지보수 계약 목록/상세**(D4-1·D4-2 — keyword 3종 서버 필터, 사이트·연락처·이슈 요약) · **유지보수 이슈 목록**(D3-4 — 담당자·고객사 상시 노출, 내 담당/미배정) · **통합 감사 로그**(G1-3) · **프로젝트 상세 이력 탭**(G2-2). 알림(F1-3)은 백엔드가 501이라 제외했고, 유지보수·이슈의 등록·수정 버튼도 만들지 않았다(D 쓰기는 라우트 자체가 없다 — conventions §3 "있는 엔드포인트만 부른다"). 감사 표는 두 화면이 공유한다(같은 행의 두 필터 뷰라 표를 두 벌 만들면 같은 데이터가 두 모양이 된다). **실측 3건**: ①유지보수 열거는 **질의=이름·응답=한국어 라벨**로 비대칭이다 ②시드만 적재한 DB의 `audit_logs`는 **0행**이다(시드는 감사 행을 남기지 않는다) ③전사 화자(박재완)는 경영관리팀 소속이라 `scope=MY_TEAM` 집계가 정당하게 빈다. 검증 `verify.sh pms` PASS
- [x] **EPIC D 쓰기 — D2 계약·사이트(D-b1)** — **2026-08-24 완료**. 유지보수에 쓰기 입구가 처음 생겼다: `POST`/`PUT /api/maintenance/contracts[/{id}]` · `POST /contracts/{id}/sites` · `PUT /api/maintenance/sites/{id}`. 조회 계약과 **쓰기 계약을 갈랐다**(`ContractCommandService`) — 조회는 전사 공개라 화자를 받지 않고(D4-3) 쓰기는 "계약 관리" 플래그가 판정한다(D2-3). 계약 id는 §4 표대로 `max(id)+1`(하드 삭제가 없는 칸이다). **실측 3건**: ①시드에 **기간 역순 계약이 1건 실재**해 "종료일 > 시작일"을 걸지 않았다(AC에 없는 규칙은 실 데이터를 거부한다) ②수정 응답의 `version`이 flush 전 값이라 그 값으로 재수정하면 409였다 → `saveAndFlush`(project 선례). 단위 테스트로는 잡히지 않는 종류다 ③`SiteView`에 `version`이 없어 PUT 루프가 닫히지 않았다(§7 요구) → 신설. 사용자 결정: 수동 입력 연락처의 `raw`는 조각을 시트와 같은 순서로 **조립**한다 — 조립본이 `ContactParser`를 왕복한다. 테스트 435 → **464**
- [x] **EPIC D 쓰기 — D3 이슈(D-b2)** — **2026-08-24 완료**. D3-1 등록(담당 기본값 = 사이트 engineerId · `max(id)+1`) · D3-2 상태 전이·재배정 · D3-3 append-only 코멘트 + 화면 3종(목록 등록 버튼·행별 처리 모달·계약 상세의 사이트 절). **선행 결정 2건이 둘 다 닫혔다**: ①`NotificationType`에 **`ISSUE_ASSIGNED("이슈 담당 지정")`** 추가(사용자 결정 — 수신자가 담당자이므로 "등록"이 아니다. 설정 화면은 `Object.keys(enabled)`로 그리므로 6칸이 자동으로 됐다) ②**§12 "notification 루트 계약의 자리" 판정 완료** — D3-1이 마지막 반증 기회였고 **반증되지 않았다**(실측 외부 호출 0건. maintenance가 발행하고 notification이 구독한다) → `service/`로 내리는 것이 맞다는 판정만 기록하고 **이동은 별 단위**로 뒀다(순수 리팩터라 D3의 성격과 다르다). **착수 전 실측 4건**: ①`MaintenanceIssue`에 변경 메서드가 하나도 없었다(`of` 하나) — 전이·재배정·`requireVersion`이 이번 몫 ②전이 그래프를 `IssueStatus`에 뒀다(`ProjectStatus.next()` 선례 — 구 javadoc은 "쓰기 구현의 몫"이라고 적었지만 그러면 열거를 읽는 사람이 무엇이 허용되는지 모른다) ③**`ContractWriteGuard`를 재사용하면 틀린다** — US-D3은 로그인 사용자 전체라 계약과 판정 축이 다르다(관문 없는 쓰기 계약이 처음 생겼다) ④`IssueView`에 `statusCode` 신설(`ContractDetail.statusCode` 선례 — 라벨만 주면 화면이 라벨→이름 표를 갖고 그것이 서버 열거와 어긋난다). **부수 실측 2건**: 감사 스냅샷은 **JSON을 왕복해 숫자가 `Long`으로 돌아오지 않는다**(id를 담는 자리의 단정은 숫자로 읽어야 한다) · 코멘트는 **감사 행을 남기지 않는다**(자기가 이미 불변 기록이다). **파생 공백 등재**: **재배정(D3-2)은 알리지 않는다** — §8에 그 이벤트가 없고 AC도 등록만 적었다(`AssignmentChanged`에서 수정·종료를 안 알리는 것과 같은 자리). 테스트 496 → **531**
- [x] **EPIC D 이관(D-c)** — **2026-08-25 완료**. D1 `POST /projects/{id}/handover` — **EPIC D가 닫혔다**. **선행 2건이 둘 다 풀렸다**: ①`Project.handover()` 신설(§5에 완료→유지보수중이 없어 `next()`로는 못 간다 — 전용 경로만이 만든다는 규칙을 `complete()`·`reopen()`과 같은 모양으로 지켰다) ②**모듈 방향 = 역포트**(`project.HandoverPort` — project 정의, maintenance 구현. 사용자 결정 — 공용 결정 기록). **착수 전 실측이 방향을 정했다**: project와 maintenance는 서로를 **전혀 모르는 형제 모듈**이었고(둘 다 person·audit·common만) 순환이 강제하는 답이 없었다 → 잠재 의존이 이미 가리키는 쪽을 골랐다(maintenance가 `sourceProjectId`를 저장하고 D4-2가 원 프로젝트 링크를 요구한다 — 반대로 그으면 그때 순환이다). **실측 3건**: ①`ContractCommandService`를 재사용하면 **틀린다** — 모든 경로가 `ContractWriteGuard`를 지나 "계약 관리" 플래그 없는 PM이 자기 프로젝트를 이관할 수 없게 된다(D3에서 배운 것과 같은 자리) ②`ProjectAction.HANDOVER` 신설 — `COMPLETE_REOPEN`은 배정 전원이고 이관은 PM 하나다 ③**포트 시그니처를 두 번 고쳤다**: 쓰지 않는 계약 id를 돌려주다 지웠고, 감사 행위자를 사이트 담당자로 추측하던 것을 화자 인자로 바꿨다(append-only 로그에 하지 않은 사람이 남을 자리였다). **순서가 AC 두 줄로 정해졌다** — D1-2("아무것도 안 바뀜")가 상태 확인을 계약 생성보다 앞에, D1-3("상태 전이도 미발생")이 입력 검증을 전이보다 앞에 둔다. 원자성은 **통합 테스트가 실물 행 수로 실증**한다(목은 "저장을 안 불렀다"까지만 말한다). 이관 알림 수신자는 §8에 없어 **구현이 정했다**(사이트 담당 엔지니어 — 등재). 유형은 `PROJECT_COMPLETED` 재사용(사용자 결정). **부수 발견 등재**: `ProjectCompleted`·`ProjectReopened`가 **한 번도 발행되지 않고** F3-3 회수는 실사용 호출자가 0건이다. 시연 앵커 = 명화공업(부록 B). 테스트 531 → **559**
- [x] **EPIC E 쓰기 5종 골격 채우기** (E1-1·E2-2·E3-2·E4·E5) — **2026-08-24 완료**. 501 골격 **13 → 4**(남은 4는 notification뿐이다). 골격이 라우트·권한 판정(403)·DTO를 다 세워 둬서 impl 본문 + 감사 기록 + AC 테스트가 남아 있었다. **착수 전 실측으로 정해진 것 3건**: ①**id 발급** — 직급·권한 그룹은 하드 삭제가 있는 명시 id라 조직 노드와 같은 칸이다(부록 B 규칙) → **시퀀스**(Flyway V11). `max(id)+1`은 조직 노드에서 실제로 데이터를 오염시킨 방식이다 ②**E3-2 이름 중복은 막지 않는다**(사용자 결정 — AC에 없는 규칙을 구현이 지어내지 않는다) ③**E1-2 경고는 응답에 싣는다**(사용자 결정). ③이 모듈 경계를 하나 넓혔다: 배정 건수를 세려면 person이 project를 알아야 하는데 그건 순환이라 **`person.AssignmentCountPort`를 person이 정의하고 project가 구현**한다(`AccountPort` 선례). 부수: §4에 참조 데이터 id 발급 규칙을 승격(부록 B 한 줄에 지나가듯 있던 것). 테스트 409 → **427**
- [x] **project 잔여 AC** — ~~A6-3(역할 지정)~~ **2026-08-24 완료**(A6-3·A6-6·A6-7 + 배정 패널 드롭다운·확인 모달. 현 PM을 이 경로로 내리는 요청도 422로 막는다 — 허용하면 PM 없는 프로젝트가 생긴다) ~~`?phase=` 목록 필터~~ **2026-08-25 완료**(§7 `?phase=SALES|SOLUTION` + 화면. **화면 배치는 같은 날 사용자 결정으로 개정됐다** — 한 목록의 단계 탭이 아니라 내비 항목 둘(영업=SALES · 프로젝트=SOLUTION)이고 "전체" 화면은 없다(§5의 "유지보수중은 계약이 담당한다"가 화면에서도 성립한다 · 서버 변경 0 · 공용 결정 기록). **설계 지점 하나가 이 항목의 전부다** — §5가 "phase는 status 파생값이고 원본을 이중화하지 않는다"고 정하는데, 단건 응답은 status→phase로 읽고 목록 필터는 phase→status 집합으로 **반대 방향**을 읽는다. 방향마다 표를 손으로 쓰면 저장 컬럼이 아니라 코드에서 이중화가 재현되고 상태가 하나 늘 때 한쪽만 고쳐도 아무것도 실패하지 않는다 → `ProjectPhase`가 열거 상수에 status 집합을 실어 두고 `of()`·`statuses()` 양쪽으로 읽힌다(`Project.getPhase()`의 손으로 쓴 switch가 사라졌다). 목록 응답에도 파생 `phase`를 실었다 — 빼면 탭을 그리는 화면이 그 표의 **세 번째 사본**을 갖는다. 필터는 가시성 판정 **뒤에** 얹히므로 범위를 넓히지 못하고, 모르는 값은 스프링 열거 변환 실패가 §7 봉투 400으로 답한다 — 컨트롤러에서 다시 검사하면 같은 판정이 두 곳에 생긴다) → ~~A8~~ **2026-08-26 완료. EPIC A가 닫혔고 M1 pms 트랙의 마지막 칸이었다**(권한 커스텀 — Flyway **V16**. **설계는 §4-2가 이미 정해 뒀다**: 조정 가능한 칸은 {PL,참여자}×{정보수정,배정,진척률,완료·재개} 8개뿐이고 PM 열 전체·이관 행은 고정이다. **표를 `ProjectPermissionRules` 한 벌, 병합을 `EffectiveProjectPermissions` 한 곳**에 둔 이유는 읽는 쪽이 둘이기 때문이다 — 쓰기 판정과 A8-1 조회가 갈리면 화면이 "할 수 있다"고 그린 칸에서 서버가 403을 낸다. 저장은 전체 교체이고 **기본값과 같은 칸은 행을 남기지 않는다**("행이 없다 = 기본값"이 불변식 · `overrides: []`가 전체 복원이라 별도 API가 없다). **실측이 순서를 정했다**: 가시성 검사를 잠금 질의보다 먼저 하면 엔티티가 이미 영속성 컨텍스트에 올라와 `OPTIMISTIC_FORCE_INCREMENT`가 **먹지 않아** version이 0에서 그대로였고, 그러면 두 PM이 같은 version으로 각자 저장해 나중 것이 상대 매트릭스를 통째로 덮는다. 테스트 606 → **625**)
- [x] **EPIC F 알림 + SSE + 스케줄러** (F1~F3) — **2026-08-25 완료. EPIC F가 닫혔다.** F1(2026-08-24) 이벤트 배관 + 과부하·멱등·목록·읽음·설정 필터 · F2·F3(2026-08-25) 스케줄러 + 회수 배선 · **F1-4 SSE**(2026-08-25). **SSE의 설계 지점 3가지**: ①**쿼리 토큰은 이 라우트 하나로 좁혔다** — common의 `CallerIdentityResolver`를 넓히면 모든 라우트가 `?access_token=`으로 불릴 수 있게 되고 토큰이 로그에 남는 자리가 앱 전체가 된다. 그래서 `StreamCallerResolver`를 notification 안에 따로 두고 보호 체인에서는 이 경로만 `permitAll`(토큰이 헤더에 없어 리소스 서버 필터가 못 읽는다) ②**이미터는 웹 타입이라 `controller`에만 둘 수 있다**(`LayerRuleTest`)는 것이 설계를 정했다 — 적재는 service에서 일어나는데 service → controller는 금지라, 서비스가 모듈 내부 이벤트(`NotificationStored`)를 던지고 컨트롤러가 받아 밀어낸다 ③**푸시 실패는 삼킨다** — 예외를 올리면 "보내지 못했다"는 이유로 적재가 롤백돼 F1-4의 "미연결이면 재연결·재조회 시 반영"과 정반대가 된다. 재연결 복구는 **목록 재조회**(서버 재생 `Last-Event-ID`는 같은 세션 리뷰에서 걷어냈다 — 재생분이 그 사람의 모든 연결로 브로드캐스트돼 다른 탭 커서를 되감았다. 재연결도 브라우저가 아니라 앱이 직접 한다) · `X-Accel-Buffering: no`. **구조 교정 1건**: SSE 조각 5개를 `controller/`에 평평하게 쌓았다가 `impl/token`·`impl/scope` 선례대로 `controller/stream/`으로 모았다(종류가 섞이면 폴더를 쪼갠다). **배포 요구 1건 등재**: Nginx 로그 포맷에서 `access_token` 마스킹 필수. 테스트 576 → **589**
- [x] **EPIC H 내 계정 잔여** (`/api/me/account` · H1-2·H1-3) — **2026-08-25 완료. EPIC H가 닫혔다.** ~~H1-4 notif-prefs~~는 2026-08-24 EPIC F와 함께 완료. **설계 판단 2건**: ①**`/api/me/*`는 데이터를 가진 모듈이 갖는다**(H1-4 notif-prefs가 notification에 있는 선례) → **비밀번호는 auth가 통째로 가져 모듈 경계를 아예 넘지 않는다**. `AccountPort`를 넓혀 person을 거치게 했다면 person이 해시·현재 비밀번호 확인을 알게 됐을 것이다. 프로필(H1-2)만 두 모듈에 걸쳐 있어 포트를 쓴다 — 이름은 person, email·phone은 auth이고 **한 트랜잭션**이다 ②**`Person.rename` 신설** — `update(name, orgUnitId, gradeId, groupId)`(E2-2 관리자 경로)를 재사용하면 내 프로필 수정이 **자기 권한 그룹을 스스로 바꾸는 길**이 되고, 지금 값을 그대로 넘기는 방식도 칸이 늘면 조용히 초기화한다. **실측 2건**: ①`emailTaken`(등록용 "아무도 안 쓴다")을 수정에 쓰면 **전화번호만 바꾸려는 사람이 자기 email 때문에 409**를 받는다 → `emailTakenByOther` 신설 ②테스트 id 블록 6xx가 이미 `OrgAdminWritesIntegrationTest`의 것이어서 낙관적 락 실패로 충돌했다 — 100단위 블록도 실측하고 골라야 한다. 테스트 589 → **596**

### M1 — host 트랙 (owner: MCP 담당)

도구 실연결의 순서는 pms 진도에 종속된다(위 pms 절). 아래는 **어댑터 쪽에서 해야 하는 일**만 둔다.

- [x] **`/mcp` 어댑터 재승격 (`pms-old/` → `pms/`)** — **2026-08-23 완료**. 승격 방식을 재구축 구조에 맞춰 바꿨다(공용 결정 기록): 도구 8종·응답 DTO·예외 매핑은 `mcp` 모듈이 소유하고, 각 도메인은 **자기 모듈 루트에 조회 계약을 올려** 어댑터가 그것만 부른다 — 의존이 `mcp → 도메인` 한 방향이라 `mcp/`를 지워도 pms는 그대로 돈다. 디코더는 새로 만들지 않고 auth의 `accessTokenDecoder`를 받는다(audience=pms·token_type=access가 이미 부착 — 빈을 하나 더 만들면 타입 주입이 모호해져 웹 인증이 깨진다). `pms.auth.enabled`와 무관하게 `/mcp`는 항상 토큰을 요구한다(원칙 4). 테스트 11건(인증 3케이스·체인 순서·위조 서명·refresh·카탈로그 8종·가시성·503)
- [x] **person 포트 실연결** — **2026-08-23 완료**. `person/PersonLookupService`(+`PersonIdentity`) 루트 승격 → `whoami`·`find_person`이 실 DB 응답. 가시성 판정은 `PersonService`를 그대로 불러 **챗과 화면이 같은 답**을 내게 했고, `whoami`의 부문은 person 안에서 조직 트리를 올라가 채운다(`MeView`에 없는 값). 권한 플래그는 담지 않는다(FR-AI-16)
- [x] **project 포트 실연결** (`search_projects`·`update_progress`) — **2026-08-23 완료**. 도메인 계약은 PR #25가 올렸고(`ProjectLookupService`·`ProgressCommandService`) 이 항목은 **어댑터 배선**이다. **쓰기 도구가 처음 실연결됐다**: 2단계 확인·권한·낙관적 락·완료 규칙은 내부 유스케이스가 그대로 갖고 어댑터는 `confirmed` 왕복과 표현 변환만 한다(구조 원칙 5) — 프로토콜을 두 곳에 두면 한쪽이 확인을 건너뛰는 길이 생긴다. `summary` 한 줄만 어댑터가 만든다(도메인에 없는 필드 · 커밋 뒤에는 현재값=요청값이 되므로 두 단계의 문장이 갈린다). **404 은닉은 maintenance와 반대로 도메인이 든다** — 프로젝트는 가시성 밖이 부재와 같이 숨어야 하고(A3-2) 유지보수는 숨길 것이 없다(D4-3). 카탈로그 문구 3건 정정(절단 50건 명시·keyword 부분 일치·status 5종 — 결정 기록, PRD-host v2.6). 테스트 13건: 가시성 화자별 갈림 · 절단 50 · 상태 필터 19건 · 422 · 부분 일치(토큰 AND 아님) · 상세 version · 404 은닉 · 2단계 확인 양단 · 409 낙관적 락 · 403 비담당자
- [x] **resource 포트 실연결** (`get_utilization`·`list_overbooked`) — **2026-08-24 완료. 이 배선으로 도구 8종이 전부 실연결됐고 `/mcp`에 503으로 답하는 자리가 0건이 됐다**. 도메인 계약은 PR #34까지가 올렸고(`UtilizationLookupService`·`UtilizationScope`·`UtilizationBrief`·`OverbookedBrief.Cause` — 소유자 결정은 결정 기록) 이 항목은 **어댑터 배선**이다: 503 2건 제거 · `Brief` → `UtilizationEntry`·`OverbookedEntry` 변환. **어댑터가 범위를 판정하지 않는 것이 이 배선의 요점**이다 — `scope` 낱말을 조직 id로 바꿔 넘길 수 있었지만 그러면 "MY_TEAM이 누구인가"의 답이 어댑터에 남아 웹과 챗이 갈린다. 어댑터가 새로 든 것은 **`"yyyy-MM"` ↔ `YearMonth`** 하나뿐이고(계약이 `YearMonth`를 주고받는다) 그 파싱 실패 문구도 `ToolError`에서 나온다. **앵커 정본을 도구 관통으로 고정**(테스트 13건 — §7-1 표 9종 + 빈 달 2종). 부수 2건: ~~`assignedMm` 부동소수점 잡음~~ 해소 확인(도메인 반올림이 받았다) · **eval F-02 기대값 정정**(아래 eval 항목)
- [x] **maintenance 포트 실연결** (`search_maintenance`·`list_maintenance_logs`) — **2026-08-23 완료**. 도메인 계약은 PR #25가 올렸고(`maintenance.MaintenanceLookupService`) 이 항목은 **어댑터 배선**이다: 503 제거 · 절단 50건과 404 문구를 어댑터가 든다(도구 description·`ToolError`가 이 모듈 소관) · `contractId`를 nullable로 정정(계약 미연결 이슈를 0으로 내보내던 자리). **화자를 넘기지 않는 유일한 도구 묶음**이다(전사 공개 AC D4-3). eval C류 앵커를 **도구 관통으로** 고정: 사이트명으로만 45사이트 계약 도달 · `search_maintenance`가 준 계약 id로 이슈 7건 · **그 이슈 id로 ISSUE 갈래 도달**(eval C-04 전제 — #25의 시드 원본 id 재부여가 열었다)
- [x] **eval 코퍼스 시드 재앵커** — **2026-08-23 완료**. 캐스팅은 **id 보존 + 역할 라벨 정정 + 화자 교체 4건**으로 갔다 — `projects.json`·`maintenance.json`이 인물을 id로만 참조하므로 id를 지키면 프로젝트·배정·수치 앵커가 그대로 살고(실측 대조 7종 동일), 깨지는 축은 권한 그룹·직급계수·`billable` 셋뿐이다. 착수 전 전제("이름은 기계적 치환")는 실측에서 깨졌다 — 구 과부하 3인 중 하나가 실 명부에서 `billable=false`라 모집단에서 빠지므로 수치는 전량 재산출이었다. 산출: eval-cases **v1.7** · 유저_시나리오 **v1.5** · **앵커 정본 `docs/evals/seed-anchor-map.md` + 재현 SQL** 신설. 부수로 미해결 2건(팀원 scope SELF→TEAM · A2-9 진행중 제한)의 host 파급 확인 완료. **시드 구조상 A-02의 부문 경계 판별력이 사라진 것**은 손실로 명기하고 확장 후보에 등재(사용자 승인)
- [x] **host 앱 실서버 전환** — **2026-08-24 완료**. `pms.mcp.base-url` → 8080(목업은 오버라이드로 존치) · 토큰 발급 `host/scripts/pms-token.sh`(email·비밀번호를 시드 정본에서 읽는다) · 실서버 관통 IT 4건(`-Dpms.real.e2e=true` — 8종 카탈로그·whoami·ME 앵커·refresh 거절 + B-01 6건 앵커). **실 LLM 관통 14관측: 통과 12 · 불합격 2 · 치명 0**(기록 `docs/evals/실서버-관통-실측.md`). 쓰기 5관측에서 **미확인 실행 0건**이고 감사는 `confirmed=true`에만 1행(`source=MCP`)이다. **host 결함 1건을 찾아 고쳤다** — Anthropic 다중 content 블록에서 `.content()`가 첫 generation만 읽어 **답 전체가 빈 문자열로 나갔다**(목업은 응답이 작아 블록이 하나여서 드러날 수 없었다). 불합격 2건은 모델 문구 쪽(scope 오선택·결과 자기축소)이라 four-in-one 규칙대로 **G1 준비로 넘겼다**
- [x] **감사 `source=MCP` 실측** — **2026-08-23 완료**. 어댑터에 배선할 것이 없다는 판단이 맞았고(`AuditSourceResolver`가 `/mcp` 경로 접두사로 판정), 쓰기 도구가 실연결된 그 세션에 관통으로 확인했다: `confirmed=true` 직후 그 프로젝트의 최신 감사 행이 `source=MCP`다(기동 시 시드 적재분은 `WEB`이라 구분된다). 쓰기 도구 없이는 실측할 방법이 없던 항목이라 배선과 같은 세션에서 닫혔다
- [x] **eval 자동 실행 스크립트 + 오류 주입 장치** — **2026-08-24 완료**. `host/scripts/eval-run.sh`(DB 초기화 → pms 기동 → 러너) + `EvalRunnerIT`(원장 순서로 36케이스, `-Deval.run=true`로만 돈다). **원장은 `eval-cases.md` 한 벌로 유지**했다 — 실행용 기계 판(`cases.json`)에는 화자·발화·대화 체인만 담고 기대값을 담지 않으며, `EvalCasesDriftTest`가 id·순서·분류별 개수·화자를 고정한다(불일치 시 문서가 이긴다). **주입 장치는 2종이 아니라 3종이었다** — 원장이 명시하지 않은 **D-03 동시 수정**(확인 카드 사이 version 올리기)이 필요했다. **되돌리기가 러너의 요구사항으로 드러났다**: 원장의 기대값은 단독 실행 기준이라(D-06 카드 = 5%→20%) D류가 서로의 전제를 부순다 → 케이스마다 시드 값 복원 + 감사 행 삭제. 기준선 오염은 `audit_logs` 0행으로 검사한다(앵커 수치를 코드에 복제하지 않고 같은 것을 증명한다). 도구 흐름은 코드를 더하지 않고 SDK DEBUG를 in-process appender로 받는다 — **인자까지 기록되어 F5 판정이 기록만으로 된다**. 기전 6종 실측 완료(단턴·대화 체인·쓰기 2단계·동시 수정·오염·오류 — `docs/evals/results/20260824-2143/`). **채점층과 36케이스 전량 실행은 다음 항목**
- [x] **eval 채점층 (규칙층 + LLM-Judge)** — **2026-08-25 완료**. `host/scripts/eval-grade.sh` + `EvalGraderIT`(`-Deval.grade=true`로만 돈다. `--rules-only`는 LLM 비용 0). **실행과 분리했다** — 입력이 회차 기록뿐이라 pms·DB 없이 돌고, 채점 규칙을 고친 뒤 같은 회차를 다시 매길 수 있다. **원장은 여전히 한 벌**: 규칙층은 기대값 없이 서는 것만 보고(수치 출처·확인 절차·카탈로그 밖 도구·길이·근거 시점), 프로즈 기준은 Judge가 **원장 행 그대로**를 루브릭으로 받아 판정한다(`claude-opus-5` — 기준 모델과 다른 모델, 자기채점 편향 회피). 선행으로 **카탈로그 결함 1건 정정**(읽기 전용 7종이 `destructiveHint=true`로 광고되던 자리 — 회귀 규칙상 도구 표면이 바뀌면 전량 재실행이라 기준 회차 **전에** 넣었다). 검증: 기존 6케이스 회차를 규칙층·Judge 양쪽으로 채점(치명 0 · Judge 6/6 합격)
- [x] **게이트 G1** — **2026-08-26 통과 (사용자 승인 · 결정 기록)**. 조건은 36케이스 · 치명(F1~F4) 0건 · 합격률 ≥ 90%(33/36) · 사람 승인이었고, **합격률은 33~34/36으로 충족**했다. 치명 0은 **미확인 쓰기(F3)** 하나가 남아 있었으나(v0.3 3회차 중 2회차에 D-08·D-02로 재현), **F3는 `update_progress`에서만 나는 결함이고 §6-4에서 그 도구를 여는 것은 G1이 아니라 G2다** — 그래서 결함을 면제한 것이 아니라 **그것을 판정하는 게이트로 이월**했다(PRD-host §6-4 G2 조건 "쓰기 Eval 통과(F3 0건)"이 이미 그 자리다). **G1이 여는 것은 조회 6도구 + whoami이고 그 범위에서는 치명 0이다.** 통과 판정 당일 실 구동을 관통으로 확인했다(pms 8080 + host 8081 + 실 LLM — `RealPmsConnectivityIT` 4건 PASS · `/chat` 3질의 정상). **이월 조건**: `update_progress`는 G2 전까지 사내 공개 대상이 아니다. 기록 `docs/evals/results/20260825-{1100,1350,1410,1421}/`
- [x] **M2 선행 ① 프롬프트 규칙 3 보강** — **2026-08-27 완료**. 프롬프트 **v0.4 → v0.5**(구현_노트 §4-1 정본 · `SystemPrompts.java` 사본 · `SystemPromptsDriftTest`). G1이 G2로 이월한 **F3(미확인 쓰기)** 가 표적이었고, 규칙 3에 **"쓰기 요청에 대한 첫 응답이 곧 `confirmed=false` 호출"** 과 **"확인 문구는 그 응답의 `summary`"** 를 명시했다. 결과: T1 확인 카드 누락이 **2/3회차 → 0/3회차**(D군 8케이스 3회차 · `--rules-only` · 회차 `20260827-{0852,0856,0900}`)이고 **DB 물증이 3회차 전부 동일**하다(쓸 것만 `auditRows=1`, 막을 것은 전부 0). **중간에 부작용을 하나 만들었다가 되돌렸다** — 첫 판(v0.4)이 동의 유효성을 **카드 제시 순서**로 써서 모델이 T2에 카드를 새로 만들 때마다 직전 동의를 스스로 무효로 읽었고 **D-08이 커밋하지 못했다**(회차 `20260827-0845`). **"확인 카드는 변경 1건에 한 번"**(횟수)으로 바꿔 풀었다. 규칙 8은 판별 기준을 두고 **적용 범위만 조회 질의로 한정**. `verify.sh` 전 스코프 PASS
- [x] **M2 선행 ② 규칙 8 표적 회귀 회차** — **2026-08-27 완료 · 회귀 0**. 회차 `20260827-0919`(A-05·A-06·A-07·A-08·F-01 5/5 합격 · `--rules-only`). **규칙의 양쪽 끝이 그대로 섰다**: A-06은 도구를 부르지 않고 되물었고, A-08은 되묻지 않고 `get_utilization(MY_TEAM, 2026-08)` 1회로 진행하며 기준을 밝혔다(수치도 원장 기대값 일치). **전량 36케이스는 G2 판정 회차로 옮겼다**(공용 결정 기록 · PRD-host **v2.9** §6-3) — 회차가 두 층이 됐다: 병합 시점은 표적 회차, 게이트·정기 Eval 시점은 전량. **게이트 통과는 전량 회차 없이 선언하지 않는다**. 실측 비율: 전량 2회차 = 72 케이스 실행 vs 표적 회차 = 5
- [ ] **M2 선행 ③ 잔여 관측 — T2 재카드** — 커밋 직전에 `confirmed=false`를 한 번 더 부르는 호출이 v0.5에도 남는다(회차별 0·3·2건). 멈추지는 않았지만 **v0.4에서 D-08을 정지시킨 것이 이 모양**이다. 문구를 더 조이는 것은 위험하다 — v0.4가 실증했듯 이 자리를 순서로 서술하면 확인 루프가 된다. **G2 전량 회차에서 재확인**한다


## 하네스 증설 예약 (해당 고통이 발생하면 그때 추가)

- **ralph 루프**: 계획 합의된 기계적 작업 큐가 생기면
- ~~**conventions 분리**~~: 2026-08-02 완료 — 구 conventions 3종을 `docs/conventions/`로 이관(v3 정합화), 스코프 CLAUDE.md에서 참조
- ~~**CI + 원격 저장소**~~: 2026-08-02 완료 — 원격(origin) 연결 + 최소 CI(`.github/workflows/ci.yml`, verify.sh 실행·실패 시 로그 아티팩트). Gradle 캐시는 M0, `npm ci`는 M1에 추가. 경량 2인 워크플로 — 구 프로젝트의 과한 ruleset 반복 금지
- ~~**eval 자동 실행 스크립트**~~: 2026-08-24 완료 — `host/scripts/eval-run.sh` + `EvalRunnerIT`(주입 3종·DB 초기화·회차 기록). 위 M1 host 절 참조
