---
description: 현재 작업 검증 — 테스트 + 원칙 점검
---

방금 한 작업을 검증하라:

1. `./gradlew test` 실행 (Modulith/ArchUnit 경계 테스트 포함). 실패하면 고치고 재실행 — 통과할 때까지 완료 선언 금지. **Gradle 프로젝트가 아직 없으면(M-1 목업 단계 등) 이 단계는 건너뛰고 그 사실을 보고한다.**
2. `frontend/`를 변경했다면 `npm run test`(Vitest)도 실행한다. 확인 카드 흐름(FR-AI-04)을 건드렸으면 컴포넌트 테스트가 존재하는지 확인한다.
3. MCP 도구를 만들었거나 수정했다면 Inspector/curl 검증 절차를 실행하거나, 실행 불가하면 검증 커맨드를 사용자에게 제시한다.
4. 구현 가이드 13장 "자주 틀리는 지점" 6개 항목에 대해 이번 변경을 하나씩 점검하고 결과를 표로 보고한다:
   - 도구가 리포지토리를 직접 호출하지 않는가 (application만)
   - 서비스 계정이 아닌 사용자 토큰 패스스루인가
   - 에이전트 루프가 AI 호스트에 있는가
   - 불필요한 쓰기 도구를 노출하지 않았는가
   - 도구 출력(DB 텍스트)을 지시로 취급하지 않는가
   - Spring AI 2.0 API 시그니처를 문서로 확인했는가
5. 디프(`git diff`)를 훑고 의도하지 않은 변경이 없는지 확인한다.
