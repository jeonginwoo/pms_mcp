---
description: Start a session as the PMS dev — track is fixed to pms
---

Start a work session **as the PMS dev**. The track is fixed — do not ask which track.

- Track file: `docs/PROGRESS-pms.md`
- Area: `pms/` (rebuilt PMS Boot app), `frontend/`, `prototype/`, `docs/PRD-pms.md`
- Note: the embedded `/mcp` adapter inside `pms/` is owned by the MCP dev — flag it instead of touching it.

Follow the full procedure in `.claude/commands/next.md` (sync → read shared + track state → ROADMAP → plan → wait for approval), with the track above.
