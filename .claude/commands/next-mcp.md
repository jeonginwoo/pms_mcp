---
description: Start a session as the MCP dev — track is fixed to host
---

Start a work session **as the MCP dev**. The track is fixed — do not ask which track.

- Track file: `docs/PROGRESS-host.md`
- Area: `host/` (AI host Boot app: agent loop, LLM, MCP client), `docs/PRD-host.md`, plus the embedded `/mcp` adapter module inside `pms/`
- Note: everything else in `pms/` is owned by the PMS dev — flag it instead of touching it.

Follow the full procedure in `.claude/commands/next.md` (sync → read shared + track state → ROADMAP → plan → wait for approval), with the track above.
