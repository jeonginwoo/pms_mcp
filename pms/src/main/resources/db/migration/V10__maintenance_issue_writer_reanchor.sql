-- 유지보수 이슈의 작성 엔지니어 2명이 서로 바뀌어 있던 것을 되돌린다 (2026-08-24).
--
-- 원인: 시트→JSON 변환(2026-08-10) 당시 작성자 이름을 **구 익명 명부**로 매핑했다
-- (남진식 → 노도온 = id 26 · 배성수 → 송수람 = id 28). 2026-08-22 재구축에서 인원
-- 정본이 실제 명부(`seed_org_proten.sql`)로 바뀌면서 id 26이 배성수, 28이 남진식이
-- 됐고, 그 결과 **남진식이 쓴 이슈 7건이 배성수에게, 배성수의 7건이 남진식에게**
-- 붙었다. 두 사람 다 CS사업팀 실인원이라 화면에 그럴듯하게 보이는 것이 함정이다.
--
-- 시드(`reference/seed/maintenance.json`)도 함께 고쳤지만, 유지보수 로더는 계약이
-- 하나라도 있으면 적재 전체를 건너뛴다 — 그래서 참조 데이터의 '정정'은 여기서
-- 수렴시킨다(`V4__member_group_team_scope.sql` 선례). 빈 DB에서는 대상 행이 없어
-- 아무 일도 하지 않고, 이어서 시드가 교정된 값으로 넣는다.
--
-- 일괄 교환이 안전한 이유: D3 이슈 쓰기가 아직 없어(라우트 자체가 없다) 모든 행이
-- 시드 적재분이다. 담당 재배정(D3-2)이 열린 뒤였다면 사람이 바꾼 값까지 뒤집었을
-- 것이므로, 지금이 이 정정을 할 수 있는 마지막 시점이다.
update maintenance_issues
   set assignee_id = case assignee_id when 26 then 28 else 26 end
 where assignee_id in (26, 28);
